import { c as S, i as C, n as joinClassNames, o as M, r as u, t as fracToCssDim, u as k } from "./c912f986-C5K_a9LL.js";
import { $t as addDurations, A as getTableHeaderSticky, At as guid, B as applyStyle, C as findWeekdayText, Ct as flatArray, D as getFooterScrollbarSticky, Dt as getEventRangeMeta, E as getDateMeta, F as setRef, Ft as isPropsEqualShallow, Gt as sliceEventStore, H as computeElIsRtl, I as watchHeight, Kt as sortEventSegs, L as watchSize, M as memoize, N as memoizeObjArg, O as getIsHeightAuto, P as renderText$1, Pt as isArraysEqual, Qt as addDays, R as watchWidth, Rt as mapHash, S as findMonthText, T as generateClassName, Tt as formatWithOrdinals, U as getAppendableRoot, W as getEventTargetViaRoot, Wt as setElEventRange, Xt as classNames, _ as afterSize, _n as $, a as ContentContainer, an as diffWeeks, bt as fabricateEventRange, c as NowTimer, cn as formatIsoTimeString, ct as buildRangeEdgeOutput, dn as joinDateTimeFormatParts, dt as computeViewBorderless, en as addMs, et as EventImpl, f as Scroller, fn as multiplyDuration, ft as createAriaClickAttrs, g as WEEKDAY_ONLY_FORMAT, gn as wholeDivideDurations, h as ViewContextType, ht as createFormatter, i as BaseComponent, in as diffDays, j as isDimsEqual, jt as hasBgRendering, k as getScrollerSyncerClass, m as ViewContainer, mn as startOfDay, n as computeMajorUnit, nn as asRoughMs, o as DateComponent, ot as buildEventRangeKey, p as StandardEvent, pn as rangeContainsMarker, pt as createEmptyEventStore, r as isMajorUnit, rn as createDuration, sn as formatDayString, t as DateProfileGenerator, tn as addWeeks, un as intersectRanges, ut as combineEventUis, v as buildDateStr, wt as flatMapArray, x as findDayNumberText, y as buildNavLinkAttrs, yt as expandRecurring } from "./54e239b5-DsJsCsr-.js";
//#region node_modules/fullcalendar/chunks/4fae2a79.js
function intersectRects(rect1, rect2) {
	let res = {
		left: Math.max(rect1.left, rect2.left),
		right: Math.min(rect1.right, rect2.right),
		top: Math.max(rect1.top, rect2.top),
		bottom: Math.min(rect1.bottom, rect2.bottom)
	};
	if (res.left < res.right && res.top < res.bottom) return res;
	return false;
}
function computeClippedClientRect(el) {
	let clippingParents = getClippingParents(el);
	let rect = el.getBoundingClientRect();
	for (let clippingParent of clippingParents) {
		let intersection = intersectRects(rect, clippingParent.getBoundingClientRect());
		if (intersection) rect = intersection;
		else return null;
	}
	return rect;
}
function getClippingParents(el) {
	let parents = [];
	while (el instanceof HTMLElement) {
		let computedStyle = window.getComputedStyle(el);
		if (computedStyle.position === "fixed") break;
		if (/(auto|scroll)/.test(computedStyle.overflow + computedStyle.overflowY + computedStyle.overflowX)) parents.push(el);
		el = el.parentNode;
	}
	return parents;
}
//#endregion
//#region node_modules/fullcalendar/chunks/44e811e2.js
var Slicer = class {
	constructor() {
		this.sliceBusinessHours = memoize(this._sliceBusinessHours);
		this.sliceDateSelection = memoize(this._sliceDateSpan);
		this.sliceEventStore = memoize(this._sliceEventStore);
		this.sliceEventDrag = memoize(this._sliceInteraction);
		this.sliceEventResize = memoize(this._sliceInteraction);
		this.forceDayIfListItem = false;
	}
	intersectDateSpan(dateSpan, activeRange, ...extraArgs) {
		const activeDateSpanRange = intersectRanges(dateSpan.range, activeRange);
		if (activeDateSpanRange) {
			const slicedDateSpan = {
				...dateSpan,
				range: activeDateSpanRange
			};
			if (activeDateSpanRange.start.valueOf() !== dateSpan.range.start.valueOf()) delete slicedDateSpan.instantStartMs;
			if (activeDateSpanRange.end.valueOf() !== dateSpan.range.end.valueOf()) delete slicedDateSpan.instantEndMs;
			return slicedDateSpan;
		}
		return null;
	}
	sliceDateSpan(dateSpan, ...extraArgs) {
		return this.sliceRange(dateSpan.range, ...extraArgs);
	}
	sliceProps(props, dateProfile, nextDayThreshold, context, ...extraArgs) {
		let { eventUiBases } = props;
		let eventSegs = this.sliceEventStore(props.eventStore, eventUiBases, dateProfile, nextDayThreshold, ...extraArgs);
		return {
			dateSelectionSegs: this.sliceDateSelection(props.dateSelection, dateProfile, nextDayThreshold, eventUiBases, context, ...extraArgs),
			businessHourSegs: this.sliceBusinessHours(props.businessHours, dateProfile, nextDayThreshold, context, ...extraArgs),
			fgEventSegs: eventSegs.fg,
			bgEventSegs: eventSegs.bg,
			eventDrag: this.sliceEventDrag(props.eventDrag, eventUiBases, dateProfile, nextDayThreshold, ...extraArgs),
			eventResize: this.sliceEventResize(props.eventResize, eventUiBases, dateProfile, nextDayThreshold, ...extraArgs),
			eventSelection: props.eventSelection
		};
	}
	sliceNowDate(date, dateProfile, nextDayThreshold, context, ...extraArgs) {
		return this._sliceDateSpan({
			range: {
				start: date,
				end: addMs(date, 1)
			},
			allDay: false
		}, dateProfile, nextDayThreshold, {}, context, ...extraArgs);
	}
	_sliceBusinessHours(businessHours, dateProfile, nextDayThreshold, context, ...extraArgs) {
		if (!businessHours) return [];
		return this._sliceEventStore(expandRecurring(businessHours, computeActiveRange(dateProfile, Boolean(nextDayThreshold)), context), {}, dateProfile, nextDayThreshold, ...extraArgs).bg;
	}
	_sliceEventStore(eventStore, eventUiBases, dateProfile, nextDayThreshold, ...extraArgs) {
		if (eventStore) {
			let rangeRes = sliceEventStore(eventStore, eventUiBases, computeActiveRange(dateProfile, Boolean(nextDayThreshold)), nextDayThreshold);
			return {
				bg: this.sliceEventRanges(rangeRes.bg, extraArgs),
				fg: this.sliceEventRanges(rangeRes.fg, extraArgs)
			};
		}
		return {
			bg: [],
			fg: []
		};
	}
	_sliceInteraction(interaction, eventUiBases, dateProfile, nextDayThreshold, ...extraArgs) {
		if (!interaction) return null;
		let rangeRes = sliceEventStore(interaction.mutatedEvents, eventUiBases, computeActiveRange(dateProfile, Boolean(nextDayThreshold)), nextDayThreshold);
		return {
			segs: this.sliceEventRanges(rangeRes.fg, extraArgs),
			affectedInstances: interaction.affectedEvents.instances,
			isEvent: interaction.isEvent
		};
	}
	_sliceDateSpan(dateSpan, dateProfile, nextDayThreshold, eventUiBases, context, ...extraArgs) {
		if (!dateSpan) return [];
		let activeRange = computeActiveRange(dateProfile, Boolean(nextDayThreshold));
		let slicedDateSpan = this.intersectDateSpan(dateSpan, activeRange, ...extraArgs);
		if (slicedDateSpan) {
			dateSpan = slicedDateSpan;
			let eventRange = fabricateEventRange(dateSpan, eventUiBases, context);
			let segs = this.sliceDateSpan(dateSpan, ...extraArgs);
			for (let seg of segs) seg.eventRange = eventRange;
			return segs;
		}
		return [];
	}
	sliceEventRanges(eventRanges, extraArgs) {
		let segs = [];
		for (let eventRange of eventRanges) segs.push(...this.sliceEventRange(eventRange, extraArgs));
		return segs;
	}
	sliceEventRange(eventRange, extraArgs) {
		let dateRange = eventRange.range;
		if (this.forceDayIfListItem && eventRange.ui.display === "list-item") dateRange = {
			start: dateRange.start,
			end: addDays(dateRange.start, 1)
		};
		let segs = this.sliceRange(dateRange, ...extraArgs);
		for (let seg of segs) {
			seg.eventRange = eventRange;
			seg.isStart = eventRange.isStart && seg.isStart;
			seg.isEnd = eventRange.isEnd && seg.isEnd;
		}
		return segs;
	}
};
function computeActiveRange(dateProfile, isComponentAllDay) {
	let range = dateProfile.activeRange;
	if (isComponentAllDay) return range;
	return {
		start: addMs(range.start, dateProfile.slotMinTime.milliseconds),
		end: addMs(range.end, dateProfile.slotMaxTime.milliseconds - 864e5)
	};
}
var DayTableModel = class {
	constructor(daySeries, breakOnWeeks, dateEnv, majorUnit = "", activeRange) {
		this.daySeries = daySeries;
		this.dateEnv = dateEnv;
		this.majorUnit = majorUnit;
		this.activeRange = activeRange;
		let { dates } = daySeries;
		let daysPerRow;
		let firstDay;
		let rowCount;
		if (breakOnWeeks) {
			firstDay = dates[0].getUTCDay();
			for (daysPerRow = 1; daysPerRow < dates.length; daysPerRow += 1) if (dates[daysPerRow].getUTCDay() === firstDay) break;
			rowCount = Math.ceil(dates.length / daysPerRow);
		} else {
			rowCount = 1;
			daysPerRow = dates.length;
		}
		this.rowCount = rowCount;
		this.colCount = daysPerRow;
		this.cellRows = this.buildCells();
		this.headerDates = this.buildHeaderDates();
	}
	buildCells() {
		let rows = [];
		for (let row = 0; row < this.rowCount; row += 1) {
			let cells = [];
			for (let col = 0; col < this.colCount; col += 1) cells.push(this.buildCell(row, col));
			rows.push(cells);
		}
		return rows;
	}
	buildCell(row, col) {
		let date = this.daySeries.dates[row * this.colCount + col];
		return {
			key: date.toISOString(),
			date,
			isMajor: this.cellIsMajor(date),
			isDisabled: this.activeRange === null || this.activeRange !== void 0 && !rangeContainsMarker(this.activeRange, date)
		};
	}
	cellIsMajor(dateMarker) {
		return this.majorUnit ? isMajorUnit(dateMarker, this.majorUnit, this.dateEnv) : false;
	}
	buildHeaderDates() {
		let dates = [];
		for (let col = 0; col < this.colCount; col += 1) dates.push(this.cellRows[0][col].date);
		return dates;
	}
};
function buildDayGridRanges(seriesRange, daysPerRow) {
	let ranges = [];
	if (seriesRange) {
		const { start, end } = seriesRange;
		let index = start;
		while (index < end) {
			let row = Math.floor(index / daysPerRow);
			let nextIndex = Math.min((row + 1) * daysPerRow, end);
			ranges.push({
				row,
				start: index % daysPerRow,
				end: (nextIndex - 1) % daysPerRow + 1,
				isStart: seriesRange.isStart && index === start,
				isEnd: seriesRange.isEnd && nextIndex === end
			});
			index = nextIndex;
		}
	}
	return ranges;
}
var DayTableSlicer = class extends Slicer {
	constructor() {
		super(...arguments);
		this.forceDayIfListItem = true;
	}
	sliceRange(dateRange, dayTableModel) {
		return buildDayGridRanges(dayTableModel.daySeries.sliceRange(dateRange), dayTableModel.colCount);
	}
};
var DaySeriesSlicer = class extends Slicer {
	constructor() {
		super(...arguments);
		this.forceDayIfListItem = true;
	}
	sliceRange(dateRange, daySeries) {
		return buildDayGridRanges(daySeries.sliceRange(dateRange), daySeries.cnt);
	}
};
var firstSunday = /* @__PURE__ */ new Date(2592e5);
function buildDateRowConfigs(dates, datesRepDistinctDays, dateProfile, todayRange, dayHeaderFormat, context) {
	const rowConfig = buildDateRowConfig(dates, datesRepDistinctDays, dateProfile, todayRange, dayHeaderFormat, context);
	const majorUnit = computeMajorUnit(dateProfile, context.dateEnv);
	if (datesRepDistinctDays && majorUnit !== "day") {
		for (const dataConfig of rowConfig.dataConfigs) if (isMajorUnit(dataConfig.dateMarker, majorUnit, context.dateEnv)) dataConfig.renderProps.isMajor = true;
	}
	return [rowConfig];
}
function buildDateRowConfig(dateMarkers, datesRepDistinctDays, dateProfile, todayRange, dayHeaderFormat, context, colSpan, isMajorMod, totalDateCnt) {
	return {
		isDateRow: true,
		renderConfig: buildDateRenderConfig(dayHeaderFormat, datesRepDistinctDays, context),
		dataConfigs: buildDateDataConfigs(dateMarkers, datesRepDistinctDays, dateProfile, todayRange, dayHeaderFormat, context, colSpan, void 0, void 0, void 0, void 0, isMajorMod, totalDateCnt)
	};
}
function buildDateRenderConfig(dayHeaderFormat, datesRepDistinctDays, context) {
	const { options } = context;
	return {
		generatorName: "dayHeaderContent",
		customGenerator: options.dayHeaderContent,
		classNameGenerator: options.dayHeaderClass,
		innerClassNameGenerator: options.dayHeaderInnerClass,
		didMount: options.dayHeaderDidMount,
		willUnmount: options.dayHeaderWillUnmount,
		align: options.dayHeaderAlign,
		sticky: options._dayHeaderSticky,
		dayHeaderFormat,
		datesRepDistinctDays
	};
}
var dowDates = [];
for (let dow = 0; dow < 7; dow++) dowDates.push(addDays(/* @__PURE__ */ new Date(2592e5), dow));
function buildDateDataConfigs(dateMarkers, datesRepDistinctDays, dateProfile, todayRange, dayHeaderFormat, context, colSpan = 1, keyPrefix = "", extraRenderProps = {}, extraAttrs = {}, className = "", isMajorMod, totalDateCnt = dateMarkers.length) {
	const { dateEnv, viewApi, options } = context;
	return datesRepDistinctDays ? dateMarkers.map((dateMarker, i) => {
		const dateMeta = getDateMeta(dateMarker, dateEnv, dateProfile, todayRange);
		const isMajor = isMajorMod != null && !(i % isMajorMod);
		const hasNavLink = options.navLinks && !dateMeta.isDisabled && totalDateCnt > 1;
		const renderProps = {
			...dateMeta,
			...extraRenderProps,
			isMajor,
			isSticky: false,
			inPopover: false,
			hasNavLink,
			view: viewApi
		};
		const fullDateStr = buildDateStr(context, dateMarker);
		return {
			key: keyPrefix + dateMarker.toUTCString(),
			dateMarker,
			renderProps,
			attrs: {
				"aria-label": fullDateStr,
				...dateMeta.isToday ? { "aria-current": "date" } : {},
				"data-date": formatDayString(dateMarker),
				...extraAttrs
			},
			innerAttrs: hasNavLink ? buildNavLinkAttrs(context, dateMarker, void 0, fullDateStr) : { "aria-hidden": true },
			colSpan,
			hasNavLink,
			className
		};
	}) : dateMarkers.map((dateMarker, i) => {
		const dow = dateMarker.getUTCDay();
		const normDate = addDays(firstSunday, dow);
		const dateMeta = {
			date: dateEnv.toDate(dateMarker),
			dow,
			isDisabled: false,
			isFuture: false,
			isPast: false,
			isToday: false,
			isOther: false
		};
		const isMajor = isMajorMod != null && !(i % isMajorMod);
		const renderProps = {
			...dateMeta,
			date: dowDates[dow],
			isMajor,
			isSticky: false,
			inPopover: false,
			hasNavLink: false,
			view: viewApi,
			...extraRenderProps
		};
		const fullWeekDayStr = joinDateTimeFormatParts(dateEnv.formatToParts(normDate, WEEKDAY_ONLY_FORMAT));
		return {
			key: keyPrefix + String(dow),
			dateMarker,
			renderProps,
			attrs: {
				"aria-label": fullWeekDayStr,
				...extraAttrs
			},
			innerAttrs: { "aria-hidden": true },
			colSpan,
			className
		};
	});
}
var RefMap = class {
	constructor(masterCallback, ignoreDeletes = false) {
		this.masterCallback = masterCallback;
		this.ignoreDeletes = ignoreDeletes;
		this.rev = "";
		this.current = /* @__PURE__ */ new Map();
		this.callbacks = /* @__PURE__ */ new Map();
		this.handleValue = (val, key) => {
			let { current, callbacks } = this;
			let priorExists = current.has(key);
			let priorVal = priorExists ? current.get(key) : null;
			let anyChange = false;
			if (val === null) {
				if (priorExists && !this.ignoreDeletes) {
					current.delete(key);
					callbacks.delete(key);
					anyChange = true;
				}
			} else {
				anyChange = priorVal !== val;
				current.set(key, val);
			}
			if (anyChange) {
				this.rev = guid();
				if (this.masterCallback) this.masterCallback(val, key, priorVal);
			}
		};
	}
	createRef(key) {
		let refCallback = this.callbacks.get(key);
		if (!refCallback) {
			refCallback = (val) => {
				this.handleValue(val, key);
			};
			this.callbacks.set(key, refCallback);
		}
		return refCallback;
	}
};
var Ruler = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.elRef = M();
	}
	render() {
		return u("div", { ref: this.elRef });
	}
	componentDidMount() {
		this._isUnmounting = false;
		const { props } = this;
		const el = this.elRef.current;
		this.disconnectWidth = watchWidth(el, (width) => {
			if (this._isUnmounting) return;
			setRef(props.widthRef, width);
		});
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectWidth();
		const { props } = this;
		if (props.widthRef) setRef(props.widthRef, null);
	}
};
/** Identifies a DayGrid seg by event instance and start, remaining stable if its end changes. */
function getDayGridSegKey(seg) {
	return `${seg.eventRange.instance.instanceId}:${seg.start}`;
}
function splitSegsByRow(segs, rowCount) {
	const byRow = [];
	for (let row = 0; row < rowCount; row++) byRow[row] = [];
	for (const seg of segs) byRow[seg.row].push(seg);
	return byRow;
}
function splitInteractionByRow(ui, rowCount) {
	const byRow = [];
	if (!ui) for (let row = 0; row < rowCount; row++) byRow[row] = null;
	else {
		for (let row = 0; row < rowCount; row++) byRow[row] = {
			affectedInstances: ui.affectedInstances,
			isEvent: ui.isEvent,
			segs: []
		};
		for (const seg of ui.segs) byRow[seg.row].segs.push(seg);
	}
	return byRow;
}
var BgEvent = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.buildPublicEvent = memoize((context, eventDef, eventInstance) => new EventImpl(context, eventDef, eventInstance));
		this.handleEl = (el) => {
			this.el = el;
			if (el) setElEventRange(el, this.props.eventRange);
		};
	}
	render() {
		const { props, context } = this;
		const { eventRange } = props;
		const { options } = context;
		const eventUi = eventRange.ui;
		const eventApi = this.buildPublicEvent(context, eventRange.def, eventRange.instance);
		const subcontentRenderProps = {
			event: eventApi,
			isNarrow: props.isNarrow || false,
			isShort: props.isShort || false
		};
		const renderProps = {
			event: eventApi,
			view: context.viewApi,
			timeText: "",
			color: eventUi.color || options.backgroundEventColor,
			contrastColor: eventUi.contrastColor,
			isDraggable: false,
			isStartResizable: false,
			isEndResizable: false,
			isMirror: false,
			isStart: props.isStart,
			isEnd: props.isEnd,
			isFirst: false,
			isLast: false,
			isPast: props.isPast,
			isFuture: props.isFuture,
			isToday: props.isToday,
			isSelected: false,
			isDragging: false,
			isResizing: false,
			isInteractive: false,
			level: 0,
			isNarrow: props.isNarrow || false,
			isShort: props.isShort || false,
			timeClass: "",
			titleClass: generateClassName(options.backgroundEventTitleClass, subcontentRenderProps),
			options: { eventOverlap: Boolean(options.eventOverlap) }
		};
		const outerClassName = joinClassNames(eventUi.className, classNames.fill, classNames.internalEvent, classNames.internalBgEvent, props.isVertical ? classNames.flexCol : classNames.flexRow);
		const innerClassName = joinClassNames(generateClassName(options.backgroundEventInnerClass, renderProps), classNames.liquid);
		return u(ContentContainer, {
			tag: "div",
			className: outerClassName,
			style: {
				"--fc-event-color": renderProps.color,
				"--fc-event-contrast-color": renderProps.contrastColor
			},
			defaultGenerator: renderInnerContent$1,
			elRef: this.handleEl,
			renderProps,
			generatorName: "backgroundEventContent",
			customGenerator: options.backgroundEventContent,
			classNameGenerator: options.backgroundEventClass,
			didMount: options.backgroundEventDidMount,
			willUnmount: options.backgroundEventWillUnmount,
			children: (InnerContent) => u(InnerContent, {
				tag: "div",
				className: innerClassName
			})
		});
	}
	componentDidUpdate(prevProps) {
		if (this.el && this.props.eventRange !== prevProps.eventRange) setElEventRange(this.el, this.props.eventRange);
	}
};
function renderInnerContent$1(props) {
	let { title } = props.event;
	return title && u("div", {
		className: props.titleClass,
		children: props.event.title
	});
}
function renderFill(fillType, options) {
	return u("div", { className: joinClassNames(fillType === "non-business" ? options.nonBusinessHoursClass : fillType === "highlight" ? options.highlightClass : void 0, classNames.fill) });
}
var COL_BORDER_WIDTH = 1;
var ROW_BORDER_WIDTH = 1;
var SPACE_FROM_VIEWPORT = 10;
var MorePopover = class extends DateComponent {
	constructor() {
		super(...arguments);
		this.getDateMeta = memoize(getDateMeta);
		this.closeRef = M();
		this.focusStartRef = M();
		this.focusEndRef = M();
		this.handleRootEl = (rootEl) => {
			this.rootEl = rootEl;
			if (rootEl) this.context.registerInteractiveComponent(this, {
				el: rootEl,
				useEventCenter: false
			});
			else this.context.unregisterInteractiveComponent(this);
		};
		this.handleDocumentMouseDown = (ev) => {
			const target = getEventTargetViaRoot(ev);
			if (!this.rootEl.contains(target)) this.handleClose();
		};
		this.handleDocumentKeyDown = (ev) => {
			if (ev.key === "Escape") this.handleClose();
		};
		this.handleClose = () => {
			let { onClose } = this.props;
			if (onClose) onClose();
		};
	}
	render() {
		let { props, context } = this;
		let { options, dateEnv, viewApi } = context;
		let { startDate, todayRange, dateProfile } = props;
		let dateMeta = this.getDateMeta(startDate, dateEnv, dateProfile, todayRange);
		let textParts = dateEnv.formatToParts(startDate, options.popoverFormat);
		let text = joinDateTimeFormatParts(textParts);
		const dayHeaderRenderProps = {
			...dateMeta,
			isMajor: false,
			isNarrow: false,
			isSticky: false,
			inPopover: true,
			level: 0,
			hasNavLink: false,
			text,
			textParts,
			get weekdayText() {
				return findWeekdayText(textParts);
			},
			get dayNumberText() {
				return findDayNumberText(textParts);
			},
			view: viewApi
		};
		const dayCellRenderProps = {
			...dateMeta,
			isMajor: false,
			isNarrow: false,
			inPopover: true,
			hasNavLink: false,
			get weekdayText() {
				return findWeekdayText(textParts);
			},
			get dayNumberText() {
				return findDayNumberText(textParts);
			},
			get monthText() {
				return findMonthText(textParts);
			},
			view: viewApi,
			text: "",
			textParts: [],
			options: { businessHours: Boolean(options.businessHours) }
		};
		const fullDateStr = formatDayString(startDate);
		const { dayHeaderAlign } = options;
		const align = typeof dayHeaderAlign === "function" ? dayHeaderAlign({
			level: 0,
			inPopover: true,
			isNarrow: false
		}) : dayHeaderAlign;
		const isRtl = computeElIsRtl(props.alignEl);
		return $(u("div", {
			"data-date": fullDateStr,
			id: props.id,
			role: "dialog",
			"aria-labelledby": props.titleId,
			className: joinClassNames(options.popoverClass, classNames.flexCol, classNames.popoverZ, classNames.abs, classNames.borderBoxRoot, classNames.internalPopover),
			style: {
				top: 0,
				left: 0
			},
			dir: isRtl ? "rtl" : void 0,
			"data-color-scheme": options.colorScheme || void 0,
			ref: this.handleRootEl,
			children: [
				u("div", {
					tabIndex: 0,
					style: { outline: "none" },
					ref: this.focusStartRef
				}),
				u("div", {
					className: joinClassNames(generateClassName(options.dayHeaderClass, dayHeaderRenderProps), classNames.flexCol, classNames.borderlessX, classNames.borderlessTop, align === "center" ? classNames.alignCenter : align === "end" ? classNames.alignEnd : classNames.alignStart),
					children: [u("div", { children: u(ContentContainer, {
						tag: "div",
						attrs: { id: props.titleId },
						generatorName: "dayHeaderContent",
						renderProps: dayHeaderRenderProps,
						customGenerator: options.dayHeaderContent,
						defaultGenerator: renderText,
						classNameGenerator: options.dayHeaderInnerClass,
						didMount: options.dayHeaderDidMount,
						willUnmount: options.dayHeaderWillUnmount
					}) }), u(ContentContainer, {
						tag: "button",
						attrs: {
							"aria-label": options.closeHint,
							...createAriaClickAttrs(this.handleClose)
						},
						elRef: this.closeRef,
						className: joinClassNames(options.popoverCloseClass, classNames.flexRow, classNames.cursorPointer),
						renderProps: {},
						customGenerator: options.popoverCloseContent,
						generatorName: "popoverCloseContent"
					})]
				}),
				u("div", {
					className: joinClassNames(generateClassName(options.dayCellClass, dayCellRenderProps), classNames.flexCol, classNames.borderless),
					children: u("div", {
						className: generateClassName(options.dayCellInnerClass, dayCellRenderProps),
						children: props.children
					})
				}),
				u("div", {
					tabIndex: 0,
					style: { outline: "none" },
					ref: this.focusEndRef
				})
			]
		}), getAppendableRoot(props.alignEl));
	}
	queryHit(isRtl, positionLeft, positionTop, elWidth, elHeight) {
		let { rootEl, props } = this;
		if (positionLeft >= 0 && positionLeft < elWidth && positionTop >= 0 && positionTop < elHeight) return {
			dateProfile: props.dateProfile,
			dateSpan: {
				allDay: !props.forceTimed,
				range: {
					start: props.startDate,
					end: props.endDate
				},
				...props.dateSpanProps
			},
			getDayEl: () => rootEl,
			rect: {
				left: 0,
				top: 0,
				right: elWidth,
				bottom: elHeight
			},
			layer: 1
		};
		return null;
	}
	componentDidMount() {
		document.addEventListener("mousedown", this.handleDocumentMouseDown);
		document.addEventListener("keydown", this.handleDocumentKeyDown);
		this.focusStartRef.current.addEventListener("focus", this.handleClose);
		this.focusEndRef.current.addEventListener("focus", this.handleClose);
		this.closeRef.current.focus({ preventScroll: true });
		this.updateSize();
	}
	componentWillUnmount() {
		document.removeEventListener("mousedown", this.handleDocumentMouseDown);
		document.removeEventListener("keydown", this.handleDocumentKeyDown);
		this.focusStartRef.current.removeEventListener("focus", this.handleClose);
		this.focusEndRef.current.removeEventListener("focus", this.handleClose);
	}
	updateSize() {
		let { alignEl, alignParentTop } = this.props;
		let { rootEl: popoverEl } = this;
		const isRtl = computeElIsRtl(alignEl);
		const alignmentRect = computeClippedClientRect(alignEl);
		if (alignmentRect) {
			let popoverDims = popoverEl.getBoundingClientRect();
			let popoverVPTop = alignParentTop ? alignEl.closest(alignParentTop).getBoundingClientRect().top - ROW_BORDER_WIDTH : alignmentRect.top;
			let popoverVPLeft = isRtl ? alignmentRect.right - popoverDims.width : alignmentRect.left;
			popoverVPTop = Math.max(popoverVPTop, SPACE_FROM_VIEWPORT);
			popoverVPLeft = Math.min(popoverVPLeft, document.documentElement.clientWidth - SPACE_FROM_VIEWPORT - popoverDims.width);
			popoverVPLeft = Math.max(popoverVPLeft, SPACE_FROM_VIEWPORT);
			const { offsetParent } = popoverEl;
			let top;
			let left;
			if (!offsetParent || offsetParent === document.body) {
				top = popoverVPTop + window.scrollY;
				left = popoverVPLeft + window.scrollX;
			} else {
				const offsetParentRect = offsetParent.getBoundingClientRect();
				top = popoverVPTop - offsetParentRect.top + offsetParent.scrollTop;
				left = popoverVPLeft - offsetParentRect.left + offsetParent.scrollLeft;
			}
			applyStyle(popoverEl, {
				top,
				left
			});
		}
	}
};
function renderText(renderProps) {
	return renderProps.text;
}
function computeEarliestStart(segs) {
	return segs.reduce(pickEarliestStart).eventRange.range.start;
}
function computeLatestEnd(segs) {
	return segs.reduce(pickLatestEnd).eventRange.range.end;
}
function pickEarliestStart(r0, r1) {
	return r0.eventRange.range.start < r1.eventRange.range.start ? r0 : r1;
}
function pickLatestEnd(r0, r1) {
	return r0.eventRange.range.end > r1.eventRange.range.end ? r0 : r1;
}
var MoreLinkTrigger = class extends BaseComponent {
	render() {
		const { props, context } = this;
		const { options } = context;
		const renderProps = buildMoreLinkRenderProps(props.num, props.isNarrow, props.isMicro, props.display, context);
		return u(ContentContainer, {
			tag: "div",
			elRef: props.elRef,
			className: joinClassNames(generateClassName(props.display === "row" ? options.rowMoreLinkClass : options.columnMoreLinkClass, renderProps), props.className, props.display === "row" ? classNames.flexRow : classNames.flexCol, classNames.internalMoreLink, classNames.cursorPointer),
			style: props.style,
			attrs: props.attrs,
			renderProps,
			generatorName: "moreLinkContent",
			customGenerator: options.moreLinkContent,
			defaultGenerator: renderMoreLinkText,
			classNameGenerator: options.moreLinkClass,
			didMount: props.didMount,
			willUnmount: props.willUnmount,
			children: (InnerContent) => u(InnerContent, {
				tag: "div",
				className: joinClassNames(generateClassName(options.moreLinkInnerClass, renderProps), generateClassName(props.display === "row" ? options.rowMoreLinkInnerClass : options.columnMoreLinkInnerClass, renderProps), props.display === "row" ? classNames.stickyS : classNames.stickyT)
			})
		});
	}
};
var MoreLinkContainer = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.state = { isPopoverOpen: false };
		this.handleLinkEl = (linkEl) => {
			this.linkEl = linkEl;
			if (this.props.elRef) setRef(this.props.elRef, linkEl);
		};
		this.handleClick = (ev) => {
			let { props, context } = this;
			let { dateEnv, options } = context;
			let { moreLinkClick } = options;
			let date = computeRange(props).start;
			function buildPublicSeg(seg) {
				let { def, instance, range } = seg.eventRange;
				const start = buildRangeEdgeOutput(range.start, range.instantStartMs, dateEnv);
				const end = buildRangeEdgeOutput(range.end, range.instantEndMs, dateEnv);
				return {
					event: new EventImpl(context, def, instance),
					start: start.date,
					end: end.date,
					isStart: seg.isStart,
					isEnd: seg.isEnd
				};
			}
			if (typeof moreLinkClick === "function") moreLinkClick = moreLinkClick({
				date: dateEnv.toDate(date),
				allDay: Boolean(props.allDayDate),
				allSegs: props.segs.map(buildPublicSeg),
				hiddenSegs: props.hiddenSegs.map(buildPublicSeg),
				jsEvent: ev,
				view: context.viewApi
			});
			if (!moreLinkClick || moreLinkClick === "popover") this.setState({ isPopoverOpen: true });
			else if (typeof moreLinkClick === "string") context.calendarApi.zoomTo(date, moreLinkClick);
		};
		this.handlePopoverClose = () => {
			if (this.linkEl) this.linkEl.focus();
			this.setState({ isPopoverOpen: false });
		};
	}
	render() {
		const { props, state, context } = this;
		const { options, baseId } = context;
		const moreCnt = props.hiddenSegs.length;
		const range = computeRange(props);
		const popoverId = baseId + "popover-" + range.start.toISOString();
		const renderProps = buildMoreLinkRenderProps(moreCnt, props.isNarrow, props.isMicro, props.display, context);
		const hint = formatWithOrdinals(options.moreLinkHint, [moreCnt], renderProps.longText);
		return u(S, { children: [Boolean(moreCnt) && u(MoreLinkTrigger, {
			num: moreCnt,
			display: props.display,
			isNarrow: props.isNarrow,
			isMicro: props.isMicro,
			elRef: this.handleLinkEl,
			className: props.className,
			style: props.style,
			attrs: {
				...props.attrs,
				...createAriaClickAttrs(this.handleClick),
				title: hint,
				"role": "button",
				"aria-haspopup": "dialog",
				"aria-expanded": state.isPopoverOpen,
				"aria-controls": state.isPopoverOpen ? popoverId : void 0
			},
			didMount: options.moreLinkDidMount,
			willUnmount: options.moreLinkWillUnmount
		}), state.isPopoverOpen && u(MorePopover, {
			id: popoverId,
			titleId: popoverId + "-title",
			startDate: range.start,
			endDate: range.end,
			dateProfile: props.dateProfile,
			todayRange: props.todayRange,
			dateSpanProps: props.dateSpanProps,
			alignEl: props.alignElRef ? props.alignElRef.current : this.linkEl,
			alignParentTop: props.alignParentTop,
			forceTimed: props.forceTimed,
			onClose: this.handlePopoverClose,
			children: props.popoverContent()
		})] });
	}
};
function renderMoreLinkText(props) {
	return props.text;
}
function buildMoreLinkRenderProps(num, isNarrow, isMicro, display, context) {
	const { viewApi, options, calendarApi } = context;
	const numericText = `+${num}`;
	const longText = typeof options.moreLinkText === "function" ? options.moreLinkText.call(calendarApi, num) : `${numericText} ${options.moreLinkText}`;
	return {
		num,
		numericText,
		longText,
		text: isMicro || display === "column" ? numericText : longText,
		isNarrow,
		view: viewApi
	};
}
function computeRange(props) {
	if (props.allDayDate) return {
		start: props.allDayDate,
		end: addDays(props.allDayDate, 1)
	};
	return {
		start: computeEarliestStart(props.hiddenSegs),
		end: computeLatestEnd(props.hiddenSegs)
	};
}
var DEFAULT_TABLE_EVENT_TIME_FORMAT = createFormatter({
	hour: "numeric",
	minute: "2-digit",
	omitZeroMinute: true,
	meridiem: "narrow"
});
function hasListItemDisplay(range, eventRange) {
	let { display } = eventRange.ui;
	return display === "list-item" || display === "auto" && !eventRange.def.allDay && range.end - range.start === 1 && range.isStart && range.isEnd;
}
var DAY_GRID_NON_BUSINESS_Z_CLASS = classNames.z1;
var DAY_GRID_BG_EVENT_Z_CLASS = classNames.z2;
var DAY_GRID_HIGHLIGHT_Z_CLASS = classNames.z3;
var DAY_GRID_CELL_CONTENT_Z_CLASS = classNames.z4;
var DAY_GRID_EVENT_Z_CLASS = classNames.z5;
var DAY_GRID_INTERACTION_Z_CLASS = classNames.z1000;
var DayGridMoreLink = class extends BaseComponent {
	render() {
		let { props } = this;
		return u(MoreLinkContainer, {
			display: "row",
			className: joinClassNames(props.className, DAY_GRID_CELL_CONTENT_Z_CLASS),
			isNarrow: props.isNarrow,
			isMicro: props.isMicro,
			dateProfile: props.dateProfile,
			todayRange: props.todayRange,
			allDayDate: props.allDayDate,
			segs: props.segs,
			hiddenSegs: props.hiddenSegs,
			alignElRef: props.alignElRef,
			alignParentTop: props.alignParentTop,
			dateSpanProps: props.dateSpanProps,
			popoverContent: () => u(S, { children: props.segs.map((seg) => {
				let { eventRange } = seg;
				let { instanceId } = eventRange.instance;
				let isDragging = Boolean(props.eventDrag && props.eventDrag.affectedInstances[instanceId]);
				let isResizing = Boolean(props.eventResize && props.eventResize.affectedInstances[instanceId]);
				return u("div", {
					style: { visibility: isDragging || isResizing ? "hidden" : void 0 },
					children: u(StandardEvent, {
						display: hasListItemDisplay(seg, eventRange) ? "list-item" : "row",
						eventRange,
						isStart: seg.isStart,
						isEnd: seg.isEnd,
						isDragging,
						isResizing,
						isMirror: false,
						isSelected: instanceId === props.eventSelection,
						defaultTimeFormat: DEFAULT_TABLE_EVENT_TIME_FORMAT,
						defaultDisplayEventEnd: false,
						...getEventRangeMeta(eventRange, props.todayRange)
					})
				}, instanceId);
			}) })
		});
	}
};
var DayGridCell = class extends DateComponent {
	constructor() {
		super(...arguments);
		this.getDateMeta = memoize(getDayGridCellDateMeta);
		this.refineRenderProps = memoizeObjArg(refineRenderProps);
		this.rootElRef = M();
		this.handleBodyEl = (bodyEl) => {
			if (this.disconnectBodyHeight) {
				this.disconnectBodyHeight();
				this.disconnectBodyHeight = void 0;
				this.headerHeight = void 0;
				setRef(this.props.headerHeightRef, null);
				setRef(this.props.mainHeightRef, null);
			}
			if (bodyEl && (this.props.headerHeightRef || this.props.mainHeightRef)) this.disconnectBodyHeight = watchSize(bodyEl, (_bodyWidth, bodyHeight) => {
				if (this._isUnmounting) return;
				const { props } = this;
				const rootEl = this.rootElRef.current;
				if (!rootEl) return;
				const mainRect = bodyEl.getBoundingClientRect();
				const rootRect = rootEl.getBoundingClientRect();
				const headerHeight = mainRect.top - rootRect.top;
				if (!isDimsEqual(this.headerHeight, headerHeight)) {
					this.headerHeight = headerHeight;
					setRef(props.headerHeightRef, headerHeight);
				}
				setRef(props.mainHeightRef, bodyHeight);
			});
		};
	}
	render() {
		let { props, context } = this;
		let { options, dateEnv } = context;
		const { tableMode } = props;
		const isMonthStart = props.showDayNumber && shouldDisplayMonthStart(props.date, props.dateProfile.currentRange, dateEnv);
		const dateMeta = this.getDateMeta(props.date, dateEnv, props.dateProfile, props.todayRange, props.isDisabled);
		const baseClassName = joinClassNames(classNames.borderlessTop, classNames.borderlessEnd, !props.borderStart && classNames.borderlessStart, !(tableMode && props.borderBottom) && classNames.borderlessBottom, !tableMode && props.width == null && classNames.liquid, !tableMode && classNames.flexCol, classNames.rel, classNames.noMargin, classNames.noPadding);
		const CellTag = tableMode ? "td" : "div";
		const cellStyle = tableMode ? void 0 : { width: props.width };
		const hasNavLink = options.navLinks;
		const renderProps = this.refineRenderProps({
			date: props.date,
			isMajor: props.isMajor,
			isNarrow: props.isNarrow,
			dateMeta,
			hasLabel: props.showDayNumber,
			hasMonthLabel: isMonthStart,
			hasNavLink,
			renderProps: props.renderProps,
			viewApi: context.viewApi,
			dateEnv: context.dateEnv,
			monthStartFormat: options.monthStartFormat,
			dayCellFormat: options.dayCellFormat,
			businessHours: Boolean(options.businessHours)
		});
		if (dateMeta.isDisabled) return u(CellTag, {
			role: "gridcell",
			"aria-disabled": true,
			className: joinClassNames(generateClassName(options.dayCellClass, renderProps), props.className, baseClassName),
			style: cellStyle,
			children: props.fills
		});
		const fullDateStr = buildDateStr(context, props.date);
		return u(ContentContainer, {
			tag: CellTag,
			elRef: this.rootElRef,
			className: joinClassNames(props.className, baseClassName),
			attrs: {
				...props.attrs,
				role: "gridcell",
				"aria-label": fullDateStr,
				...renderProps.isToday ? { "aria-current": "date" } : {},
				"data-date": formatDayString(props.date)
			},
			style: cellStyle,
			renderProps,
			generatorName: "dayCellTopContent",
			customGenerator: options.dayCellTopContent,
			defaultGenerator: renderTopInner,
			classNameGenerator: options.dayCellClass,
			didMount: options.dayCellDidMount,
			willUnmount: options.dayCellWillUnmount,
			children: (InnerContent) => u(S, { children: [
				props.fills,
				u("div", {
					className: joinClassNames(classNames.rel, DAY_GRID_CELL_CONTENT_Z_CLASS, generateClassName(options.dayCellTopClass, renderProps)),
					children: props.showDayNumber && u(InnerContent, {
						tag: "div",
						attrs: hasNavLink ? buildNavLinkAttrs(context, props.date, void 0, fullDateStr) : { "aria-hidden": true },
						className: generateClassName(options.dayCellTopInnerClass, renderProps)
					})
				}),
				u("div", {
					className: joinClassNames(!tableMode && classNames.flexCol, !tableMode && (props.fgLiquidHeight ? classNames.liquid : classNames.grow), tableMode && classNames.printCellContentMinHeight),
					ref: this.handleBodyEl,
					children: [u("div", {
						className: joinClassNames(classNames.rel, generateClassName(options.dayCellInnerClass, renderProps)),
						style: { minHeight: props.fgHeight },
						children: props.fg
					}), u(DayGridMoreLink, {
						className: classNames.rel,
						allDayDate: props.date,
						segs: props.segs,
						hiddenSegs: props.hiddenSegs,
						alignElRef: this.rootElRef,
						alignParentTop: props.showDayNumber ? "[role=row]" : `.${classNames.internalView}`,
						dateSpanProps: props.dateSpanProps,
						dateProfile: props.dateProfile,
						eventSelection: props.eventSelection,
						eventDrag: props.eventDrag,
						eventResize: props.eventResize,
						todayRange: props.todayRange,
						isNarrow: props.isNarrow,
						isMicro: props.isMicro
					})]
				}),
				u("div", { className: joinClassNames(classNames.rel, DAY_GRID_CELL_CONTENT_Z_CLASS, generateClassName(options.dayCellBottomClass, renderProps)) })
			] })
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentWillUnmount() {
		this._isUnmounting = true;
	}
};
function getDayGridCellDateMeta(date, dateEnv, dateProfile, todayRange, isDisabled) {
	return {
		...getDateMeta(date, dateEnv, dateProfile, todayRange),
		isDisabled
	};
}
function renderTopInner(props) {
	return props.text || u(S, { children: "\xA0" });
}
function shouldDisplayMonthStart(date, currentRange, dateEnv) {
	const { start: currentStart, end: currentEnd } = currentRange;
	const currentEndIncl = addMs(currentEnd, -1);
	const currentFirstYear = dateEnv.getYear(currentStart);
	const currentFirstMonth = dateEnv.getMonth(currentStart);
	const currentLastYear = dateEnv.getYear(currentEndIncl);
	const currentLastMonth = dateEnv.getMonth(currentEndIncl);
	return !(currentFirstYear === currentLastYear && currentFirstMonth === currentLastMonth) && Boolean(date.valueOf() === currentStart.valueOf() || dateEnv.getDay(date) === 1 && date.valueOf() < currentEnd.valueOf());
}
function refineRenderProps(raw) {
	let { date, dateEnv, hasLabel, hasMonthLabel, hasNavLink, businessHours } = raw;
	let textParts = [];
	let text = "";
	if (hasLabel) {
		textParts = dateEnv.formatToParts(date, hasMonthLabel ? raw.monthStartFormat : raw.dayCellFormat);
		text = joinDateTimeFormatParts(textParts);
	}
	return {
		...raw.dateMeta,
		...raw.renderProps,
		text,
		textParts,
		isMajor: raw.isMajor,
		isNarrow: raw.isNarrow,
		inPopover: false,
		hasNavLink,
		get weekdayText() {
			return findWeekdayText(textParts);
		},
		get dayNumberText() {
			return findDayNumberText(textParts);
		},
		get monthText() {
			return findMonthText(textParts);
		},
		options: { businessHours },
		view: raw.viewApi
	};
}
var MeasuredHeightHarness = class extends C {
	constructor() {
		super(...arguments);
		this.rootElRef = M();
		this._isUnmounting = false;
	}
	render() {
		const { props } = this;
		return u("div", {
			className: props.className,
			style: props.style,
			ref: this.rootElRef,
			children: props.children
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		const rootEl = this.rootElRef.current;
		this.disconnectHeight = watchHeight(rootEl, (height) => {
			if (this._isUnmounting) return;
			this.height = height;
			setRef(this.props.heightRef, height);
		});
	}
	componentDidUpdate(prevProps) {
		const { heightRef } = this.props;
		if (prevProps.heightRef !== heightRef) {
			setRef(prevProps.heightRef, null);
			if (this.height != null) setRef(heightRef, this.height);
		}
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectHeight?.();
		setRef(this.props.heightRef, null);
	}
};
/**
* Pure lateral-span geometry shared by the seg-placement engine.
*
* A span is a half-open interval `[start, end)` on the lateral axis. The axis
* can be discrete (DayGrid columns) or continuous (Timeline pixels). Exactly
* adjacent spans never intersect.
*/
function doSpansIntersect(a, b) {
	return a.start < b.end && b.start < a.end;
}
/** Returns the strict intersection; exactly adjacent spans do not intersect. */
function intersectSpans(a, b) {
	const start = Math.max(a.start, b.start);
	const end = Math.min(a.end, b.end);
	return start < end ? {
		start,
		end
	} : null;
}
function getSpanLength(span) {
	return span.end - span.start;
}
/**
* Finds every intersection within entries sorted by `start`. When the entries
* are also pairwise non-intersecting, only the single entry before the lower
* bound can straddle the span's start, so the scan begins one entry early.
*/
function findIntersections(entries, span) {
	let index = findLowerBoundByStart(entries, span.start);
	if (index > 0) index--;
	const matches = [];
	for (; index < entries.length; index++) {
		const entry = entries[index];
		if (entry.start >= span.end) break;
		if (doSpansIntersect(entry, span)) matches.push(entry);
	}
	return matches;
}
/**
* Computes the coverage set difference `span - covered`. The covered spans
* must be sorted by `start` and pairwise non-overlapping.
*/
function subtractCoveredSpans(span, covered) {
	const result = [];
	let cursor = span.start;
	for (const item of covered) {
		if (item.end <= cursor) continue;
		if (item.start >= span.end) break;
		if (item.start > cursor) result.push({
			start: cursor,
			end: Math.min(item.start, span.end)
		});
		cursor = Math.max(cursor, item.end);
		if (cursor >= span.end) break;
	}
	if (cursor < span.end) result.push({
		start: cursor,
		end: span.end
	});
	return result;
}
/** Maintains a sorted strict-overlap union; adjacent spans remain separate. */
function addToUnion(spans, addition) {
	const result = [];
	let pending = { ...addition };
	let inserted = false;
	for (const span of spans) if (span.end <= pending.start) result.push(span);
	else if (pending.end <= span.start) {
		if (!inserted) {
			result.push(pending);
			inserted = true;
		}
		result.push(span);
	} else pending = {
		start: Math.min(pending.start, span.start),
		end: Math.max(pending.end, span.end)
	};
	if (!inserted) result.push(pending);
	spans.splice(0, spans.length, ...result);
}
/** Preserves increasing lateral-start order within a sorted entry list. */
function insertLaterally(entries, entry) {
	entries.splice(findLowerBoundByStart(entries, entry.start), 0, entry);
}
function findLowerBoundByStart(entries, start) {
	let low = 0;
	let high = entries.length;
	while (low < high) {
		const middle = low + high >>> 1;
		if (entries[middle].start < start) low = middle + 1;
		else high = middle;
	}
	return low;
}
/**
* Pure event-positioning kernel implementing measured logical repacking.
*
* Source segs own identity and event order. Slices own only lateral geometry,
* while their outer array index is their dimensionless level. Limiting stays
* primarily in logical slice-level space; the pixel path admits speculative
* slices only through occupied logical territory, then monotonically prunes
* the measured result against exact pixel and more-link boundaries.
*/
/** Permissive epsilon for geometric coordinate and budget comparisons. */
var GEOMETRY_TOLERANCE = 1e-6;
/**
* Streams at most `maxLevels` into the initial structure and fires every
* rejected slice back at those levels in event order.
*
* With slicing disabled, a failed slice hides whole. With slicing enabled,
* every level independently offers its maximal free runs and the winning plan
* balances exposed length against fragmentation. Every hidden slice grows a
* coverage accumulator; with a level tax, only newly covered runs reserve the
* bottom event level for a more link, evicting any slice already there.
*/
function buildLevelLimitedLayout(segs, eventOrderStrict, eventSlicing, maxLevels, moreLinkLevelTax, sliceHeights) {
	const { segLevels, excludedSegs } = buildSegLevels(segs, eventOrderStrict, maxLevels);
	const placement = placeExtraSlicesInLevels(convertSegLevelsToWholeSlices(segLevels), convertSegsToWholeSlices(excludedSegs), eventOrderStrict, eventSlicing, moreLinkLevelTax);
	const resolution = resolveLevelCoords(placement.sliceLevels, sliceHeights);
	return {
		renderSlices: flatArray(placement.sliceLevels),
		hiddenSlices: placement.hiddenSlices,
		sliceLevels: placement.sliceLevels,
		sliceCoords: resolution.sliceCoords,
		isSettled: resolution.isSettled
	};
}
/**
* Resolves the bounded whole-slice frontier, then offers excluded slices back
* to its occupied logical territory through slicing. Exact pixel pruning hides
* any measured result that crosses the canvas or an existing more-link band.
* Placement-only slices remain mounted as invisible measurement donors until
* measured, preventing mount-measure cycles.
*
* `levelCapacity` bounds the initial DOM whole-slice candidates; later
* slices begin hidden and unmeasured.
*/
function buildPixelLimitedLayout(segs, eventOrderStrict, eventSlicing, sliceHeights, canvasHeight, levelCapacity, moreLinkHeight) {
	const { segLevels, excludedSegs } = buildSegLevels(segs, eventOrderStrict, levelCapacity);
	const domWholeSliceLevels = convertSegLevelsToWholeSlices(segLevels);
	const domExcludedWholeSlices = convertSegsToWholeSlices(excludedSegs);
	const wholeResolution = resolveLevelCoords(domWholeSliceLevels, sliceHeights, canvasHeight);
	if (canvasHeight == null || moreLinkHeight == null) return {
		renderSlices: flatArray(domWholeSliceLevels),
		hiddenSlices: domExcludedWholeSlices,
		sliceLevels: domWholeSliceLevels,
		sliceCoords: wholeResolution.sliceCoords,
		isSettled: wholeResolution.isSettled
	};
	const excludedWholeSlices = wholeResolution.excludedSlices.concat(domExcludedWholeSlices);
	excludedWholeSlices.sort(compareByEventOrder);
	const placement = placeExtraSlicesInLevels(wholeResolution.placementSliceLevels, excludedWholeSlices, eventOrderStrict, eventSlicing, eventSlicing ? 1 : 0, true, true);
	const sliceResolution = resolveLevelCoords(placement.sliceLevels, sliceHeights);
	const moreLinkEventMax = Math.max(0, canvasHeight - moreLinkHeight);
	const pixelPrunedSlices = prunePixelLimitedSliceLevels(placement.sliceLevels, placement.hiddenSlices, sliceResolution.sliceCoords, sliceHeights, canvasHeight, moreLinkEventMax);
	const renderSlices = flatArray(domWholeSliceLevels).concat(placement.addedSlices);
	const isSettled = wholeResolution.isSettled && sliceResolution.isSettled;
	return {
		renderSlices,
		hiddenSlices: pixelPrunedSlices.concat(placement.hiddenSlices),
		sliceLevels: placement.sliceLevels,
		sliceCoords: sliceResolution.sliceCoords,
		isSettled
	};
}
/** Builds whole-source logical levels without consulting any dimensions. */
function buildSegLevels(segs, eventOrderStrict, maxLevels = Infinity) {
	const segLevels = [];
	const excludedSegs = [];
	for (const seg of segs) {
		const levelIndex = findPackedLevelIndex(segLevels, seg, eventOrderStrict);
		if (levelIndex >= maxLevels) excludedSegs.push(seg);
		else {
			while (segLevels.length <= levelIndex) segLevels.push([]);
			insertLaterally(segLevels[levelIndex], seg);
		}
	}
	return {
		segLevels,
		excludedSegs
	};
}
/**
* The packed level a span belongs to: the shallowest vacant level, or with
* gap reuse forbidden, directly below the deepest intersecting occupant.
* `levels.length` means a new level must open.
*/
function findPackedLevelIndex(levels, span, orderStrict) {
	let levelIndex = 0;
	if (orderStrict) {
		for (let i = 0; i < levels.length; i++) if (findIntersections(levels[i], span).length) levelIndex = i + 1;
	} else while (levelIndex < levels.length && findIntersections(levels[levelIndex], span).length) levelIndex++;
	return levelIndex;
}
function convertSegLevelsToWholeSlices(segLevels) {
	return segLevels.map((level) => convertSegsToWholeSlices(level));
}
function convertSegsToWholeSlices(segs) {
	return segs.map(createWholeSlice);
}
/**
* Resolves fixed logical levels without changing the input or its slices.
* An unmeasured slice leaves the resolution unsettled; a measured bounded
* rejection is final. Neither blocks later traversal entries, so excluding a
* lower slice can let a later slice move upward. The returned placement
* structure re-levels the admitted slices from scratch, compacted around
* pending and excluded slices exactly like the coordinates. Each admitted
* slice files below every admitted slice it intersects — never into a
* shallower gap — so level order mirrors pixel stacking and strict input
* order survives without consulting it.
*/
function resolveLevelCoords(sliceLevels, sliceHeights, maxPixels = Infinity) {
	const placementSliceLevels = [];
	const sliceCoords = /* @__PURE__ */ new Map();
	let isSettled = true;
	const excludedSlices = [];
	for (let levelIndex = 0; levelIndex < sliceLevels.length; levelIndex++) for (const slice of sliceLevels[levelIndex]) {
		const sliceHeight = sliceHeights.get(getSliceKey(slice));
		if (sliceHeight === void 0) {
			isSettled = false;
			continue;
		}
		const { bottom: levelCoord, levelIndex: packedLevelIndex } = computeLateralSpanPlacement(placementSliceLevels, slice, sliceCoords, sliceHeights);
		if (levelCoord + sliceHeight <= maxPixels + GEOMETRY_TOLERANCE) {
			while (placementSliceLevels.length <= packedLevelIndex) placementSliceLevels.push([]);
			insertLaterally(placementSliceLevels[packedLevelIndex], slice);
			sliceCoords.set(getSliceKey(slice), levelCoord);
		} else excludedSlices.push(slice);
	}
	return {
		placementSliceLevels,
		sliceCoords,
		isSettled,
		excludedSlices
	};
}
/** Deepest measured, coordinated bottom and level among slices touching the span. */
function computeLateralSpanPlacement(sliceLevels, span, sliceCoords, sliceHeights) {
	let bottom = 0;
	let levelIndex = 0;
	for (let i = 0; i < sliceLevels.length; i++) {
		const level = sliceLevels[i];
		for (const slice of findIntersections(level, span)) {
			const key = getSliceKey(slice);
			const sliceTop = sliceCoords.get(key);
			const sliceHeight = sliceHeights.get(key);
			if (sliceTop !== void 0 && sliceHeight !== void 0) {
				bottom = Math.max(bottom, sliceTop + sliceHeight);
				levelIndex = i + 1;
			}
		}
	}
	return {
		bottom,
		levelIndex
	};
}
/** Recomputes every coordinated slice against the compacted visible set. */
function recomputeVisibleCoords(sliceLevels, sliceHeights, sliceCoords) {
	const freshCoords = resolveLevelCoords(sliceLevels.map((level) => level.filter((slice) => sliceCoords.has(getSliceKey(slice)))), sliceHeights).sliceCoords;
	for (const [key, coord] of freshCoords) sliceCoords.set(key, coord);
}
/** Returns a measured slice's bottom, or `undefined` while it is pending. */
function getSliceBottom(slice, sliceCoords, sliceHeights) {
	const key = getSliceKey(slice);
	const coord = sliceCoords.get(key);
	const height = sliceHeights.get(key);
	return coord === void 0 || height === void 0 ? void 0 : coord + height;
}
/**
* Monotonically removes measured slices that cross either the canvas boundary
* or an active more-link boundary. Hiding a slice grows the more-link coverage;
* only newly covered spans are fired through the remaining slices, so every
* removal can expose more link-band intruders without reconsidering old spans.
*
* SIDE EFFECT: mutates `sliceCoords`. A removed coordinate is the renderer's
* signal that the still-mounted slice is invisible. After every removal,
* surviving coordinates are resolved again so later queue entries are tested
* against the compacted pixel structure. Slices without a proposed coordinate
* are ignored. Existing hidden slices seed the more-link coverage before the
* first removal is considered.
*
* Returns the slices whose coordinates this pass removed, with no ordering
* guarantee.
*/
function prunePixelLimitedSliceLevels(sliceLevels, initialHiddenSlices, sliceCoords, sliceHeights, maxPixelHeight, moreLinkMaxPixelHeight) {
	const moreLinkGroups = [];
	const pixelPrunedSlices = [];
	const sliceHideQueue = [];
	let sliceHideIndex = 0;
	for (const hiddenSlice of initialHiddenSlices) addHiddenSliceToGroups(moreLinkGroups, hiddenSlice);
	enqueueViolators();
	while (sliceHideIndex < sliceHideQueue.length) {
		const slice = sliceHideQueue[sliceHideIndex++];
		const sliceBottom = getSliceBottom(slice, sliceCoords, sliceHeights);
		if (sliceBottom === void 0 || !violatesPixelBoundary(slice, sliceBottom)) continue;
		sliceCoords.delete(getSliceKey(slice));
		pixelPrunedSlices.push(slice);
		const newMoreLinkSpans = addHiddenSliceToGroups(moreLinkGroups, slice);
		recomputeVisibleCoords(sliceLevels, sliceHeights, sliceCoords);
		for (const newMoreLinkSpan of newMoreLinkSpans) enqueueViolators(newMoreLinkSpan);
	}
	return pixelPrunedSlices;
	/** Whether a measured bottom crosses the canvas or an intersecting link band. */
	function violatesPixelBoundary(slice, sliceBottom) {
		return sliceBottom > maxPixelHeight + GEOMETRY_TOLERANCE || sliceBottom > moreLinkMaxPixelHeight + GEOMETRY_TOLERANCE && findIntersections(moreLinkGroups, slice).length > 0;
	}
	/** Queues every measured violator, or only those touching one span. */
	function enqueueViolators(withinSpan) {
		for (const level of sliceLevels) {
			const candidates = withinSpan ? findIntersections(level, withinSpan) : level;
			for (const slice of candidates) {
				const sliceBottom = getSliceBottom(slice, sliceCoords, sliceHeights);
				if (sliceBottom !== void 0 && violatesPixelBoundary(slice, sliceBottom)) sliceHideQueue.push(slice);
			}
		}
	}
}
/**
* Fires event-ordered extras into a fixed set of logical levels. Repacking may
* reuse gaps in the received levels but never creates additional levels.
* By default, a more-link tax reserves the globally final level. Pixel flows
* can instead reserve the deepest occupied level local to each link span and
* require initial extras to expose some hidden coverage before admission.
*
* PRECONDITION: `extraSlices` is sorted by event order.
*
* SIDE EFFECT: mutates `sliceLevels`; callers transfer ownership of its outer
* array and level arrays to this placement operation.
*/
function placeExtraSlicesInLevels(sliceLevels, extraSlices, eventOrderStrict, eventSlicing, moreLinkLevelTax, requiresSlicing = false, taxDeepestOccupiedLevel = false) {
	const addedSliceSet = /* @__PURE__ */ new Set();
	const hiddenSlices = [];
	const moreLinkGroups = [];
	const moreLinkReservations = [];
	const placementState = {
		levels: sliceLevels,
		moreLinkReservations,
		eventOrderStrict
	};
	const work = [];
	pushFire(extraSlices, requiresSlicing);
	while (work.length) {
		const item = work.pop();
		if (item.type === "fire") fire(item.slice, item.requiresSlicing);
		else fireMoreLink(item.span);
	}
	return {
		sliceLevels,
		hiddenSlices,
		addedSlices: [...addedSliceSet]
	};
	/** Tries an allowed whole insertion before scored same-level slice plans. */
	function fire(slice, requiresSlicing) {
		if (!requiresSlicing) {
			const levelIndex = findInsertionLevel(slice, placementState);
			if (levelIndex !== null) {
				insertLaterally(sliceLevels[levelIndex], slice);
				addedSliceSet.add(slice);
				return;
			}
		}
		if (!eventSlicing) {
			hide(slice);
			return;
		}
		const plan = findBestSlicePlan(slice, placementState, requiresSlicing);
		if (!plan) {
			hide(slice);
			return;
		}
		for (const visibleSlice of plan.slices) {
			insertLaterally(sliceLevels[plan.levelIndex], visibleSlice);
			addedSliceSet.add(visibleSlice);
		}
		for (const hiddenSlice of subtractSpansFromSlice(slice, plan.slices)) hide(hiddenSlice);
	}
	/** Adds hidden membership and fires links only over new accumulator coverage. */
	function hide(slice) {
		hiddenSlices.push(slice);
		const newMoreLinkSpans = addHiddenSliceToGroups(moreLinkGroups, slice);
		if (moreLinkLevelTax) for (let i = newMoreLinkSpans.length - 1; i >= 0; i--) work.push({
			type: "moreLink",
			span: newMoreLinkSpans[i]
		});
	}
	/**
	* Reserves one logical level over fresh accumulator coverage. The ordinary
	* flow taxes the final level; span-local mode taxes the deepest level with
	* an intersecting occupant, ignoring unrelated deeper territory.
	*/
	function fireMoreLink(span) {
		if (!sliceLevels.length) return;
		let taxedLevelIndex = sliceLevels.length - 1;
		let victims = findIntersections(sliceLevels[taxedLevelIndex], span);
		if (taxDeepestOccupiedLevel) while (!victims.length && taxedLevelIndex > 0) {
			taxedLevelIndex--;
			victims = findIntersections(sliceLevels[taxedLevelIndex], span);
		}
		insertLaterally(moreLinkReservations, {
			...span,
			levelIndex: taxedLevelIndex
		});
		const taxedLevel = sliceLevels[taxedLevelIndex];
		for (const victim of victims) {
			taxedLevel.splice(taxedLevel.indexOf(victim), 1);
			addedSliceSet.delete(victim);
			if (eventSlicing) {
				hide(intersectSlice(victim, span));
				pushFire(subtractSpansFromSlice(victim, [span]), false);
			} else hide(victim);
		}
	}
	/** Reversing preserves received order on the LIFO work stack. */
	function pushFire(slices, requiresSlicing) {
		for (let i = slices.length - 1; i >= 0; i--) work.push({
			type: "fire",
			slice: slices[i],
			requiresSlicing
		});
	}
}
/** Returns the shallowest vacant level within the slice's fence, if any. */
function findInsertionLevel(slice, state) {
	const fence = computeLevelFence(slice, state);
	for (let levelIndex = fence.min; levelIndex < fence.maxExclusive; levelIndex++) if (!findIntersections(state.levels[levelIndex], slice).length) return levelIndex;
	return null;
}
/**
* The level range where a slice may legally sit. The bottom reservation
* closes its taxed level and everything deeper over link coverage; strict
* event order additionally fences against intersecting neighbors' order.
*
* The pre-kernel SegHierarchy needed no upper fence: it inserted everything
* in event order, so "stay below anything you touch" sufficed. Repacking
* fires rejected extras after later-ordered slices are already committed,
* which is what makes strict order two-sided here.
*/
function computeLevelFence(slice, state) {
	const { levels } = state;
	let min = 0;
	let maxExclusive = levels.length;
	for (const reservation of findIntersections(state.moreLinkReservations, slice)) maxExclusive = Math.min(maxExclusive, reservation.levelIndex);
	if (state.eventOrderStrict) {
		for (let levelIndex = 0; levelIndex < levels.length; levelIndex++) for (const other of findIntersections(levels[levelIndex], slice)) if (other.sourceSeg.orderIndex < slice.sourceSeg.orderIndex) min = Math.max(min, levelIndex + 1);
		else if (other.sourceSeg.orderIndex > slice.sourceSeg.orderIndex) maxExclusive = Math.min(maxExclusive, levelIndex);
	}
	return {
		min,
		maxExclusive
	};
}
var MAX_SLICES_PER_PLAN = 3;
var EXTRA_SLICE_PENALTY = .15;
/**
* Scores the best one-, two-, or three-run insertion offered by each level.
* Runs from different levels are deliberately never mixed into one plan.
*/
function findBestSlicePlan(slice, state, requiresSlicing) {
	let selected = null;
	const sourceLength = getSpanLength(slice);
	for (let levelIndex = 0; levelIndex < state.levels.length; levelIndex++) {
		const blockers = findIntersections(state.levels[levelIndex], slice);
		for (const reservation of state.moreLinkReservations) if (levelIndex >= reservation.levelIndex) addToUnion(blockers, reservation);
		const runs = subtractSpansFromSlice(slice, blockers).filter((run) => isWithinLevelFence(run, levelIndex, state)).sort((a, b) => getSpanLength(b) - getSpanLength(a) || a.start - b.start);
		let visibleLength = 0;
		for (let sliceCount = 1; sliceCount <= Math.min(MAX_SLICES_PER_PLAN, runs.length); sliceCount++) {
			visibleLength += getSpanLength(runs[sliceCount - 1]);
			if (requiresSlicing && visibleLength >= sourceLength - GEOMETRY_TOLERANCE) break;
			const candidate = {
				levelIndex,
				slices: runs.slice(0, sliceCount),
				score: visibleLength / sourceLength - EXTRA_SLICE_PENALTY * (sliceCount - 1)
			};
			if (isBetterSlicePlan(candidate, selected)) selected = candidate;
		}
	}
	if (selected) selected.slices.sort(compareByEventOrder);
	return selected;
}
/** Whether a slice may legally sit at this level, per its own fence. */
function isWithinLevelFence(slice, levelIndex, state) {
	const fence = computeLevelFence(slice, state);
	return levelIndex >= fence.min && levelIndex < fence.maxExclusive;
}
/** Comparison: score, then less fragmentation, then the shallower level. */
function isBetterSlicePlan(candidate, current) {
	if (!current || candidate.score > current.score) return true;
	if (candidate.score < current.score) return false;
	if (candidate.slices.length !== current.slices.length) return candidate.slices.length < current.slices.length;
	return candidate.levelIndex < current.levelIndex;
}
/**
* Merges strict lateral intersections into groups sorted by lateral start.
* Each group's hidden slices are event-ordered.
*/
function groupLaterallyIntersecting(hiddenSlices) {
	const groups = [];
	for (const slice of hiddenSlices) addHiddenSliceToGroups(groups, slice);
	return finalizeHiddenGroups(groups);
}
/**
* Adds one hidden slice to its strict-intersection component and returns only
* the newly covered spans. Exactly adjacent groups deliberately stay separate
* because each group corresponds to one independently rendered more link.
* The group list remains sorted by lateral start.
*/
function addHiddenSliceToGroups(groups, slice) {
	const newSpans = subtractCoveredSpans(slice, groups);
	const untouchedGroups = [];
	const mergedSlices = [slice];
	let start = slice.start;
	let end = slice.end;
	for (const group of groups) if (intersectSpans(group, slice)) {
		mergedSlices.push(...group.hiddenSlices);
		start = Math.min(start, group.start);
		end = Math.max(end, group.end);
	} else untouchedGroups.push(group);
	mergedSlices.sort(compareByEventOrder);
	insertLaterally(untouchedGroups, {
		start,
		end,
		hiddenSlices: mergedSlices
	});
	groups.splice(0, groups.length, ...untouchedGroups);
	return newSpans;
}
/**
* Compiles internal accumulator groups for components: one entry per hidden
* source event, in event order, spanning that source's fragment hull.
*/
function finalizeHiddenGroups(groups) {
	return groups.map((group) => {
		const hiddenSlices = mergeAdjacentSlices(group.hiddenSlices);
		return {
			key: getSliceKey(hiddenSlices[0]),
			start: group.start,
			end: group.end,
			hiddenSlices
		};
	});
}
/**
* Identifies a whole or partial slice derived from a source seg. Partial keys
* deliberately omit the lateral end so a fragment re-cut at the same start
* keeps its DOM wrapper. The re-cut fragment transiently reuses the previous
* cut's measurement, which can mis-prune one pass; the structure still settles
* because every fragment cut depends only on logical geometry — the same
* wrapper just re-reports at its new width and the next pass corrects the
* decision.
*/
function getSliceKey(slice) {
	if (!isPartialSlice(slice)) return slice.sourceSeg.key;
	return `${slice.sourceSeg.key}:${slice.start}:slice`;
}
function isPartialSlice(slice) {
	return slice.start !== slice.sourceSeg.start || slice.end !== slice.sourceSeg.end;
}
function compareByEventOrder(a, b) {
	return a.sourceSeg.orderIndex - b.sourceSeg.orderIndex || a.start - b.start || b.end - a.end;
}
function sortByEventOrder(slices) {
	return [...slices].sort(compareByEventOrder);
}
/** Orders by axis start, then resolved event order. */
function compareByAxisOrder(a, b) {
	return a.start - b.start || a.sourceSeg.orderIndex - b.sourceSeg.orderIndex;
}
function sortByAxisOrder(items) {
	return [...items].sort(compareByAxisOrder);
}
/**
* Collapses same-source runs of an event-ordered slice list into one slice
* per run spanning the run's lateral hull. The hull can bridge territory
* where the source is actually visible; consumers derive start/end
* continuity from the outermost hidden edges, not exact hidden coverage.
*/
function mergeAdjacentSlices(slices) {
	const merged = [];
	for (const slice of slices) {
		const previous = merged[merged.length - 1];
		if (previous && previous.sourceSeg === slice.sourceSeg) merged[merged.length - 1] = createNarrowerSlice(createWholeSlice(previous.sourceSeg), previous.start, Math.max(previous.end, slice.end));
		else merged.push(slice);
	}
	return merged;
}
/**
* Removes covered spans from a slice, returning identity-preserving
* remainders. Like `subtractCoveredSpans`, the covered spans must be sorted
* by start and pairwise non-overlapping — every caller already holds them
* that way (plan slices, union blockers, a single span).
*/
function subtractSpansFromSlice(slice, covered) {
	return subtractCoveredSpans(slice, covered).map((span) => createNarrowerSlice(slice, span.start, span.end));
}
/** Finds the strict intersection while retaining source identity. */
function intersectSlice(slice, barrier) {
	const intersection = intersectSpans(slice, barrier);
	return intersection ? createNarrowerSlice(slice, intersection.start, intersection.end) : null;
}
function createWholeSlice(sourceSeg) {
	return {
		sourceSeg,
		start: sourceSeg.start,
		end: sourceSeg.end,
		isStart: sourceSeg.isStart,
		isEnd: sourceSeg.isEnd
	};
}
function createNarrowerSlice(parent, start, end) {
	return {
		sourceSeg: parent.sourceSeg,
		start,
		end,
		isStart: parent.isStart && start === parent.start,
		isEnd: parent.isEnd && end === parent.end
	};
}
/** Initial DOM candidate frontier, before any measurement can widen it. */
var DEFAULT_LEVEL_CAPACITY = estimateLevelCapacity(150, 20);
/** Converts sorted production ranges into the shared source vocabulary. */
function buildDayGridSegSources(eventOrderedSegs) {
	return eventOrderedSegs.map((seg, orderIndex) => ({
		...seg,
		key: getDayGridSegKey(seg),
		orderIndex
	}));
}
/**
* Builds an immediately renderable kernel layout for unlimited and numeric
* DayGrid modes. Boolean-auto uses the pixel-limited adapter below.
*/
function buildDayGridLevelPlacements(eventOrderedSegs, maxLevels, moreLinkLevelTax, orderStrict, eventSlicing, columnCount, sliceHeights) {
	const sourceSegs = buildDayGridSegSources(eventOrderedSegs);
	return buildDayGridPlacementLayout(sourceSegs, buildLevelLimitedLayout(sourceSegs, orderStrict, eventSlicing, maxLevels, moreLinkLevelTax, sliceHeights), sliceHeights, columnCount);
}
/** Builds the boolean-auto DayGrid route with a real pixel ceiling. */
function buildDayGridPixelPlacements(eventOrderedSegs, orderStrict, eventSlicing, columnCount, canvasHeight, moreLinkHeight, levelCapacity, sliceHeights) {
	const sourceSegs = buildDayGridSegSources(eventOrderedSegs);
	return buildDayGridPlacementLayout(sourceSegs, buildPixelLimitedLayout(sourceSegs, orderStrict, eventSlicing, sliceHeights, canvasHeight, levelCapacity, moreLinkHeight), sliceHeights, columnCount);
}
/** Projects ordered sources and event-ordered hidden slices into one cell. */
function buildDayGridPopoverSegs(eventOrderedSegs, hiddenSlices, column) {
	return {
		segs: flatMapArray(eventOrderedSegs, (source) => cutSegToColumn(source, column) ?? []),
		hiddenSegs: flatMapArray(hiddenSlices, (slice) => cutSegToColumn(slice.sourceSeg, column, slice) ?? [])
	};
}
/**
* Projects one complete source onto a column when its relevant span intersects.
*
* The optional span lets a hidden slice control membership while real event
* boundaries still control whether the projected entry reports "continues."
*/
function cutSegToColumn(source, column, intersectionSpan = source) {
	if (intersectionSpan.start >= column + 1 || column >= intersectionSpan.end) return null;
	const { key, orderIndex, ...seg } = source;
	return {
		...seg,
		start: column,
		end: column + 1,
		isStart: seg.isStart && source.start === column,
		isEnd: seg.isEnd && source.end - 1 === column
	};
}
/**
* Resolves which measured route a row takes from the two max options.
*
* This is the single definition of production's option precedence: a boolean
* `true` on either option means auto, and only then does a number on either
* one apply, `dayMaxEvents` first.
*/
function resolveDayGridPlacementMode(dayMaxEvents, dayMaxEventRows) {
	if (dayMaxEvents === true || dayMaxEventRows === true) return "auto";
	if (typeof dayMaxEvents === "number") return "maxEvents";
	if (typeof dayMaxEventRows === "number") return "maxEventRows";
	return "unlimited";
}
/**
* Computes the dimensionless DOM frontier from an already-resolved mode,
* without applying more-link tax. Numeric limits own their explicit cap;
* unlimited rows mount all sources; boolean-auto rows consume their row-local
* observed frontier.
*/
function computeDayGridDomCandidateMaxLevels(mode, dayMaxEvents, dayMaxEventRows, maxDomLevels) {
	switch (mode) {
		case "auto": return maxDomLevels;
		case "maxEvents": return dayMaxEvents;
		case "maxEventRows": return dayMaxEventRows;
		default: return Infinity;
	}
}
/**
* Logical levels an active more link charges its column. Only `dayMaxEventRows`
* counts the link as one of its rows.
*/
function computeDayGridMoreLinkLevelTax(mode) {
	return mode === "maxEventRows" ? 1 : 0;
}
function buildDayGridPlacementLayout(sourceSegs, layout, sliceHeights, columnCount) {
	const { hiddenSlices, renderSlices, sliceCoords } = layout;
	const eventOrderedHiddenSlices = sortByEventOrder(hiddenSlices);
	const slicesByStart = federateSlicesByStart(renderSlices, columnCount);
	const columns = Array.from({ length: columnCount }, (_, column) => ({
		renderSlices: slicesByStart[column],
		contentHeight: 0,
		...buildDayGridPopoverSegs(sourceSegs, eventOrderedHiddenSlices, column)
	}));
	for (const slice of renderSlices) {
		const key = getSliceKey(slice);
		const sliceTop = sliceCoords.get(key);
		if (sliceTop === void 0) continue;
		const sliceBottom = sliceTop + sliceHeights.get(key);
		for (let column = slice.start; column < slice.end; column += 1) columns[column].contentHeight = Math.max(columns[column].contentHeight, sliceBottom);
	}
	return {
		columns,
		sliceCoords
	};
}
function federateSlicesByStart(renderSlices, columnCount) {
	const slicesByStart = Array.from({ length: columnCount }, () => []);
	for (const slice of renderSlices) slicesByStart[slice.start].push(slice);
	for (const slices of slicesByStart) slices.sort(compareByEventOrder);
	return slicesByStart;
}
function estimateLevelCapacity(eventAreaHeight, eventHeight) {
	return Math.max(1, Math.ceil(eventAreaHeight / eventHeight));
}
/** High but finite safety cap for event levels in either print view. */
var DEFAULT_PRINT_MAX_LEVELS = 200;
function planPrintDomCandidates(eventOrderedSegs, eventOrderStrict, eventSlicing) {
	const { segLevels, excludedSegs } = buildSegLevels(eventOrderedSegs, eventOrderStrict, DEFAULT_PRINT_MAX_LEVELS);
	const placement = placeExtraSlicesInLevels(convertSegLevelsToWholeSlices(segLevels), convertSegsToWholeSlices(excludedSegs), eventOrderStrict, eventSlicing, 0);
	return {
		sliceLevels: placement.sliceLevels,
		hiddenSlices: placement.hiddenSlices
	};
}
/**
* Projects dimensionless levels into independently page-breakable bands.
*
* The level entries may carry unit-thickness planning coordinates, but those
* coordinates have no print meaning. Every print slice begins at level
* coordinate zero in its own band, whose thickness is the largest current
* slice-wrapper measurement. Missing measurements use the supplied fallback.
* Empty or sparse levels do not create empty DOM bands. The key resolver lets
* adapters retain their wrapper-level measurement identity.
*/
function buildPrintEventBands(levels, printEventThicknesses, getPrintEventKey = (slice) => slice.sourceSeg.key, defaultPrintEventThickness = 20) {
	const bands = [];
	for (let levelIndex = 0; levelIndex < levels.length; levelIndex++) {
		const entries = levels[levelIndex];
		if (!entries?.length) continue;
		let thickness = 0;
		const slices = entries.map((slice) => {
			thickness = Math.max(thickness, printEventThicknesses.get(getPrintEventKey(slice)) ?? defaultPrintEventThickness);
			return slice;
		});
		bands.push({
			levelIndex,
			slices,
			thickness
		});
	}
	return bands;
}
/** Plans one print row from its complete, resolved-order source list. */
function buildDayGridPrintPlan(eventOrderedSegs, orderStrict, eventSlicing, columnCount) {
	const sourceSegs = buildDayGridSegSources(eventOrderedSegs);
	const candidatePlan = planPrintDomCandidates(sourceSegs, orderStrict, eventSlicing);
	return {
		...candidatePlan,
		hiddenSlices: sortByEventOrder(candidatePlan.hiddenSlices),
		sourceSegs,
		columnCount
	};
}
/** Transposes row-wide print bands into one aligned slot sequence per cell. */
function buildDayGridPrintColumns(plan, printSegHeights) {
	const columns = Array.from({ length: plan.columnCount }, () => []);
	for (const band of buildPrintEventBands(plan.sliceLevels, printSegHeights, getDayGridPrintSliceKey)) {
		const slicesByColumn = Array(plan.columnCount).fill(null);
		for (const slice of band.slices) slicesByColumn[slice.start] = slice;
		for (let column = 0; column < plan.columnCount; column++) columns[column].push({
			levelIndex: band.levelIndex,
			thickness: band.thickness,
			slice: slicesByColumn[column]
		});
	}
	return columns;
}
/** Stable identity for one print wrapper when a source is split laterally. */
function getDayGridPrintSliceKey(slice) {
	return `${slice.sourceSeg.key}:${slice.start}:${slice.end}`;
}
var DEFAULT_WEEK_NUM_FORMAT$1 = createFormatter({ week: "narrow" });
var DayGridRow = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.headerHeightRefMap = new RefMap(() => {
			afterSize(this.handleSegPositioning);
		});
		this.mainHeightRefMap = new RefMap(() => {
			if (this.props.dayMaxEvents === true || this.props.dayMaxEventRows === true) afterSize(this.handleSegPositioning);
		});
		this.sliceHeightRefMap = new RefMap(() => {
			afterSize(this.handleSegPositioning);
		});
		this.handlePrintSegHeightChange = () => {
			afterSize(this.handlePrintSegHeights);
		};
		this.printSegHeightRefMap = new RefMap(this.handlePrintSegHeightChange);
		this.buildWeekNumberRenderProps = memoize(buildWeekNumberRenderProps);
		this.buildPrintPlan = memoize(buildDayGridPrintPlan);
		this.sortEventSegs = memoize(sortEventSegs);
		this.levelCapacity = DEFAULT_LEVEL_CAPACITY;
		this.handleRootEl = (rootEl) => {
			this.disconnectHeight?.();
			this.disconnectHeight = void 0;
			setRef(this.props.rootElRef, rootEl);
			if (rootEl) this.disconnectHeight = watchHeight(rootEl, (contentHeight) => {
				setRef(this.props.heightRef, contentHeight);
			});
		};
		this.handleSegPositioning = () => {
			if (this._isUnmounting || this.props.forPrint) return;
			this.updateAutoPlacementRatchets();
			this.forceUpdate();
		};
		this.handlePrintSegHeights = () => {
			if (this._isUnmounting || !this.props.forPrint) return;
			this.forceUpdate();
		};
	}
	render() {
		const { props, context, headerHeightRefMap, mainHeightRefMap } = this;
		const { cells, tableMode } = props;
		const { options } = context;
		const weekDateMarker = props.cells[0].date;
		const fgEventSegs = this.sortEventSegs(props.fgEventSegs, options.eventOrder);
		const screenFgLiquidHeight = props.dayMaxEvents === true || props.dayMaxEventRows === true;
		let printPlan = null;
		let printColumns = null;
		let screenColumns = null;
		let screenSliceCoords = /* @__PURE__ */ new Map();
		let screenMainOffsetsByCol = [];
		let screenHeightsByCol = [];
		if (props.forPrint) {
			printPlan = this.buildPrintPlan(fgEventSegs, options.eventOrderStrict, options.eventSlicing, cells.length);
			printColumns = buildDayGridPrintColumns(printPlan, this.printSegHeightRefMap.current);
		} else {
			const placementMode = resolveDayGridPlacementMode(props.dayMaxEvents, props.dayMaxEventRows);
			const [maxMainTop, minMainHeight] = this.computeFgDims();
			const screenLayout = placementMode === "auto" ? buildDayGridPixelPlacements(fgEventSegs, options.eventOrderStrict, options.eventSlicing, cells.length, minMainHeight, props.moreLinkHeight, this.levelCapacity, this.sliceHeightRefMap.current) : buildDayGridLevelPlacements(fgEventSegs, computeDayGridDomCandidateMaxLevels(placementMode, props.dayMaxEvents, props.dayMaxEventRows, Infinity), computeDayGridMoreLinkLevelTax(placementMode), options.eventOrderStrict, options.eventSlicing, cells.length, this.sliceHeightRefMap.current);
			screenColumns = screenLayout.columns;
			screenSliceCoords = screenLayout.sliceCoords;
			if (maxMainTop != null) for (let col = 0; col < cells.length; col++) {
				const cellHeaderHeight = headerHeightRefMap.current.get(cells[col].key);
				const mainOffset = cellHeaderHeight != null ? maxMainTop - cellHeaderHeight : void 0;
				screenMainOffsetsByCol.push(mainOffset);
				screenHeightsByCol.push(mainOffset != null ? screenColumns[col].contentHeight + mainOffset : void 0);
			}
		}
		const highlightSegs = this.getHighlightSegs();
		const hasNavLink = options.navLinks;
		const fullWeekStr = buildDateStr(context, weekDateMarker, "week");
		const weekNumberRenderProps = this.buildWeekNumberRenderProps(weekDateMarker, context, props.cellIsNarrow, hasNavLink);
		const fillsByCol = cells.map(() => []);
		const weekNumberNode = props.showWeekNumbers && !props.cellIsMicro ? u(ContentContainer, {
			tag: "div",
			attrs: {
				...hasNavLink ? buildNavLinkAttrs(context, weekDateMarker, "week", fullWeekStr, false) : {},
				"role": void 0,
				"aria-hidden": true
			},
			className: DAY_GRID_EVENT_Z_CLASS,
			renderProps: weekNumberRenderProps,
			generatorName: "inlineWeekNumberContent",
			customGenerator: options.inlineWeekNumberContent,
			defaultGenerator: renderText$1,
			classNameGenerator: options.inlineWeekNumberClass,
			didMount: options.inlineWeekNumberDidMount,
			willUnmount: options.inlineWeekNumberWillUnmount
		}) : null;
		if (tableMode && weekNumberNode) fillsByCol[0].push(u("div", {
			className: joinClassNames(classNames.fillY, classNames.start0, classNames.pointerEventsNone),
			style: { width: this.computeSpanWidth(0, cells.length) },
			children: weekNumberNode
		}, "week-number"));
		this.appendFillSegs(fillsByCol, props.businessHourSegs, "non-business", DAY_GRID_NON_BUSINESS_Z_CLASS);
		this.appendFillSegs(fillsByCol, props.bgEventSegs, "bg-event", DAY_GRID_BG_EVENT_Z_CLASS);
		this.appendFillSegs(fillsByCol, highlightSegs, "highlight", DAY_GRID_HIGHLIGHT_Z_CLASS);
		return u(tableMode ? "tr" : "div", {
			role: props.role,
			"aria-label": props.role === "row" ? fullWeekStr : void 0,
			className: joinClassNames(options.dayRowClass, props.className, tableMode && classNames.borderless, !tableMode && classNames.flexRow, !tableMode && classNames.rel, !tableMode && classNames.borderlessX, !tableMode && classNames.borderlessTop, !tableMode && !props.borderBottom && classNames.borderlessBottom, classNames.isolate),
			style: { flexBasis: tableMode ? void 0 : props.basis },
			ref: this.handleRootEl,
			children: [!tableMode && weekNumberNode, props.cells.map((cell, col) => {
				const printPopover = printPlan ? buildDayGridPopoverSegs(printPlan.sourceSegs, printPlan.hiddenSlices, col) : null;
				let fg;
				if (printPlan) fg = this.renderPrintBandSlots(printColumns[col]);
				else fg = [...this.renderLevelFgSegs(screenMainOffsetsByCol[col], screenColumns[col].renderSlices, screenSliceCoords), ...this.renderMirrorFgSegs(col, screenMainOffsetsByCol[col], screenSliceCoords)];
				return u(DayGridCell, {
					dateProfile: props.dateProfile,
					todayRange: props.todayRange,
					date: cell.date,
					isMajor: cell.isMajor,
					isDisabled: cell.isDisabled,
					showDayNumber: props.showDayNumbers,
					isNarrow: props.cellIsNarrow,
					isMicro: props.cellIsMicro,
					borderStart: Boolean(col),
					borderBottom: props.borderBottom,
					tableMode,
					fills: fillsByCol[col],
					segs: printPopover ? printPopover.segs : screenColumns[col].segs,
					hiddenSegs: printPopover ? printPopover.hiddenSegs : screenColumns[col].hiddenSegs,
					fgLiquidHeight: printPlan ? false : screenFgLiquidHeight,
					fg,
					eventDrag: printPlan ? null : props.eventDrag,
					eventResize: printPlan ? null : props.eventResize,
					eventSelection: props.eventSelection,
					renderProps: cell.renderProps,
					dateSpanProps: cell.dateSpanProps,
					attrs: cell.attrs,
					className: cell.className,
					fgHeight: printPlan ? void 0 : screenHeightsByCol[col],
					width: props.colWidth,
					headerHeightRef: printPlan ? void 0 : headerHeightRefMap.createRef(cell.key),
					mainHeightRef: printPlan ? void 0 : mainHeightRefMap.createRef(cell.key)
				}, cell.key);
			})]
		});
	}
	/** Mirrors align with kernel coordinates but bypass admission and measurement. */
	renderMirrorFgSegs(col, mainOffset, sliceCoords) {
		const { props } = this;
		const { eventSelection } = props;
		const nodes = [];
		for (const seg of this.getMirrorSegs()) {
			if (seg.start !== col) continue;
			const key = getDayGridSegKey(seg);
			const { eventRange } = seg;
			const { instanceId } = eventRange.instance;
			const top = mainOffset != null ? mainOffset + (sliceCoords.get(key) ?? 0) : void 0;
			const isDragging = Boolean(props.eventDrag && props.eventDrag.affectedInstances[instanceId]);
			const isResizing = Boolean(props.eventResize && props.eventResize.affectedInstances[instanceId]);
			const isSelected = instanceId === eventSelection;
			nodes.push(u(MeasuredHeightHarness, {
				className: joinClassNames(classNames.abs, classNames.start0, DAY_GRID_INTERACTION_Z_CLASS),
				style: {
					top,
					width: this.computeSpanWidth(seg.start, seg.end)
				},
				heightRef: null,
				children: this.renderEventContent(seg, eventRange, {
					isDragging,
					isResizing,
					isMirror: true,
					isSelected
				})
			}, `mirror:${key}`));
		}
		return nodes;
	}
	/** Renders every kernel slice with its own measurement ref. */
	renderLevelFgSegs(mainOffset, slices, sliceCoords) {
		const { props } = this;
		const { eventSelection } = props;
		const nodes = [];
		for (const slice of slices) {
			const key = getSliceKey(slice);
			const sliceTop = sliceCoords.get(key);
			const { eventRange } = slice.sourceSeg;
			const { instanceId } = eventRange.instance;
			const top = mainOffset != null && sliceTop != null ? mainOffset + sliceTop : void 0;
			const isDragging = Boolean(props.eventDrag && props.eventDrag.affectedInstances[instanceId]);
			const isResizing = Boolean(props.eventResize && props.eventResize.affectedInstances[instanceId]);
			const isInvisible = isDragging || isResizing || top == null;
			const isSelected = instanceId === eventSelection;
			nodes.push(u(MeasuredHeightHarness, {
				className: joinClassNames(classNames.abs, classNames.start0, isSelected ? DAY_GRID_INTERACTION_Z_CLASS : DAY_GRID_EVENT_Z_CLASS),
				style: {
					visibility: isInvisible ? "hidden" : void 0,
					top,
					width: this.computeSpanWidth(slice.start, slice.end)
				},
				heightRef: this.sliceHeightRefMap.createRef(key),
				children: this.renderEventContent(slice, eventRange, {
					isDragging,
					isResizing,
					isSelected
				})
			}, key));
		}
		return nodes;
	}
	/**
	* The inner event, identical on both placement routes. Only the wrapper
	* around it differs: the screen route positions it, print lets it sit at the
	* static top of its band slot.
	*/
	renderEventContent(range, eventRange, interaction) {
		const { props } = this;
		const isListItem = hasListItemDisplay(range, eventRange);
		return u(StandardEvent, {
			display: isListItem ? "list-item" : "row",
			eventRange,
			isStart: range.isStart,
			isEnd: range.isEnd,
			isDragging: Boolean(interaction.isDragging),
			isResizing: Boolean(interaction.isResizing),
			isMirror: Boolean(interaction.isMirror),
			isSelected: Boolean(interaction.isSelected),
			isNarrow: props.cellIsNarrow,
			defaultTimeFormat: DEFAULT_TABLE_EVENT_TIME_FORMAT,
			defaultDisplayEventEnd: props.cells.length === 1,
			disableResizing: isListItem,
			forcedTimeText: props.cellIsMicro ? "" : void 0,
			...getEventRangeMeta(eventRange, props.todayRange)
		});
	}
	/** Renders aligned print slots with in-flow event wrappers that can paginate with their bands. */
	renderPrintBandSlots(slots) {
		const { printSegHeightRefMap } = this;
		return slots.map((slot) => {
			const { slice } = slot;
			let eventNode = null;
			if (slice) {
				const sliceKey = getDayGridPrintSliceKey(slice);
				eventNode = u(MeasuredHeightHarness, {
					className: joinClassNames(classNames.rel, classNames.flowRoot, DAY_GRID_EVENT_Z_CLASS),
					style: { width: this.computeSpanWidth(slice.start, slice.end) },
					heightRef: printSegHeightRefMap.createRef(sliceKey),
					children: this.renderEventContent(slice, slice.sourceSeg.eventRange, {})
				}, sliceKey);
			}
			return u("div", {
				className: classNames.breakInsideAvoid,
				style: { height: slot.thickness },
				children: eventNode
			}, slot.levelIndex);
		});
	}
	computeSpanWidth(start, end) {
		const span = end - start;
		const percentWidth = `${span * 100}%`;
		const crossedBorderWidth = this.props.tableMode && start === 0 ? 0 : Math.max(0, span - 1) * COL_BORDER_WIDTH;
		return crossedBorderWidth ? `calc(${percentWidth} + ${crossedBorderWidth}px)` : percentWidth;
	}
	/** Places each fill in its first cell while allowing its wrapper to span subsequent cells. */
	appendFillSegs(fillsByCol, segs, fillType, zClassName) {
		const { props, context } = this;
		const { todayRange } = props;
		for (const seg of segs) fillsByCol[seg.start].push(u("div", {
			className: joinClassNames(classNames.fillY, classNames.start0, zClassName),
			style: { width: this.computeSpanWidth(seg.start, seg.end) },
			children: fillType === "bg-event" ? u(BgEvent, {
				eventRange: seg.eventRange,
				isStart: seg.isStart,
				isEnd: seg.isEnd,
				isNarrow: props.cellIsNarrow,
				isVertical: false,
				...getEventRangeMeta(seg.eventRange, todayRange)
			}) : renderFill(fillType, context.options)
		}, `${fillType}:${buildEventRangeKey(seg.eventRange)}:${seg.start}:${seg.end}`));
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentDidUpdate(prevProps) {
		if (prevProps.forPrint && !this.props.forPrint) this.printSegHeightRefMap = new RefMap(this.handlePrintSegHeightChange);
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectHeight?.();
		setRef(this.props.heightRef, null);
	}
	computeFgDims() {
		const { cells } = this.props;
		const headerHeightMap = this.headerHeightRefMap.current;
		const mainHeightMap = this.mainHeightRefMap.current;
		let maxMainTop;
		let minMainBottom;
		let isComplete = true;
		for (const cell of cells) {
			if (cell.isDisabled) continue;
			const mainTop = headerHeightMap.get(cell.key);
			const mainHeight = mainHeightMap.get(cell.key);
			if (mainTop == null || mainHeight == null) isComplete = false;
			if (mainTop != null) {
				if (maxMainTop === void 0 || mainTop > maxMainTop) maxMainTop = mainTop;
				if (mainHeight != null) {
					const mainBottom = mainTop + mainHeight;
					if (minMainBottom === void 0 || mainBottom < minMainBottom) minMainBottom = mainBottom;
				}
			}
		}
		return [maxMainTop, isComplete && minMainBottom != null && maxMainTop != null ? minMainBottom - maxMainTop : void 0];
	}
	/**
	* Grows the row-local DOM candidate frontier from one post-size snapshot.
	* This is the only monotone state auto placement needs: the engine itself
	* consumes exact measurements and never predicts a thickness.
	*/
	updateAutoPlacementRatchets() {
		if (resolveDayGridPlacementMode(this.props.dayMaxEvents, this.props.dayMaxEventRows) !== "auto") return;
		const [, canvasHeight] = this.computeFgDims();
		if (canvasHeight != null) {
			const smallestSliceHeight = Math.min(...this.sliceHeightRefMap.current.values());
			this.levelCapacity = Math.max(this.levelCapacity, estimateLevelCapacity(canvasHeight, smallestSliceHeight));
		}
	}
	getMirrorSegs() {
		let { props } = this;
		if (props.eventResize && props.eventResize.segs.length) return props.eventResize.segs;
		return [];
	}
	getHighlightSegs() {
		let { props } = this;
		if (props.eventDrag && props.eventDrag.segs.length) return props.eventDrag.segs;
		if (props.eventResize && props.eventResize.segs.length) return props.eventResize.segs;
		return props.dateSelectionSegs;
	}
};
function buildWeekNumberRenderProps(weekDateMarker, context, isNarrow, hasNavLink) {
	const { dateEnv, options } = context;
	const weekNum = dateEnv.computeWeekNumber(weekDateMarker);
	const weekNumTextParts = dateEnv.formatToParts(weekDateMarker, options.weekNumberFormat || DEFAULT_WEEK_NUM_FORMAT$1);
	return {
		num: weekNum,
		text: joinDateTimeFormatParts(weekNumTextParts),
		textParts: weekNumTextParts,
		date: dateEnv.toDate(weekDateMarker),
		isNarrow,
		hasNavLink
	};
}
var DaySeriesModel = class {
	constructor(range, dateProfileGenerator) {
		let date = range.start;
		let { end } = range;
		let entries = [];
		let dates = [];
		let dayIndex = -1;
		while (date < end) {
			if (dateProfileGenerator.isHiddenDay(date)) entries.push({
				kind: "hidden",
				previousIndex: dayIndex,
				nextIndex: dayIndex + 1
			});
			else {
				dayIndex += 1;
				entries.push({
					kind: "visible",
					index: dayIndex
				});
				dates.push(date);
			}
			date = addDays(date, 1);
		}
		this.rangeStart = range.start;
		this.dates = dates;
		this.entries = entries;
		this.cnt = dates.length;
	}
	sliceRange(range) {
		let firstResult = this.getDateIndex(range.start);
		let lastResult = this.getDateIndex(addDays(range.end, -1));
		let firstIndex = getFirstVisibleIndex(firstResult);
		let lastIndex = getLastVisibleIndex(lastResult);
		let clippedFirstIndex = Math.max(0, firstIndex);
		let clippedLastIndex = Math.min(this.cnt - 1, lastIndex);
		if (clippedFirstIndex <= clippedLastIndex) return {
			start: clippedFirstIndex,
			end: clippedLastIndex + 1,
			isStart: firstResult.kind === "visible" && firstIndex === clippedFirstIndex,
			isEnd: lastResult.kind === "visible" && lastIndex === clippedLastIndex
		};
		return null;
	}
	getDateIndex(date) {
		let dayOffset = Math.floor(diffDays(this.rangeStart, date));
		if (dayOffset < 0) return {
			kind: "before",
			index: -1
		};
		if (dayOffset >= this.entries.length) return {
			kind: "after",
			index: this.cnt
		};
		return this.entries[dayOffset];
	}
};
function getFirstVisibleIndex(result) {
	return result.kind === "hidden" ? result.nextIndex : result.index;
}
function getLastVisibleIndex(result) {
	return result.kind === "hidden" ? result.previousIndex : result.index;
}
function buildDayTableModel(dateProfile, dateProfileGenerator, dateEnv) {
	const daySeries = new DaySeriesModel(dateProfile.renderRange, dateProfileGenerator);
	const breakOnWeeks = /year|month|week/.test(dateProfile.currentRangeUnit);
	const majorUnit = !breakOnWeeks && computeMajorUnit(dateProfile, dateEnv);
	return new DayTableModel(daySeries, breakOnWeeks, dateEnv, majorUnit !== "day" ? majorUnit : void 0, dateProfile.activeRange);
}
function computeColWidth(colCount, colMinWidth, viewportWidth) {
	if (viewportWidth == null) return [void 0, void 0];
	if (viewportWidth / colCount < colMinWidth) return [colMinWidth * colCount, colMinWidth];
	return [viewportWidth, void 0];
}
function computeTopFromDate(date, cellRows, rowHeightMap) {
	let top = 0;
	for (const cells of cellRows) {
		const key = cells[0].key;
		const start = cells[0].date;
		const end = cells[cells.length - 1].date;
		if (date >= start && date <= end) return top;
		const rowHeight = rowHeightMap.get(key);
		if (rowHeight == null) return;
		top += rowHeight;
	}
	return top;
}
function computeColFromPosition(positionLeft, elWidth, colWidth, colCount, isRtl) {
	const realColWidth = colWidth != null ? colWidth : elWidth / colCount;
	const colFromLeft = Math.floor(positionLeft / realColWidth);
	const col = isRtl ? colCount - colFromLeft - 1 : colFromLeft;
	const left = colFromLeft * realColWidth;
	return {
		col,
		left,
		right: left + realColWidth
	};
}
function computeRowFromPosition(positionTop, cellRows, rowHeightMap) {
	let row = 0;
	let top = 0;
	let bottom = 0;
	for (const cells of cellRows) {
		const key = cells[0].key;
		top = bottom;
		bottom = top + rowHeightMap.get(key);
		if (positionTop < bottom) break;
		row++;
	}
	return {
		row,
		top,
		bottom
	};
}
function getRowEl(rootEl, row) {
	return rootEl.querySelectorAll("[role=row]")[row];
}
function getCellEl(rowEl, col) {
	return rowEl.querySelectorAll("[role=gridcell]")[col];
}
var dayHeaderMicroFormat = createFormatter({ weekday: "narrow" });
function createDayHeaderFormatter(explicitFormat, datesRepDistinctDays, dateCnt) {
	return explicitFormat || computeFallbackHeaderFormat(datesRepDistinctDays, dateCnt);
}
function computeFallbackHeaderFormat(datesRepDistinctDays, dayCnt) {
	if (!datesRepDistinctDays) return createFormatter({ weekday: "short" });
	if (dayCnt > 1) return createFormatter({
		weekday: "short",
		weekdayJustify: "start",
		day: "numeric",
		omitCommas: true,
		omitTrailing: true
	});
	return createFormatter({
		weekday: "long",
		weekdayJustify: "start",
		day: "numeric",
		omitCommas: true,
		omitTrailing: true
	});
}
var DayGridRows = class extends DateComponent {
	constructor() {
		super(...arguments);
		this.state = {};
		this.splitBusinessHourSegs = memoize(splitSegsByRow);
		this.splitBgEventSegs = memoize(splitAllDaySegsByRow);
		this.splitFgEventSegs = memoize(splitSegsByRow);
		this.splitDateSelectionSegs = memoize(splitSegsByRow);
		this.splitEventDrag = memoize(splitInteractionByRow);
		this.splitEventResize = memoize(splitInteractionByRow);
		this.rowHeightRefMap = new RefMap((height, key) => {
			const { rowHeightRefMap } = this.props;
			if (rowHeightRefMap) rowHeightRefMap.handleValue(height, key);
		});
		this.handleMoreLinkEl = (el) => {
			this.disconnectMoreLinkHeight?.();
			this.disconnectMoreLinkHeight = void 0;
			if (el) this.disconnectMoreLinkHeight = watchHeight(el, (height) => {
				if (this._isUnmounting) return;
				this.setState({ moreLinkHeight: height });
			});
		};
		this.handleRootEl = (rootEl) => {
			this.rootEl = rootEl;
			if (rootEl) this.context.registerInteractiveComponent(this, {
				el: rootEl,
				isHitComboAllowed: this.props.isHitComboAllowed
			});
			else this.context.unregisterInteractiveComponent(this);
		};
	}
	render() {
		let { props, state, context, rowHeightRefMap } = this;
		let { options } = context;
		let { cellRows, tableMode } = props;
		let rowCount = cellRows.length;
		let firstCellKey = cellRows[0]?.[0]?.key || "";
		let fgEventSegsByRow = this.splitFgEventSegs(props.fgEventSegs, rowCount);
		let bgEventSegsByRow = this.splitBgEventSegs(props.bgEventSegs, rowCount);
		let businessHourSegsByRow = this.splitBusinessHourSegs(props.businessHourSegs, rowCount);
		let dateSelectionSegsByRow = this.splitDateSelectionSegs(props.dateSelectionSegs, rowCount);
		let eventDragByRow = this.splitEventDrag(props.eventDrag, rowCount);
		let eventResizeByRow = this.splitEventResize(props.eventResize, rowCount);
		let isHeightAuto = getIsHeightAuto(options);
		let rowHeightsRedistribute = !props.forPrint && !isHeightAuto;
		let rowBasis = computeRowBasis(props.visibleWidth, rowCount, isHeightAuto, options);
		const needsMoreLinkProbe = !props.forPrint && resolveDayGridPlacementMode(props.dayMaxEvents, props.dayMaxEventRows) === "auto";
		return u(S, { children: [u(tableMode ? "tbody" : "div", {
			role: "rowgroup",
			className: joinClassNames(props.className, !tableMode && !props.forPrint && classNames.flexCol),
			style: tableMode ? void 0 : { width: props.width },
			ref: this.handleRootEl,
			children: cellRows.map((cells, row) => u(DayGridRow, {
				role: "row",
				dateProfile: props.dateProfile,
				todayRange: props.todayRange,
				cells,
				cellIsNarrow: props.cellIsNarrow,
				cellIsMicro: props.cellIsMicro,
				showDayNumbers: rowCount > 1,
				showWeekNumbers: rowCount > 1 && options.weekNumbers,
				forPrint: props.forPrint,
				tableMode,
				borderBottom: row < rowCount - 1,
				className: rowHeightsRedistribute ? classNames.grow : void 0,
				fgEventSegs: fgEventSegsByRow[row],
				bgEventSegs: bgEventSegsByRow[row],
				businessHourSegs: businessHourSegsByRow[row],
				dateSelectionSegs: dateSelectionSegsByRow[row],
				eventSelection: props.eventSelection,
				eventDrag: eventDragByRow[row],
				eventResize: eventResizeByRow[row],
				dayMaxEvents: props.dayMaxEvents,
				dayMaxEventRows: props.dayMaxEventRows,
				colWidth: props.colWidth,
				basis: rowBasis,
				moreLinkHeight: state.moreLinkHeight,
				heightRef: rowHeightRefMap.createRef(cells[0].key)
			}, firstCellKey + ":" + cells[0].key))
		}), needsMoreLinkProbe && u(MoreLinkTrigger, {
			num: 1,
			display: "row",
			isNarrow: props.cellIsNarrow,
			isMicro: props.cellIsMicro,
			elRef: this.handleMoreLinkEl,
			className: classNames.offscreen,
			attrs: {
				"aria-hidden": true,
				inert: ""
			}
		})] });
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectMoreLinkHeight?.();
	}
	queryHit(isRtl, positionLeft, positionTop, elWidth) {
		const { props } = this;
		const colCount = props.cellRows[0].length;
		const { col, left, right } = computeColFromPosition(positionLeft, elWidth, props.colWidth, colCount, isRtl);
		const { row, top, bottom } = computeRowFromPosition(positionTop, props.cellRows, this.rowHeightRefMap.current);
		const cell = props.cellRows[row][col];
		const cellStartDate = cell.date;
		const cellEndDate = addDays(cellStartDate, 1);
		return {
			dateProfile: props.dateProfile,
			dateSpan: {
				range: {
					start: cellStartDate,
					end: cellEndDate
				},
				allDay: true,
				...cell.dateSpanProps
			},
			getDayEl: () => getCellEl(getRowEl(this.rootEl, row), col),
			rect: {
				left,
				right,
				top,
				bottom
			},
			layer: 0
		};
	}
};
function isSegAllDay(seg) {
	return seg.eventRange.def.allDay;
}
function splitAllDaySegsByRow(segs, rowCnt) {
	return splitSegsByRow(segs.filter(isSegAllDay), rowCnt);
}
function computeRowBasis(visibleWidth, rowCount, isHeightAuto, options) {
	if (visibleWidth != null) {
		const rowBasis = visibleWidth / options.aspectRatio / 6;
		return rowCount > 6 || isHeightAuto ? rowBasis : 0;
	}
	return 0;
}
var DayGridHeaderCell = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.state = {};
		this.buildDayHeaderText = memoize(buildDayHeaderText);
		this.handleInnerEl = (innerEl) => {
			if (this.disconnectSize) {
				this.disconnectSize();
				this.disconnectSize = void 0;
			}
			if (innerEl) this.disconnectSize = watchSize(innerEl, (width, height) => {
				if (this._isUnmounting) return;
				setRef(this.props.innerHeightRef, height);
				this.setState({ innerWidth: width });
			});
			else setRef(this.props.innerHeightRef, null);
		};
	}
	render() {
		const { props, state, context } = this;
		const { renderConfig, dataConfig, tableMode } = props;
		const colSpan = dataConfig.colSpan || 1;
		const totalColWidth = props.colWidth != null ? props.colWidth * colSpan : void 0;
		const isLiquid = !tableMode && totalColWidth == null;
		const isSpanning = isLiquid && colSpan > 1;
		const style = tableMode ? void 0 : isSpanning ? {
			flexGrow: colSpan,
			flexBasis: 0,
			minWidth: 0
		} : { width: totalColWidth };
		const isDisabled = dataConfig.renderProps.isDisabled;
		const finalRenderProps = renderConfig.dayHeaderFormat ? this.buildDayHeaderRenderProps(dataConfig.renderProps, props.cellIsNarrow, props.rowLevel, props.cellIsMicro, dataConfig.dateMarker, renderConfig.dayHeaderFormat, Boolean(renderConfig.datesRepDistinctDays), context.dateEnv) : {
			...dataConfig.renderProps,
			isNarrow: props.cellIsNarrow,
			level: props.rowLevel
		};
		const alignInput = renderConfig.align;
		const align = typeof alignInput === "function" ? alignInput({
			level: props.rowLevel,
			inPopover: dataConfig.renderProps.inPopover,
			isNarrow: props.cellIsNarrow
		}) : alignInput;
		const stickyInput = renderConfig.sticky;
		const isSticky = !tableMode && props.rowLevel > 0 && stickyInput !== false && (align !== "center" || totalColWidth != null && props.viewportWidth != null && totalColWidth > props.viewportWidth * .75);
		let edgeCoord;
		if (isSticky) {
			if (align === "center") {
				if (state.innerWidth != null) edgeCoord = `calc(50% - ${state.innerWidth / 2}px)`;
			} else edgeCoord = typeof stickyInput === "number" || typeof stickyInput === "string" ? stickyInput : 0;
		}
		const alignClassName = align === "center" ? classNames.alignCenter : align === "end" ? classNames.alignEnd : classNames.alignStart;
		return u(ContentContainer, {
			tag: tableMode ? "th" : "div",
			attrs: {
				role: "columnheader",
				"aria-colspan": dataConfig.colSpan,
				colSpan: tableMode ? colSpan : void 0,
				...dataConfig.attrs
			},
			className: joinClassNames(dataConfig.className, classNames.noMargin, classNames.noPadding, !tableMode && classNames.flexCol, classNames.borderlessTop, classNames.borderlessEnd, !props.borderStart && classNames.borderlessStart, !(tableMode && props.borderBottom) && classNames.borderlessBottom, !tableMode && alignClassName, isLiquid && !isSpanning && classNames.liquid, !isSticky && classNames.crop),
			style,
			renderProps: finalRenderProps,
			generatorName: renderConfig.generatorName,
			customGenerator: renderConfig.customGenerator,
			defaultGenerator: renderText$1,
			classNameGenerator: isDisabled ? void 0 : renderConfig.classNameGenerator,
			didMount: renderConfig.didMount,
			willUnmount: renderConfig.willUnmount,
			children: (InnerContainer) => u("div", {
				ref: this.handleInnerEl,
				className: joinClassNames(classNames.flexCol, classNames.noShrink, classNames.whiteSpaceNoWrap, tableMode && alignClassName, isSticky && classNames.sticky),
				style: {
					left: edgeCoord,
					right: edgeCoord
				},
				children: u(InnerContainer, {
					tag: "div",
					attrs: dataConfig.innerAttrs,
					className: generateClassName(renderConfig.innerClassNameGenerator, finalRenderProps)
				})
			})
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentWillUnmount() {
		this._isUnmounting = true;
	}
	buildDayHeaderRenderProps(renderProps, cellIsNarrow, rowLevel, cellIsMicro, dateMarker, dayHeaderFormat, datesRepDistinctDays, dateEnv) {
		const baseText = this.buildDayHeaderText(datesRepDistinctDays ? dateMarker : renderProps.date, dayHeaderFormat, datesRepDistinctDays, dateEnv);
		const textData = cellIsMicro ? this.buildDayHeaderText(dateMarker, dayHeaderMicroFormat, false, dateEnv) : baseText;
		return {
			...renderProps,
			isNarrow: cellIsNarrow,
			level: rowLevel,
			text: textData.text,
			textParts: textData.textParts,
			weekdayText: cellIsMicro ? textData.text : baseText.weekdayText,
			dayNumberText: baseText.dayNumberText
		};
	}
};
function buildDayHeaderText(date, formatter, includeDayNumber, dateEnv) {
	const textParts = dateEnv.formatToParts(date, formatter);
	return {
		text: joinDateTimeFormatParts(textParts),
		textParts,
		weekdayText: findWeekdayText(textParts),
		dayNumberText: includeDayNumber ? findDayNumberText(textParts) : ""
	};
}
var DayGridHeaderRow = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.innerHeightRefMap = new RefMap(() => {
			afterSize(this.handleInnerHeights);
		});
		this.handleInnerHeights = () => {
			if (this._isUnmounting) return;
			const innerHeightMap = this.innerHeightRefMap.current;
			let max = 0;
			for (const innerHeight of innerHeightMap.values()) max = Math.max(max, innerHeight);
			if (this.currentInnerHeight !== max) {
				this.currentInnerHeight = max;
				setRef(this.props.innerHeightRef, max);
			}
		};
	}
	render() {
		const { props, context } = this;
		const { tableMode } = props;
		const { options } = context;
		return u(tableMode ? "tr" : "div", {
			role: props.role,
			"aria-rowindex": props.rowIndex != null ? 1 + props.rowIndex : void 0,
			className: joinClassNames(options.dayHeaderRowClass, props.className, tableMode && classNames.borderless, !tableMode && classNames.flexRow, !tableMode && classNames.contentBox, !tableMode && classNames.borderlessX, !tableMode && classNames.borderlessTop, !tableMode && !props.borderBottom && classNames.borderlessBottom),
			style: { height: props.height },
			children: props.dataConfigs.map((dataConfig, cellI) => u(DayGridHeaderCell, {
				renderConfig: props.renderConfig,
				dataConfig,
				borderStart: Boolean(cellI),
				colWidth: props.colWidth,
				viewportWidth: props.viewportWidth,
				innerHeightRef: this.innerHeightRefMap.createRef(dataConfig.key),
				cellIsNarrow: props.cellIsNarrow,
				cellIsMicro: props.cellIsMicro,
				rowLevel: props.rowLevel,
				tableMode,
				borderBottom: props.borderBottom
			}, dataConfig.key))
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.currentInnerHeight = void 0;
		setRef(this.props.innerHeightRef, null);
	}
};
var DayGridHeaderRows = class extends BaseComponent {
	render() {
		const { props } = this;
		const { headerTiers, tableMode } = props;
		return headerTiers.map((rowConfig, i) => k(DayGridHeaderRow, {
			...rowConfig,
			key: i,
			role: "row",
			borderBottom: i < headerTiers.length - 1,
			colWidth: props.colWidth,
			viewportWidth: props.viewportWidth,
			cellIsNarrow: props.cellIsNarrow,
			cellIsMicro: props.cellIsMicro,
			rowLevel: headerTiers.length - i - 1,
			tableMode
		}));
	}
};
var DayGridLayoutPrint = class extends BaseComponent {
	render() {
		const { props, context } = this;
		const { options } = context;
		const tableDisplayInfo = {
			borderlessX: props.borderlessX,
			borderlessTop: props.borderlessTop,
			borderlessBottom: props.borderlessBottom,
			multiMonthColumns: props.multiMonthColumns
		};
		return u("table", {
			role: "presentation",
			className: joinClassNames(generateClassName(options.tableClass, tableDisplayInfo), classNames.printTable),
			style: props.style,
			children: [
				u("colgroup", { children: props.cellRows[0].map((cell) => u("col", {}, cell.key)) }),
				props.showHeader && u("thead", {
					ref: props.headerElRef,
					role: "rowgroup",
					className: generateClassName(options.tableHeaderClass, {
						...tableDisplayInfo,
						isSticky: false
					}),
					children: [u(DayGridHeaderRows, {
						tableMode: true,
						headerTiers: props.headerTiers,
						cellIsNarrow: props.cellIsNarrow,
						cellIsMicro: props.cellIsMicro
					}), u("tr", {
						role: "presentation",
						children: u("th", {
							role: "presentation",
							colSpan: props.cellRows[0].length,
							className: joinClassNames(classNames.noPadding, generateClassName(options.dayHeaderDividerClass, {
								isSticky: false,
								multiMonthColumns: props.multiMonthColumns,
								options: { allDaySlot: Boolean(options.allDaySlot) }
							}))
						})
					})]
				}),
				u(DayGridRows, {
					dateProfile: props.dateProfile,
					todayRange: props.todayRange,
					cellRows: props.cellRows,
					forPrint: true,
					tableMode: true,
					className: generateClassName(options.tableBodyClass, tableDisplayInfo),
					dayMaxEvents: void 0,
					dayMaxEventRows: props.dayMaxEventRows,
					fgEventSegs: props.fgEventSegs,
					bgEventSegs: props.bgEventSegs,
					businessHourSegs: props.businessHourSegs,
					dateSelectionSegs: [],
					eventDrag: null,
					eventResize: null,
					eventSelection: props.eventSelection,
					visibleWidth: props.visibleWidth,
					cellIsNarrow: props.cellIsNarrow,
					cellIsMicro: props.cellIsMicro,
					rowHeightRefMap: props.rowHeightRefMap
				})
			]
		});
	}
};
//#endregion
//#region node_modules/fullcalendar/chunks/64c4ade2.js
var DayGridHeader = class extends BaseComponent {
	render() {
		const { props } = this;
		return u("div", {
			role: "rowgroup",
			className: joinClassNames(props.className, classNames.flexCol, props.width == null && classNames.liquid),
			style: { width: props.width },
			children: u(DayGridHeaderRows, {
				headerTiers: props.headerTiers,
				colWidth: props.colWidth,
				viewportWidth: props.viewportWidth,
				cellIsNarrow: props.cellIsNarrow,
				cellIsMicro: props.cellIsMicro
			})
		});
	}
};
var DayGridLayoutNormal = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.state = {};
		this.handleScroller = (scroller) => {
			setRef(this.props.scrollerRef, scroller);
		};
		this.handleTotalWidth = (totalWidth) => {
			if (this._isUnmounting) return;
			this.setState({ totalWidth });
		};
		this.handleClientWidth = (clientWidth) => {
			if (this._isUnmounting) return;
			this.setState({ clientWidth });
		};
	}
	render() {
		const { props, state, context } = this;
		const { options } = context;
		const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
		const { totalWidth, clientWidth } = state;
		let endScrollbarWidth = totalWidth != null && clientWidth != null ? totalWidth - clientWidth : void 0;
		if (endScrollbarWidth < 3) endScrollbarWidth = 0;
		const verticalScrollbars = !props.forPrint && !getIsHeightAuto(options);
		const tableHeaderSticky = !props.forPrint && getTableHeaderSticky(options);
		const colCount = props.cellRows[0].length;
		const measuredColWidth = clientWidth != null ? clientWidth / colCount : void 0;
		const cellIsMicro = measuredColWidth != null && measuredColWidth <= 60;
		const cellIsNarrow = cellIsMicro || measuredColWidth != null && measuredColWidth <= options.dayNarrowWidth;
		if (props.forPrint) return u(DayGridLayoutPrint, {
			dateProfile: props.dateProfile,
			todayRange: props.todayRange,
			cellRows: props.cellRows,
			headerTiers: props.headerTiers,
			showHeader: Boolean(options.dayHeaders),
			fgEventSegs: props.fgEventSegs,
			bgEventSegs: props.bgEventSegs,
			businessHourSegs: props.businessHourSegs,
			eventSelection: props.eventSelection,
			dayMaxEventRows: options.dayMaxEventRows,
			borderlessX,
			borderlessTop,
			borderlessBottom,
			multiMonthColumns: 0,
			visibleWidth: totalWidth,
			cellIsNarrow,
			cellIsMicro,
			rowHeightRefMap: props.rowHeightRefMap
		});
		return u(S, { children: [
			options.dayHeaders && u("div", {
				className: joinClassNames(generateClassName(options.tableHeaderClass, {
					isSticky: tableHeaderSticky,
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), classNames.flexCol, tableHeaderSticky && classNames.tableHeaderSticky),
				children: [u("div", {
					className: classNames.flexRow,
					children: [u(DayGridHeader, {
						headerTiers: props.headerTiers,
						cellIsNarrow,
						cellIsMicro
					}), Boolean(endScrollbarWidth) && u("div", {
						className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: true }), classNames.borderlessY, classNames.borderlessEnd),
						style: { minWidth: endScrollbarWidth }
					})]
				}), u("div", { className: generateClassName(options.dayHeaderDividerClass, {
					isSticky: tableHeaderSticky,
					multiMonthColumns: 0,
					options: { allDaySlot: Boolean(options.allDaySlot) }
				}) })]
			}),
			u(Scroller, {
				vertical: verticalScrollbars,
				className: joinClassNames(generateClassName(options.tableBodyClass, {
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), !props.forPrint && classNames.flexCol, verticalScrollbars && classNames.liquid),
				ref: this.handleScroller,
				clientWidthRef: this.handleClientWidth,
				children: u(DayGridRows, {
					dateProfile: props.dateProfile,
					todayRange: props.todayRange,
					cellRows: props.cellRows,
					forPrint: props.forPrint,
					isHitComboAllowed: props.isHitComboAllowed,
					className: classNames.grow,
					dayMaxEvents: props.forPrint ? void 0 : options.dayMaxEvents,
					dayMaxEventRows: options.dayMaxEventRows,
					fgEventSegs: props.fgEventSegs,
					bgEventSegs: props.bgEventSegs,
					businessHourSegs: props.businessHourSegs,
					dateSelectionSegs: props.dateSelectionSegs,
					eventDrag: props.eventDrag,
					eventResize: props.eventResize,
					eventSelection: props.eventSelection,
					visibleWidth: totalWidth,
					cellIsNarrow,
					cellIsMicro,
					rowHeightRefMap: props.rowHeightRefMap
				})
			}),
			u(Ruler, { widthRef: this.handleTotalWidth })
		] });
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentWillUnmount() {
		this._isUnmounting = true;
	}
};
var FooterScrollbar = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.rootElRef = M();
	}
	render() {
		const { props } = this;
		return u("div", {
			ref: this.rootElRef,
			className: joinClassNames(classNames.footerScrollbar, props.isSticky && classNames.footerScrollbarSticky),
			children: u(Scroller, {
				horizontal: true,
				ref: props.scrollerRef,
				children: u("div", { style: { minWidth: props.canvasWidth } })
			})
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		this.disconnectHeight = watchHeight(this.rootElRef.current, (height) => {
			if (this._isUnmounting) return;
			setRef(this.props.scrollbarWidthRef, height);
		});
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectHeight();
		setRef(this.props.scrollbarWidthRef, null);
	}
};
var DayGridLayoutPannable = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.state = {};
		this.headerScrollerRef = M();
		this.bodyScrollerRef = M();
		this.footerScrollerRef = M();
		this.handleTotalWidth = (totalWidth) => {
			if (this._isUnmounting) return;
			this.setState({ totalWidth });
		};
		this.handleClientWidth = (clientWidth) => {
			if (this._isUnmounting) return;
			this.setState({ clientWidth });
		};
	}
	render() {
		const { props, state, context } = this;
		const { options } = context;
		const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
		const { totalWidth, clientWidth } = state;
		const endScrollbarWidth = totalWidth != null && clientWidth != null ? totalWidth - clientWidth : void 0;
		const verticalScrollbars = !props.forPrint && !getIsHeightAuto(options);
		const tableHeaderSticky = !props.forPrint && getTableHeaderSticky(options);
		const footerScrollbarSticky = !props.forPrint && getFooterScrollbarSticky(options);
		const colCount = props.cellRows[0].length;
		const [canvasWidth, appliedColWidth] = computeColWidth(colCount, props.dayMinWidth, clientWidth);
		const measuredColWidth = appliedColWidth ?? (clientWidth != null ? clientWidth / colCount : void 0);
		const cellIsMicro = measuredColWidth != null && measuredColWidth <= 60;
		const cellIsNarrow = cellIsMicro || measuredColWidth != null && measuredColWidth <= options.dayNarrowWidth;
		if (props.forPrint) return u(DayGridLayoutPrint, {
			dateProfile: props.dateProfile,
			todayRange: props.todayRange,
			cellRows: props.cellRows,
			headerTiers: props.headerTiers,
			showHeader: Boolean(options.dayHeaders),
			fgEventSegs: props.fgEventSegs,
			bgEventSegs: props.bgEventSegs,
			businessHourSegs: props.businessHourSegs,
			eventSelection: props.eventSelection,
			dayMaxEventRows: options.dayMaxEventRows,
			borderlessX,
			borderlessTop,
			borderlessBottom,
			multiMonthColumns: 0,
			visibleWidth: totalWidth,
			cellIsNarrow,
			cellIsMicro,
			rowHeightRefMap: props.rowHeightRefMap
		});
		return u(S, { children: [
			options.dayHeaders && u("div", {
				className: joinClassNames(generateClassName(options.tableHeaderClass, {
					isSticky: tableHeaderSticky,
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), classNames.flexCol, tableHeaderSticky && classNames.tableHeaderSticky),
				children: [u(Scroller, {
					horizontal: true,
					hideScrollbars: true,
					className: classNames.flexRow,
					ref: this.headerScrollerRef,
					children: [u(DayGridHeader, {
						headerTiers: props.headerTiers,
						colWidth: appliedColWidth,
						viewportWidth: clientWidth,
						width: canvasWidth,
						cellIsNarrow,
						cellIsMicro
					}), Boolean(endScrollbarWidth) && u("div", {
						className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: true }), classNames.borderlessY, classNames.borderlessEnd),
						style: { minWidth: endScrollbarWidth }
					})]
				}), u("div", { className: generateClassName(options.dayHeaderDividerClass, {
					isSticky: tableHeaderSticky,
					multiMonthColumns: 0,
					options: { allDaySlot: Boolean(options.allDaySlot) }
				}) })]
			}),
			u(Scroller, {
				vertical: verticalScrollbars,
				horizontal: true,
				hideScrollbars: footerScrollbarSticky || props.forPrint,
				className: joinClassNames(generateClassName(options.tableBodyClass, {
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), !props.forPrint && classNames.flexCol, verticalScrollbars && classNames.liquid),
				ref: this.bodyScrollerRef,
				clientWidthRef: this.handleClientWidth,
				children: u(DayGridRows, {
					dateProfile: props.dateProfile,
					todayRange: props.todayRange,
					cellRows: props.cellRows,
					forPrint: props.forPrint,
					isHitComboAllowed: props.isHitComboAllowed,
					className: classNames.grow,
					dayMaxEvents: props.forPrint ? void 0 : options.dayMaxEvents,
					dayMaxEventRows: options.dayMaxEventRows,
					fgEventSegs: props.fgEventSegs,
					bgEventSegs: props.bgEventSegs,
					businessHourSegs: props.businessHourSegs,
					dateSelectionSegs: props.dateSelectionSegs,
					eventDrag: props.eventDrag,
					eventResize: props.eventResize,
					eventSelection: props.eventSelection,
					colWidth: appliedColWidth,
					width: canvasWidth,
					visibleWidth: totalWidth,
					cellIsNarrow,
					cellIsMicro,
					rowHeightRefMap: props.rowHeightRefMap
				})
			}),
			Boolean(footerScrollbarSticky) && u(FooterScrollbar, {
				isSticky: true,
				canvasWidth,
				scrollerRef: this.footerScrollerRef
			}),
			u(Ruler, { widthRef: this.handleTotalWidth })
		] });
	}
	componentDidMount() {
		this._isUnmounting = false;
		const ScrollerSyncer = getScrollerSyncerClass(this.context.pluginHooks);
		this.syncedScroller = new ScrollerSyncer(true);
		setRef(this.props.scrollerRef, this.syncedScroller);
		this.updateSyncedScroller();
	}
	componentDidUpdate() {
		this.updateSyncedScroller();
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.syncedScroller.destroy();
	}
	updateSyncedScroller() {
		this.syncedScroller.handleChildren([
			this.headerScrollerRef.current,
			this.bodyScrollerRef.current,
			this.footerScrollerRef.current
		]);
	}
};
var DayGridLayout = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.scrollerRef = M();
		this.rowHeightRefMap = new RefMap(() => {
			afterSize(this.updateScrollY);
		});
		this.scrollDate = null;
		this.updateScrollY = () => {
			if (this._isUnmounting) return;
			const rowHeightMap = this.rowHeightRefMap.current;
			const scroller = this.scrollerRef.current;
			if (scroller && this.scrollDate) {
				let scrollTop = computeTopFromDate(this.scrollDate, this.props.cellRows, rowHeightMap);
				if (scrollTop != null) {
					if (scrollTop) scrollTop++;
					scroller.scrollTo({ y: scrollTop });
				}
			}
		};
		this.handleScrollEnd = (isDevice) => {
			if (isDevice) this.scrollDate = null;
		};
	}
	render() {
		const { props, context } = this;
		const { options } = context;
		const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
		const dateSelectionSegs = props.forPrint ? [] : props.dateSelectionSegs;
		const eventDrag = props.forPrint ? null : props.eventDrag;
		const eventResize = props.forPrint ? null : props.eventResize;
		const commonLayoutProps = {
			...props,
			dateSelectionSegs,
			eventDrag,
			eventResize,
			scrollerRef: this.scrollerRef,
			rowHeightRefMap: this.rowHeightRefMap
		};
		return u(ViewContainer, {
			viewSpec: context.viewSpec,
			attrs: {
				role: "grid",
				"aria-rowcount": props.headerTiers.length + props.cellRows.length,
				"aria-colcount": props.cellRows[0].length,
				"aria-labelledby": props.labelId,
				"aria-label": props.labelStr
			},
			className: joinClassNames(props.className, !props.forPrint && classNames.flexCol, !props.forPrint && generateClassName(options.tableClass, {
				borderlessX,
				borderlessTop,
				borderlessBottom,
				multiMonthColumns: 0
			})),
			children: options.dayMinWidth ? u(DayGridLayoutPannable, {
				...commonLayoutProps,
				dayMinWidth: options.dayMinWidth
			}) : u(DayGridLayoutNormal, { ...commonLayoutProps })
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		if (!this.props.forPrint) {
			this.resetScroll();
			this.scrollerRef.current?.addScrollEndListener(this.handleScrollEnd);
		}
	}
	componentDidUpdate(prevProps) {
		if (prevProps.forPrint && !this.props.forPrint) {
			this.scrollerRef.current?.addScrollEndListener(this.handleScrollEnd);
			this.resetScroll();
		}
		if (prevProps.dateProfile !== this.props.dateProfile && this.context.options.scrollTimeReset) this.resetScroll();
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.scrollerRef.current?.removeScrollEndListener(this.handleScrollEnd);
	}
	resetScroll() {
		this.scrollDate = this.props.dateProfile.currentDate;
		this.updateScrollY();
		this.scrollerRef.current?.scrollTo({ x: 0 });
	}
};
//#endregion
//#region node_modules/fullcalendar/chunks/3057f12c.js
var TableDateProfileGenerator = class extends DateProfileGenerator {
	buildRenderRange(currentRange, currentRangeUnit, isRangeAllDay) {
		let renderRange = super.buildRenderRange(currentRange, currentRangeUnit, isRangeAllDay);
		let { props } = this;
		return buildDayTableRenderRange({
			currentRange: renderRange,
			snapToWeek: /^(year|month)$/.test(currentRangeUnit),
			fixedWeekCount: props.fixedWeekCount,
			dateEnv: props.dateEnv
		});
	}
};
function buildDayTableRenderRange(props) {
	let { dateEnv, currentRange } = props;
	let { start, end } = currentRange;
	let endOfWeek;
	if (props.snapToWeek) {
		start = dateEnv.startOfWeek(start);
		endOfWeek = dateEnv.startOfWeek(end);
		if (endOfWeek.valueOf() !== end.valueOf()) end = addWeeks(endOfWeek, 1);
	}
	if (props.fixedWeekCount) {
		let lastMonthRenderStart = dateEnv.startOfWeek(dateEnv.startOfMonth(addDays(currentRange.end, -1)));
		let rowCount = Math.ceil(diffWeeks(lastMonthRenderStart, end));
		end = addWeeks(end, 6 - rowCount);
	}
	return {
		start,
		end
	};
}
//#endregion
//#region node_modules/fullcalendar/chunks/d7bcc98b.js
var DayGridView = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.buildDayTableModel = memoize(buildDayTableModel);
		this.buildDateRowConfigs = memoize(buildDateRowConfigs);
		this.createDayHeaderFormatter = memoize(createDayHeaderFormatter);
		this.slicer = new DayTableSlicer();
	}
	render() {
		const { props, context } = this;
		const { dateProfile } = props;
		const { options, dateEnv } = context;
		const dayTableModel = this.buildDayTableModel(dateProfile, context.dateProfileGenerator, dateEnv);
		const datesRepDistinctDays = dayTableModel.rowCount === 1;
		const dayHeaderFormat = this.createDayHeaderFormatter(context.options.dayHeaderFormat, datesRepDistinctDays, dayTableModel.colCount);
		const slicedProps = this.slicer.sliceProps(props, dateProfile, options.nextDayThreshold, context, dayTableModel);
		return u(NowTimer, {
			unit: "day",
			children: (nowDate, todayRange) => {
				const headerTiers = this.buildDateRowConfigs(dayTableModel.headerDates, datesRepDistinctDays, dateProfile, todayRange, dayHeaderFormat, context);
				return u(DayGridLayout, {
					labelId: props.labelId,
					labelStr: props.labelStr,
					dateProfile,
					todayRange,
					cellRows: dayTableModel.cellRows,
					forPrint: props.forPrint,
					className: props.className,
					headerTiers,
					fgEventSegs: slicedProps.fgEventSegs,
					bgEventSegs: slicedProps.bgEventSegs,
					businessHourSegs: slicedProps.businessHourSegs,
					dateSelectionSegs: slicedProps.dateSelectionSegs,
					eventDrag: slicedProps.eventDrag,
					eventResize: slicedProps.eventResize,
					eventSelection: slicedProps.eventSelection
				});
			}
		});
	}
};
var dayGridPlugin = {
	name: "daygrid",
	initialView: "dayGridMonth",
	views: {
		dayGrid: {
			component: DayGridView,
			dateProfileGeneratorClass: TableDateProfileGenerator
		},
		dayGridDay: {
			type: "dayGrid",
			duration: { days: 1 }
		},
		dayGridWeek: {
			type: "dayGrid",
			duration: { weeks: 1 }
		},
		dayGridMonth: {
			type: "dayGrid",
			duration: { months: 1 },
			fixedWeekCount: true
		},
		dayGridYear: {
			type: "dayGrid",
			duration: { years: 1 }
		}
	}
};
//#endregion
//#region node_modules/fullcalendar/chunks/4b6eee98.js
function buildDayColsFromSeries(daySeries, dateEnv, config = {}) {
	const { slotRange, majorUnit = "", activeRange } = config;
	return daySeries.dates.map((date) => ({
		key: date.toISOString(),
		date,
		range: slotRange ? {
			start: dateEnv.add(date, slotRange.slotMinTime),
			end: dateEnv.add(date, slotRange.slotMaxTime)
		} : {
			start: date,
			end: addDays(date, 1)
		},
		isMajor: majorUnit ? isMajorUnit(date, majorUnit, dateEnv) : false,
		isDisabled: activeRange === null || activeRange !== void 0 && !rangeContainsMarker(activeRange, date)
	}));
}
var EMPTY_EVENT_STORE = createEmptyEventStore();
var Splitter = class {
	constructor() {
		this.getKeysForEventDefs = memoize(this._getKeysForEventDefs);
		this.splitDateSelection = memoize(this._splitDateSpan);
		this.splitEventStore = memoize(this._splitEventStore);
		this.splitIndividualUi = memoize(this._splitIndividualUi);
		this.splitEventDrag = memoize(this._splitInteraction);
		this.splitEventResize = memoize(this._splitInteraction);
		this.eventUiBuilders = {};
	}
	splitProps(props) {
		let keyInfos = this.getKeyInfo(props);
		let defKeys = this.getKeysForEventDefs(props.eventStore);
		let dateSelections = this.splitDateSelection(props.dateSelection);
		let individualUi = this.splitIndividualUi(props.eventUiBases, defKeys);
		let eventStores = this.splitEventStore(props.eventStore, defKeys);
		let eventDrags = this.splitEventDrag(props.eventDrag);
		let eventResizes = this.splitEventResize(props.eventResize);
		let splitProps = {};
		this.eventUiBuilders = mapHash(keyInfos, (info, key) => this.eventUiBuilders[key] || memoize(buildEventUiForKey));
		for (let key in keyInfos) {
			let keyInfo = keyInfos[key];
			let eventStore = eventStores[key] || EMPTY_EVENT_STORE;
			let buildEventUi = this.eventUiBuilders[key];
			splitProps[key] = {
				businessHours: keyInfo.businessHours || props.businessHours,
				dateSelection: dateSelections[key] || null,
				eventStore,
				eventUiBases: buildEventUi(props.eventUiBases[""], keyInfo.ui, individualUi[key]),
				eventDrag: eventDrags[key] || null,
				eventResize: eventResizes[key] || null,
				eventSelection: eventStore.instances[props.eventSelection] ? props.eventSelection : ""
			};
		}
		return splitProps;
	}
	_splitDateSpan(dateSpan) {
		let dateSpans = {};
		if (dateSpan) {
			let keys = this.getKeysForDateSpan(dateSpan);
			for (let key of keys) dateSpans[key] = dateSpan;
		}
		return dateSpans;
	}
	_getKeysForEventDefs(eventStore) {
		return mapHash(eventStore.defs, (eventDef) => this.getKeysForEventDef(eventDef));
	}
	_splitEventStore(eventStore, defKeys) {
		let { defs, instances } = eventStore;
		let splitStores = {};
		for (let defId in defs) for (let key of defKeys[defId]) {
			if (!splitStores[key]) splitStores[key] = createEmptyEventStore();
			splitStores[key].defs[defId] = defs[defId];
		}
		for (let instanceId in instances) {
			let instance = instances[instanceId];
			for (let key of defKeys[instance.defId]) if (splitStores[key]) splitStores[key].instances[instanceId] = instance;
		}
		return splitStores;
	}
	_splitIndividualUi(eventUiBases, defKeys) {
		let splitHashes = {};
		for (let defId in eventUiBases) if (defId) for (let key of defKeys[defId]) {
			if (!splitHashes[key]) splitHashes[key] = {};
			splitHashes[key][defId] = eventUiBases[defId];
		}
		return splitHashes;
	}
	_splitInteraction(interaction) {
		let splitStates = {};
		if (interaction) {
			let affectedStores = this._splitEventStore(interaction.affectedEvents, this._getKeysForEventDefs(interaction.affectedEvents));
			let mutatedKeysByDefId = this._getKeysForEventDefs(interaction.mutatedEvents);
			let mutatedStores = this._splitEventStore(interaction.mutatedEvents, mutatedKeysByDefId);
			let populate = (key) => {
				if (!splitStates[key]) splitStates[key] = {
					affectedEvents: affectedStores[key] || EMPTY_EVENT_STORE,
					mutatedEvents: mutatedStores[key] || EMPTY_EVENT_STORE,
					isEvent: interaction.isEvent
				};
			};
			for (let key in affectedStores) populate(key);
			for (let key in mutatedStores) populate(key);
		}
		return splitStates;
	}
};
function buildEventUiForKey(allUi, eventUiForKey, individualUi) {
	let baseParts = [];
	if (allUi) baseParts.push(allUi);
	if (eventUiForKey) baseParts.push(eventUiForKey);
	let stuff = { "": combineEventUis(baseParts) };
	if (individualUi) Object.assign(stuff, individualUi);
	return stuff;
}
var AllDaySplitter = class extends Splitter {
	getKeyInfo() {
		return {
			allDay: {},
			timed: {}
		};
	}
	getKeysForDateSpan(dateSpan) {
		if (dateSpan.allDay) return ["allDay"];
		return ["timed"];
	}
	getKeysForEventDef(eventDef) {
		if (!eventDef.allDay) return ["timed"];
		if (hasBgRendering(eventDef)) return ["timed", "allDay"];
		return ["allDay"];
	}
};
var DayTimeColsSlicer = class extends Slicer {
	sliceRange(range, dayRanges) {
		let segs = [];
		for (let col = 0; col < dayRanges.length; col += 1) {
			let segRange = intersectRanges(range, dayRanges[col]);
			if (segRange) segs.push({
				startDate: segRange.start,
				endDate: segRange.end,
				isStart: segRange.start.valueOf() === range.start.valueOf(),
				isEnd: segRange.end.valueOf() === range.end.valueOf(),
				col
			});
		}
		return segs;
	}
};
function organizeSegsByCol(segs, colCount) {
	let segsByCol = [];
	let i = 0;
	for (; i < colCount; i += 1) segsByCol.push([]);
	if (segs) for (i = 0; i < segs.length; i += 1) segsByCol[segs[i].col].push(segs[i]);
	return segsByCol;
}
function splitInteractionByCol(ui, colCount) {
	let byRow = [];
	if (!ui) for (let i = 0; i < colCount; i += 1) byRow[i] = null;
	else {
		for (let i = 0; i < colCount; i += 1) byRow[i] = {
			affectedInstances: ui.affectedInstances,
			isEvent: ui.isEvent,
			segs: []
		};
		for (let seg of ui.segs) byRow[seg.col].segs.push(seg);
	}
	return byRow;
}
var STOCK_SUB_DURATIONS = [
	{ hours: 1 },
	{ minutes: 30 },
	{ minutes: 15 },
	{ seconds: 30 },
	{ seconds: 15 }
];
function buildSlatMetas(slotMinTime, slotMaxTime, explicitLabelInterval, slotDuration, dateEnv) {
	let dayStart = /* @__PURE__ */ new Date(0);
	let slatTime = slotMinTime;
	let slatIterator = createDuration(0);
	let labelInterval = explicitLabelInterval || computeLabelInterval(slotDuration);
	let metas = [];
	let i = 0;
	while (asRoughMs(slatTime) < asRoughMs(slotMaxTime)) {
		let date = dateEnv.add(dayStart, slatTime);
		let isLabeled = wholeDivideDurations(slatIterator, labelInterval) !== null;
		metas.push({
			date,
			time: slatTime,
			key: date.toISOString(),
			isoTimeStr: formatIsoTimeString(date),
			isLabeled,
			isFirst: i === 0
		});
		slatTime = addDurations(slatTime, slotDuration);
		slatIterator = addDurations(slatIterator, slotDuration);
		i += 1;
	}
	return metas;
}
function computeLabelInterval(slotDuration) {
	let i;
	let labelInterval;
	let slotsPerLabel;
	for (i = STOCK_SUB_DURATIONS.length - 1; i >= 0; i -= 1) {
		labelInterval = createDuration(STOCK_SUB_DURATIONS[i]);
		slotsPerLabel = wholeDivideDurations(labelInterval, slotDuration);
		if (slotsPerLabel !== null && slotsPerLabel > 1) return labelInterval;
	}
	return slotDuration;
}
var TimeGridAllDayHeader = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.innerElRef = M();
	}
	render() {
		let { props } = this;
		let { options, viewApi } = this.context;
		let renderProps = {
			text: options.allDayText,
			view: viewApi,
			isNarrow: props.isNarrow
		};
		return u(ContentContainer, {
			tag: "div",
			attrs: { role: "rowheader" },
			className: joinClassNames(classNames.flexRow, classNames.noMargin, classNames.noPadding, classNames.contentBox),
			style: { width: props.width },
			renderProps,
			generatorName: "allDayHeaderContent",
			customGenerator: options.allDayHeaderContent,
			defaultGenerator: renderAllDayInner,
			classNameGenerator: options.allDayHeaderClass,
			didMount: options.allDayHeaderDidMount,
			willUnmount: options.allDayHeaderWillUnmount,
			children: (InnerContent) => u("div", {
				className: joinClassNames(classNames.flexRow, classNames.noShrink, classNames.whiteSpacePre),
				ref: this.innerElRef,
				children: u(InnerContent, {
					tag: "div",
					className: generateClassName(options.allDayHeaderInnerClass, renderProps)
				})
			})
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		const { props } = this;
		const innerEl = this.innerElRef.current;
		this.disconnectInnerWidth = watchWidth(innerEl, (width) => {
			if (this._isUnmounting) return;
			setRef(props.innerWidthRef, width);
		});
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectInnerWidth();
		setRef(this.props.innerWidthRef, null);
	}
};
function renderAllDayInner(renderProps) {
	return renderProps.text;
}
var TimeGridAllDayLane = class extends DateComponent {
	constructor() {
		super(...arguments);
		this.state = {};
		this.heightRef = M();
		this.handleMoreLinkEl = (el) => {
			this.disconnectMoreLinkHeight?.();
			this.disconnectMoreLinkHeight = void 0;
			if (el) this.disconnectMoreLinkHeight = watchHeight(el, (height) => {
				if (this._isUnmounting) return;
				this.setState({ moreLinkHeight: height });
			});
		};
		this.handleRootEl = (rootEl) => {
			this.rootEl = rootEl;
			if (rootEl) this.context.registerInteractiveComponent(this, { el: rootEl });
			else this.context.unregisterInteractiveComponent(this);
		};
	}
	render() {
		const { props, state } = this;
		const needsMoreLinkProbe = !props.forPrint && resolveDayGridPlacementMode(props.dayMaxEvents, props.dayMaxEventRows) === "auto";
		return u(S, { children: [u(DayGridRow, {
			...props,
			moreLinkHeight: state.moreLinkHeight,
			rootElRef: this.handleRootEl,
			heightRef: this.heightRef
		}), needsMoreLinkProbe && u(MoreLinkTrigger, {
			num: 1,
			display: "row",
			isNarrow: props.cellIsNarrow,
			isMicro: props.cellIsMicro,
			elRef: this.handleMoreLinkEl,
			className: classNames.offscreen,
			attrs: {
				"aria-hidden": true,
				inert: ""
			}
		})] });
	}
	componentDidMount() {
		this._isUnmounting = false;
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.disconnectMoreLinkHeight?.();
	}
	queryHit(isRtl, positionLeft, positionTop, elWidth) {
		const { props, heightRef } = this;
		const colCount = props.cells.length;
		const { col, left, right } = computeColFromPosition(positionLeft, elWidth, props.colWidth, colCount, isRtl);
		const cell = props.cells[col];
		const cellStartDate = cell.date;
		const cellEndDate = addDays(cellStartDate, 1);
		return {
			dateProfile: props.dateProfile,
			dateSpan: {
				range: {
					start: cellStartDate,
					end: cellEndDate
				},
				allDay: true,
				...cell.dateSpanProps
			},
			getDayEl: () => getCellEl(this.rootEl, col),
			rect: {
				left,
				right,
				top: 0,
				bottom: heightRef.current
			},
			layer: 0
		};
	}
};
function computeSlatHeight(expandRows, slatCnt, explicitSlatMinHeight = 0, slatInnerHeight, scrollerHeight) {
	if (!slatInnerHeight || !scrollerHeight) return [void 0, false];
	const slatMinHeight = Math.max(slatInnerHeight + 1, explicitSlatMinHeight);
	const slatLiquidHeight = scrollerHeight / slatCnt;
	let slatLiquid;
	let slatHeight;
	if (expandRows && slatLiquidHeight >= slatMinHeight) {
		slatLiquid = true;
		slatHeight = slatLiquidHeight;
	} else {
		slatLiquid = false;
		slatHeight = slatMinHeight;
	}
	return [slatHeight, slatLiquid];
}
function computeDateTopFrac(date, dateProfile, startOfDayDate) {
	if (!startOfDayDate) startOfDayDate = startOfDay(date);
	return computeTimeTopFrac(createDuration(date.valueOf() - startOfDayDate.valueOf()), dateProfile);
}
function computeTimeTopFrac(time, dateProfile) {
	const startMs = asRoughMs(dateProfile.slotMinTime);
	const endMs = asRoughMs(dateProfile.slotMaxTime);
	let frac = (time.milliseconds - startMs) / (endMs - startMs);
	frac = Math.max(0, frac);
	frac = Math.min(1, frac);
	return frac;
}
/**
* Projects production's date range onto the slat canvas.
*
* This is TimeGrid's production-specific coordinate conversion: it owns hidden
* days, DST, and `eventMinHeight`. The shared engine consumes only the numbers
* it produces.
*
* The canvas height may be assumed rather than measured — callers substitute
* `ESTIMATED_SLAT_HEIGHT` so the first paint contains events — in which case
* they also pass no `eventMinHeight`. A null height still yields no verticals,
* but no production caller passes one.
*/
function computeFgSegVerticals(segs, dateProfile, colDate, slatCnt, slatHeight, eventMinHeight, eventShortHeight) {
	const res = [];
	if (slatHeight != null) {
		const totalHeight = slatHeight * slatCnt;
		for (const seg of segs) {
			const startFrac = computeDateTopFrac(seg.startDate, dateProfile, colDate);
			const endFrac = computeDateTopFrac(seg.endDate, dateProfile, colDate);
			const startCoord = startFrac * totalHeight;
			let endCoord = endFrac * totalHeight;
			let height = endCoord - startCoord;
			if (eventMinHeight != null && height < eventMinHeight) {
				height = eventMinHeight;
				endCoord = startCoord + height;
			}
			res.push({
				start: startCoord,
				end: endCoord,
				size: height,
				isShort: height <= eventShortHeight
			});
		}
	}
	return res;
}
/**
* Adapts production TimeGrid segs to and from the shared placement engine.
*
* The caller owns vertical geometry: `segVerticals` already incorporates
* clipping, and `eventMinHeight` too once the slat height is measured — the
* caller withholds that floor while the height is only assumed. Any missing
* entry is excluded. The engine owns only admission and normalized horizontal
* placement. `slotEventOverlap` remains a final CSS concern, and mirrors
* deliberately stay outside this normal-event admission path.
*/
function buildTimeGridSegPlacements(segs, segVerticals, eventOrderStrict, eventMaxStack) {
	const sourceSegs = [];
	const segVerticalBySeg = /* @__PURE__ */ new Map();
	for (let orderIndex = 0; orderIndex < segs.length; orderIndex += 1) {
		const seg = segs[orderIndex];
		const segVertical = segVerticals[orderIndex];
		if (segVertical) {
			const sourceSeg = {
				...seg,
				key: seg.eventRange.instance.instanceId,
				start: segVertical.start,
				end: segVertical.end,
				orderIndex
			};
			sourceSegs.push(sourceSeg);
			segVerticalBySeg.set(sourceSeg, segVertical);
		}
	}
	const layout = layoutTimeGridColumnByMaxLevel(sourceSegs, eventMaxStack ?? Infinity, { orderStrict: eventOrderStrict ?? false });
	return {
		placements: layout.domOrderedPlacements.map((placement) => {
			const seg = placement.sourceSeg;
			return {
				seg,
				segVertical: segVerticalBySeg.get(seg),
				levelCoord: placement.levelCoord,
				thickness: placement.thickness,
				stackDepth: placement.backwardDepth,
				stackForward: placement.forwardDepth
			};
		}),
		hiddenGroups: layout.moreLinkGroups.map((group) => {
			const groupSegs = group.hiddenSlices.map((slice) => slice.sourceSeg);
			return {
				key: group.key,
				start: group.start,
				end: group.end,
				segs: groupSegs
			};
		})
	};
}
/**
* Builds, limits, and expands one TimeGrid day/resource column.
*
* TimeGrid rotates the visual meaning of the shared kernel's level structure.
* A seg's lateral span runs down the time axis, while logical levels proceed
* across the column. The retained levels become a normalized collision web:
* connected components get equal base columns, and events widen through empty
* deeper columns until their first collider.
*/
function layoutTimeGridColumnByMaxLevel(eventOrderedSegs, maxLevels, options) {
	const { segLevels, excludedSegs } = buildSegLevels(eventOrderedSegs, options.orderStrict, maxLevels);
	const placements = positionTimeGridPlacements(segLevels);
	const moreLinkGroups = groupLaterallyIntersecting(convertSegsToWholeSlices(excludedSegs));
	return {
		domOrderedPlacements: sortByAxisOrder(placements),
		moreLinkGroups
	};
}
/**
* Turns retained dimensionless levels into normalized placement rectangles.
*
* Each connected collision component shares equal-width base columns. An
* event then expands through consecutive deeper columns until the first one
* containing a collider. A single intersection sweep supplies component
* membership, expansion stops, and backward/forward longest-chain depths.
*/
function positionTimeGridPlacements(levels) {
	const placementLevels = levels.map((level, levelIndex) => level.map((sourceSeg) => ({
		sourceSeg,
		start: sourceSeg.start,
		end: sourceSeg.end,
		isStart: sourceSeg.isStart,
		isEnd: sourceSeg.isEnd,
		levelIndex
	})));
	const placements = flatArray(placementLevels);
	const collidersByKey = /* @__PURE__ */ new Map();
	const parentByKey = new Map(placements.map((placement) => [placement.sourceSeg.key, placement.sourceSeg.key]));
	for (const placement of placements) {
		const colliders = [];
		for (let levelIndex = placement.levelIndex + 1; levelIndex < levels.length; levelIndex += 1) colliders.push(...findIntersections(placementLevels[levelIndex], placement));
		collidersByKey.set(placement.sourceSeg.key, colliders);
		for (const collider of colliders) unionPlacementKeys(parentByKey, placement.sourceSeg.key, collider.sourceSeg.key);
	}
	const maxLevelByRoot = /* @__PURE__ */ new Map();
	for (const placement of placements) {
		const root = findPlacementRoot(parentByKey, placement.sourceSeg.key);
		maxLevelByRoot.set(root, Math.max(maxLevelByRoot.get(root) ?? 0, placement.levelIndex));
	}
	const backwardDepthByKey = new Map(placements.map((placement) => [placement.sourceSeg.key, 0]));
	const forwardDepthByKey = new Map(placements.map((placement) => [placement.sourceSeg.key, 0]));
	for (const placement of placements) {
		const depth = backwardDepthByKey.get(placement.sourceSeg.key) + 1;
		for (const collider of collidersByKey.get(placement.sourceSeg.key)) backwardDepthByKey.set(collider.sourceSeg.key, Math.max(backwardDepthByKey.get(collider.sourceSeg.key), depth));
	}
	for (let index = placements.length - 1; index >= 0; index -= 1) {
		const placement = placements[index];
		let depth = 0;
		for (const collider of collidersByKey.get(placement.sourceSeg.key)) depth = Math.max(depth, forwardDepthByKey.get(collider.sourceSeg.key) + 1);
		forwardDepthByKey.set(placement.sourceSeg.key, depth);
	}
	return placements.map((placement) => {
		const key = placement.sourceSeg.key;
		const levelCount = maxLevelByRoot.get(findPlacementRoot(parentByKey, key)) + 1;
		let farLevel = levelCount;
		for (const collider of collidersByKey.get(key)) farLevel = Math.min(farLevel, collider.levelIndex);
		const levelCoord = placement.levelIndex / levelCount;
		const thickness = (farLevel - placement.levelIndex) / levelCount;
		return {
			...placement,
			levelCoord,
			thickness,
			levelEndCoord: levelCoord + thickness,
			backwardDepth: backwardDepthByKey.get(key),
			forwardDepth: forwardDepthByKey.get(key)
		};
	});
}
function findPlacementRoot(parentByKey, key) {
	const parent = parentByKey.get(key);
	if (parent === key) return key;
	const root = findPlacementRoot(parentByKey, parent);
	parentByKey.set(key, root);
	return root;
}
function unionPlacementKeys(parentByKey, first, second) {
	const firstRoot = findPlacementRoot(parentByKey, first);
	const secondRoot = findPlacementRoot(parentByKey, second);
	if (firstRoot !== secondRoot) parentByKey.set(secondRoot, firstRoot);
}
/**
* Slat height assumed on the first render, before the real one is measured.
*
* Without it, `computeFgSegVerticals` has no canvas to project onto and returns
* nothing, so the view paints a frame containing no events at all and the
* calendar looks empty until measurement lands. Projecting against an assumed
* height instead means events are present in that very first paint.
*
* A flat constant is enough, and deliberately so. Placement compares segs
* against each other and never against the axis length, so scaling every
* coordinate by the same factor leaves collisions and level assignment
* untouched. Guessing wrong rescales the raw geometry; it does not reorganize
* it. That is why the caller withholds `eventMinHeight` while assuming — a
* pixel floor applied at the wrong scale would stretch the wrong segs, and
* stretching is what creates collisions.
*
* This is a claim about the raw coordinates only. The measured pass turns
* `eventMinHeight` back on, and stretching a sub-minimum seg there can make it
* collide with a neighbor it previously cleared, changing levels and
* `eventMaxStack` admission. Expect the assumed pass to agree in the common
* case; do not treat it as final.
*
* TimeGrid's caveat list stops there, unlike Timeline's: it places with unit
* thickness and measures no event wrappers, so nothing about the rendered size
* can feed back into placement. `isShort` also differs across passes, but that
* is a styling decision rather than geometry.
*/
var ESTIMATED_SLAT_HEIGHT = 50;
var isBrowserPrintQuirky = typeof navigator !== "undefined" && navigator.userAgent.toLowerCase().includes("firefox");
function computeTimeGridPrintMode(forPrint, eventPrintLayout) {
	if (!forPrint) return "positioned";
	return eventPrintLayout === "stack" || eventPrintLayout !== "grid" && isBrowserPrintQuirky ? "stack" : "positioned";
}
var DEFAULT_TIME_FORMAT = createFormatter({
	hour: "numeric",
	minute: "2-digit",
	meridiem: false
});
var TimeGridEvent = class extends BaseComponent {
	render() {
		const { props } = this;
		return u(StandardEvent, {
			...props,
			display: "column",
			level: props.level,
			isNarrow: props.isNarrow,
			isShort: props.isShort,
			className: props.isLiquid ? classNames.liquid : "",
			disableLiquid: !props.isLiquid,
			defaultTimeFormat: DEFAULT_TIME_FORMAT
		});
	}
};
var TimeGridMoreLink = class extends BaseComponent {
	render() {
		let { props } = this;
		return u("div", {
			className: joinClassNames(classNames.abs, classNames.flexCol, classNames.end0, classNames.z9999),
			style: {
				top: props.top,
				height: props.height
			},
			children: u(MoreLinkContainer, {
				className: classNames.liquid,
				display: "column",
				allDayDate: null,
				segs: props.hiddenSegs,
				hiddenSegs: props.hiddenSegs,
				dateSpanProps: props.dateSpanProps,
				dateProfile: props.dateProfile,
				todayRange: props.todayRange,
				popoverContent: () => renderPlainFgSegs(props.hiddenSegs, props, false),
				forceTimed: true,
				isNarrow: props.isNarrow,
				isMicro: props.isMicro
			})
		});
	}
};
var NowIndicatorDot = (props) => u(ViewContextType.Consumer, { children: (context) => {
	let { options } = context;
	return u("div", {
		className: joinClassNames(props.className, options.nowIndicatorDotClass),
		style: props.style
	});
} });
var NowIndicatorLineContainer = (props) => u(ViewContextType.Consumer, { children: (context) => {
	let { options } = context;
	let renderProps = {
		date: context.dateEnv.toDate(props.date),
		view: context.viewApi
	};
	return u(ContentContainer, {
		elRef: props.elRef,
		tag: props.tag || "div",
		attrs: props.attrs,
		className: props.className,
		style: props.style,
		renderProps,
		generatorName: "nowIndicatorLineContent",
		customGenerator: options.nowIndicatorLineContent,
		classNameGenerator: options.nowIndicatorLineClass,
		didMount: options.nowIndicatorLineDidMount,
		willUnmount: options.nowIndicatorLineWillUnmount,
		children: props.children
	});
} });
function TimeGridNowIndicatorLine(props) {
	const top = props.totalHeight != null ? props.totalHeight * computeDateTopFrac(props.nowDate, props.dateProfile, props.dayDate) : void 0;
	return u("div", {
		className: joinClassNames(classNames.fill, classNames.pointerEventsNone, classNames.z2),
		children: [u(NowIndicatorLineContainer, {
			className: joinClassNames(classNames.fillX, classNames.noMarginX, classNames.borderlessX),
			style: { top },
			date: props.nowDate
		}), (props.showDot ?? true) && u(NowIndicatorDot, {
			className: joinClassNames(classNames.abs, classNames.start0),
			style: { top }
		})]
	});
}
var TimeGridCol = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.sortEventSegs = memoize(sortEventSegs);
		this.getDateMeta = memoize(getDateMeta);
	}
	render() {
		let { props, context } = this;
		let { options, dateEnv } = context;
		let isSelectMirror = options.selectMirror;
		let mirrorSegs = props.eventDrag && props.eventDrag.segs || props.eventResize && props.eventResize.segs || isSelectMirror && props.dateSelectionSegs || [];
		let dateMeta = this.getDateMeta(props.date, dateEnv, props.dateProfile, props.todayRange);
		const baseClassName = joinClassNames(classNames.borderlessY, classNames.borderlessEnd, !props.borderStart && classNames.borderlessStart, props.width == null && classNames.liquid, classNames.rel, classNames.z1);
		const baseStyle = { width: props.width };
		const isStack = this.getIsStack();
		const renderProps = {
			...dateMeta,
			...props.renderProps,
			isStack,
			isNarrow: props.isNarrow,
			isMajor: props.isMajor,
			view: context.viewApi
		};
		if (dateMeta.isDisabled) return u("div", {
			role: "gridcell",
			"aria-disabled": true,
			className: joinClassNames(generateClassName(options.dayLaneClass, renderProps), baseClassName),
			style: baseStyle
		});
		const innerClassName = joinClassNames(generateClassName(options.dayLaneInnerClass, renderProps), !isStack && classNames.fill, classNames.z1);
		const sortedFgSegs = this.sortEventSegs(props.fgEventSegs, options.eventOrder);
		return u(ContentContainer, {
			tag: "div",
			attrs: {
				...props.attrs,
				role: "gridcell",
				...dateMeta.isToday ? { "aria-current": "date" } : {},
				"data-date": formatDayString(props.date)
			},
			className: baseClassName,
			style: baseStyle,
			renderProps,
			generatorName: void 0,
			classNameGenerator: options.dayLaneClass,
			didMount: options.dayLaneDidMount,
			willUnmount: options.dayLaneWillUnmount,
			children: () => u(S, { children: [
				this.renderFillSegs(props.businessHourSegs, "non-business"),
				this.renderFillSegs(props.bgEventSegs, "bg-event"),
				this.renderFillSegs(props.dateSelectionSegs, "highlight"),
				u("div", {
					className: innerClassName,
					children: this.renderFgSegs(sortedFgSegs, false)
				}),
				Boolean(mirrorSegs.length) && u("div", {
					className: innerClassName,
					children: this.renderFgSegs(mirrorSegs, true)
				}),
				this.renderNowIndicator(props.nowIndicatorSegs)
			] })
		});
	}
	renderFgSegs(sortedFgSegs, isMirror) {
		const { props } = this;
		if (this.getIsStack()) return renderPlainFgSegs(sortedFgSegs, props, isMirror);
		if (isMirror) return this.renderPositionedMirrorSegs(sortedFgSegs);
		return this.renderPositionedFgSegs(sortedFgSegs);
	}
	renderPositionedFgSegs(segs) {
		let { eventMaxStack, eventOrderStrict } = this.context.options;
		let { placements, hiddenGroups } = buildTimeGridSegPlacements(segs, this.computeSegVerticals(segs), eventOrderStrict, eventMaxStack);
		return u(S, { children: [placements.map((placement) => this.renderPositionedSeg(placement.seg, placement.segVertical, this.computeSegHStyle(placement), placement.stackDepth, false)), this.renderHiddenGroups(hiddenGroups)] });
	}
	renderPositionedMirrorSegs(segs) {
		let segVerticals = this.computeSegVerticals(segs);
		return segs.map((seg, index) => this.renderPositionedSeg(seg, segVerticals[index] || {}, {
			left: 0,
			right: 0,
			zIndex: 0
		}, 0, true));
	}
	renderPositionedSeg(seg, segVertical, hStyle, level, isMirror) {
		let { props } = this;
		let { eventRange } = seg;
		let { instanceId } = eventRange.instance;
		let isSelected = instanceId === props.eventSelection;
		if (isSelected) hStyle.zIndex += 1e3;
		let isDragging = Boolean(props.eventDrag && props.eventDrag.affectedInstances[instanceId]);
		let isResizing = Boolean(props.eventResize && props.eventResize.affectedInstances[instanceId]);
		let isInvisible = !isMirror && (isDragging || isResizing);
		return u("div", {
			className: joinClassNames(classNames.abs, classNames.flexCol),
			style: {
				visibility: isInvisible ? "hidden" : void 0,
				top: segVertical.start,
				height: segVertical.size,
				...hStyle
			},
			children: u(TimeGridEvent, {
				eventRange,
				slicedStart: seg.startDate,
				slicedEnd: seg.endDate,
				isStart: seg.isStart,
				isEnd: seg.isEnd,
				isDragging,
				isResizing,
				isMirror,
				isSelected,
				level,
				isNarrow: props.isNarrow,
				isShort: segVertical.isShort || false,
				isLiquid: true,
				...getEventRangeMeta(eventRange, props.todayRange, props.nowDate, props.nowMs)
			})
		}, instanceId);
	}
	computeSegVerticals(segs) {
		let { props, context } = this;
		let isMeasured = props.slatHeight != null;
		return computeFgSegVerticals(segs, props.dateProfile, props.date, props.slatCnt, props.slatHeight ?? ESTIMATED_SLAT_HEIGHT, isMeasured ? context.options.eventMinHeight : void 0, context.options.eventShortHeight);
	}
	renderHiddenGroups(hiddenGroups) {
		let { dateSpanProps, dateProfile, todayRange, nowDate, nowMs, eventSelection, eventDrag, eventResize, isNarrow, isMicro } = this.props;
		return u(S, { children: hiddenGroups.map((hiddenGroup) => {
			return u(TimeGridMoreLink, {
				hiddenSegs: hiddenGroup.segs,
				top: hiddenGroup.start,
				height: hiddenGroup.end - hiddenGroup.start,
				isNarrow,
				isMicro,
				dateSpanProps,
				dateProfile,
				todayRange,
				nowDate,
				nowMs,
				eventSelection,
				eventDrag,
				eventResize
			}, hiddenGroup.key);
		}) });
	}
	renderFillSegs(segs, fillType) {
		let { props, context } = this;
		let segVerticals = this.computeSegVerticals(segs);
		return u(S, { children: segs.map((seg, index) => {
			const { eventRange } = seg;
			const segVertical = segVerticals[index] || {};
			return u("div", {
				className: classNames.fillX,
				style: {
					top: segVertical.start,
					height: segVertical.size,
					marginInlineStart: -1
				},
				children: fillType === "bg-event" ? u(BgEvent, {
					eventRange,
					isStart: seg.isStart,
					isEnd: seg.isEnd,
					isNarrow: props.isNarrow,
					isShort: segVertical.isShort || false,
					isVertical: true,
					...getEventRangeMeta(eventRange, props.todayRange, props.nowDate, props.nowMs)
				}) : renderFill(fillType, context.options)
			}, buildEventRangeKey(eventRange));
		}) });
	}
	renderNowIndicator(segs) {
		let { props } = this;
		if (props.forPrint || this.getIsStack()) return;
		return segs.map((seg, i) => u(TimeGridNowIndicatorLine, {
			nowDate: seg.startDate,
			dayDate: props.date,
			dateProfile: props.dateProfile,
			totalHeight: props.slatHeight != null ? props.slatHeight * props.slatCnt : void 0,
			showDot: seg.showDot ?? true
		}, i));
	}
	computeSegHStyle(segRect) {
		let { options } = this.context;
		let shouldOverlap = options.slotEventOverlap;
		let nearCoord = segRect.levelCoord;
		let farCoord = segRect.levelCoord + segRect.thickness;
		if (shouldOverlap) farCoord = Math.min(1, nearCoord + (farCoord - nearCoord) * 2);
		let props = {
			zIndex: segRect.stackDepth + 1,
			insetInlineStart: fracToCssDim(nearCoord),
			insetInlineEnd: fracToCssDim(1 - farCoord),
			marginInlineEnd: void 0
		};
		if (shouldOverlap && segRect.stackForward) props.marginInlineEnd = 20;
		return props;
	}
	getIsStack() {
		const { eventPrintLayout } = this.context.options;
		return computeTimeGridPrintMode(this.props.forPrint, eventPrintLayout) === "stack";
	}
};
function renderPlainFgSegs(sortedFgSegs, { todayRange, nowDate, nowMs, eventSelection, eventDrag, eventResize }, isMirror) {
	return u(S, { children: sortedFgSegs.map((seg) => {
		let { eventRange } = seg;
		let { instanceId } = eventRange.instance;
		let isDragging = Boolean(eventDrag && eventDrag.affectedInstances[instanceId]);
		let isResizing = Boolean(eventResize && eventResize.affectedInstances[instanceId]);
		let isInvisible = isDragging || isResizing;
		return u("div", {
			className: classNames.breakInsideAvoid,
			style: { visibility: isInvisible ? "hidden" : void 0 },
			children: u(TimeGridEvent, {
				eventRange,
				slicedStart: seg.startDate,
				slicedEnd: seg.endDate,
				isStart: seg.isStart,
				isEnd: seg.isEnd,
				isDragging,
				isResizing,
				isMirror,
				isSelected: instanceId === eventSelection,
				level: 0,
				isShort: false,
				isNarrow: false,
				disableResizing: true,
				...getEventRangeMeta(eventRange, todayRange, nowDate, nowMs)
			})
		}, instanceId);
	}) });
}
var TimeGridCols = class extends DateComponent {
	constructor() {
		super(...arguments);
		this.processSlotOptions = memoize(processSlotOptions);
		this.handleRootEl = (el) => {
			this.rootEl = el;
			if (el) this.context.registerInteractiveComponent(this, {
				el,
				isHitComboAllowed: this.props.isHitComboAllowed
			});
			else this.context.unregisterInteractiveComponent(this);
		};
	}
	render() {
		const { props } = this;
		return u("div", {
			role: props.role,
			className: joinClassNames(props.className, classNames.flexRow),
			ref: this.handleRootEl,
			children: props.cells.map((cell, col) => u(TimeGridCol, {
				dateProfile: props.dateProfile,
				nowDate: props.nowDate,
				nowMs: props.nowMs,
				todayRange: props.todayRange,
				date: cell.date,
				isMajor: cell.isMajor,
				slatCnt: props.slatCnt,
				renderProps: cell.renderProps,
				attrs: cell.attrs,
				dateSpanProps: cell.dateSpanProps,
				forPrint: props.forPrint,
				borderStart: Boolean(col),
				isNarrow: props.cellIsNarrow,
				isMicro: props.cellIsMicro,
				fgEventSegs: props.fgEventSegsByCol[col],
				bgEventSegs: props.bgEventSegsByCol[col],
				businessHourSegs: props.businessHourSegsByCol[col],
				nowIndicatorSegs: props.nowIndicatorSegsByCol[col],
				dateSelectionSegs: props.dateSelectionSegsByCol[col],
				eventDrag: props.eventDragByCol[col],
				eventResize: props.eventResizeByCol[col],
				eventSelection: props.eventSelection,
				width: props.colWidth,
				slatHeight: props.slatHeight
			}, cell.key))
		});
	}
	queryHit(isRtl, positionLeft, positionTop, elWidth) {
		const { dateProfile, cells, colWidth, slatHeight } = this.props;
		const { dateEnv, options } = this.context;
		const { snapDuration, snapsPerSlot } = this.processSlotOptions(options.slotDuration, options.snapDuration);
		const colCount = cells.length;
		const { col, left, right } = computeColFromPosition(positionLeft, elWidth, colWidth, colCount, isRtl);
		const cell = cells[col];
		const slatIndex = Math.floor(positionTop / slatHeight);
		const slatTop = slatIndex * slatHeight;
		const partial = (positionTop - slatTop) / slatHeight;
		const localSnapIndex = Math.floor(partial * snapsPerSlot);
		const snapIndex = slatIndex * snapsPerSlot + localSnapIndex;
		const time = addDurations(dateProfile.slotMinTime, multiplyDuration(snapDuration, snapIndex));
		const start = dateEnv.add(cell.date, time);
		return {
			dateProfile,
			dateSpan: {
				range: {
					start,
					end: dateEnv.add(start, snapDuration)
				},
				allDay: false,
				...cell.dateSpanProps
			},
			getDayEl: () => getCellEl(this.rootEl, col),
			rect: {
				left,
				right,
				top: slatTop,
				bottom: slatTop + slatHeight
			},
			layer: 0
		};
	}
};
TimeGridCols.addPropsEquality({ style: isPropsEqualShallow });
function processSlotOptions(slotDuration, snapDurationOverride) {
	let snapDuration = snapDurationOverride || slotDuration;
	let snapsPerSlot = wholeDivideDurations(slotDuration, snapDuration);
	if (snapsPerSlot === null) {
		snapDuration = slotDuration;
		snapsPerSlot = 1;
	}
	return {
		snapDuration,
		snapsPerSlot
	};
}
var NowIndicatorHeaderContainer = (props) => u(ViewContextType.Consumer, { children: (context) => {
	let { options } = context;
	let renderProps = {
		date: context.dateEnv.toDate(props.date),
		view: context.viewApi
	};
	return u(ContentContainer, {
		elRef: props.elRef,
		tag: props.tag || "div",
		attrs: props.attrs,
		className: props.className,
		style: props.style,
		renderProps,
		generatorName: "nowIndicatorHeaderContent",
		customGenerator: options.nowIndicatorHeaderContent,
		classNameGenerator: options.nowIndicatorHeaderClass,
		didMount: options.nowIndicatorHeaderDidMount,
		willUnmount: options.nowIndicatorHeaderWillUnmount,
		children: props.children
	});
} });
function TimeGridNowIndicatorArrow(props) {
	return u("div", {
		className: joinClassNames(classNames.fill, classNames.crop, classNames.pointerEventsNone, classNames.z2),
		children: u(NowIndicatorHeaderContainer, {
			className: classNames.abs,
			style: { top: props.totalHeight != null ? props.totalHeight * computeDateTopFrac(props.nowDate, props.dateProfile) : void 0 },
			date: props.nowDate
		})
	});
}
var DEFAULT_SLAT_LABEL_FORMAT = createFormatter({
	hour: "numeric",
	minute: "2-digit",
	omitZeroMinute: true,
	meridiem: "short"
});
var TimeGridSlatHeader = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.createRenderProps = memoize(createRenderProps);
		this.innerElRef = M();
	}
	render() {
		let { props, context } = this;
		let { options } = context;
		let headerFormat = options.slotHeaderFormat == null ? DEFAULT_SLAT_LABEL_FORMAT : Array.isArray(options.slotHeaderFormat) ? createFormatter(options.slotHeaderFormat[0]) : createFormatter(options.slotHeaderFormat);
		let renderProps = this.createRenderProps(props.date, props.time, !props.isLabeled, props.isNarrow, props.isFirst, headerFormat, context);
		let className = joinClassNames(props.liquidHeight && classNames.liquid, classNames.flexRow, classNames.alignStart, classNames.noMargin, classNames.noPadding, classNames.borderlessX, classNames.borderlessBottom, !props.borderTop && classNames.borderlessTop);
		if (!props.isLabeled) return u("div", {
			className: joinClassNames(generateClassName(options.slotHeaderClass, renderProps), className),
			style: { height: props.height }
		});
		return u(ContentContainer, {
			tag: "div",
			attrs: { "data-time": props.isoTimeStr },
			style: { height: props.height },
			className,
			renderProps,
			generatorName: "slotHeaderContent",
			customGenerator: options.slotHeaderContent,
			defaultGenerator: renderInnerContent,
			classNameGenerator: options.slotHeaderClass,
			didMount: options.slotHeaderDidMount,
			willUnmount: options.slotHeaderWillUnmount,
			children: (InnerContent) => u("div", {
				ref: this.innerElRef,
				className: joinClassNames(classNames.noShrink, classNames.whiteSpaceNoWrap, classNames.flexRow),
				children: u(InnerContent, {
					tag: "div",
					className: generateClassName(options.slotHeaderInnerClass, renderProps)
				})
			})
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		const { props } = this;
		const innerEl = this.innerElRef.current;
		if (innerEl) this.disconnectInnerSize = watchSize(innerEl, (width, height) => {
			if (this._isUnmounting) return;
			setRef(props.innerWidthRef, width);
			setRef(props.innerHeightRef, height);
		});
	}
	componentWillUnmount() {
		const { props } = this;
		this._isUnmounting = true;
		if (this.disconnectInnerSize) {
			this.disconnectInnerSize();
			setRef(props.innerWidthRef, null);
			setRef(props.innerHeightRef, null);
		}
	}
};
function createRenderProps(date, time, isMinor, isNarrow, isFirst, headerFormat, context) {
	return {
		...getDateMeta(date, context.dateEnv),
		level: 0,
		text: joinDateTimeFormatParts(context.dateEnv.formatToParts(date, headerFormat)),
		time,
		isMajor: false,
		isMinor,
		isTime: true,
		isNarrow,
		hasNavLink: false,
		isFirst,
		view: context.viewApi
	};
}
function renderInnerContent(props) {
	return props.text;
}
var TimeGridSlatLane = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.getDateMeta = memoize(getDateMeta);
	}
	render() {
		let { props, context } = this;
		let { options } = context;
		let renderProps = {
			...this.getDateMeta(props.date, context.dateEnv),
			time: props.time,
			isMajor: false,
			isMinor: !props.isLabeled,
			view: context.viewApi
		};
		return u(ContentContainer, {
			tag: "div",
			attrs: { "data-time": props.isoTimeStr },
			className: joinClassNames(classNames.noMargin, classNames.noPadding, classNames.liquid, classNames.borderlessX, classNames.borderlessBottom, !props.borderTop && classNames.borderlessTop),
			renderProps,
			generatorName: void 0,
			classNameGenerator: options.slotLaneClass,
			didMount: options.slotLaneDidMount,
			willUnmount: options.slotLaneWillUnmount
		});
	}
};
var DEFAULT_WEEK_NUM_FORMAT = createFormatter({ week: "short" });
var TimeGridWeekNumber = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.innerElRef = M();
	}
	render() {
		let { props, context } = this;
		let { options, dateEnv } = context;
		let range = props.dateProfile.renderRange;
		let hasNavLink = diffDays(range.start, range.end) === 1 && options.navLinks;
		let weekDateMarker = range.start;
		let fullDateStr = buildDateStr(context, weekDateMarker, "week");
		let weekNum = dateEnv.computeWeekNumber(weekDateMarker);
		let weekTextParts = dateEnv.formatToParts(weekDateMarker, options.weekNumberFormat || DEFAULT_WEEK_NUM_FORMAT);
		const weekNumberRenderProps = {
			num: weekNum,
			text: joinDateTimeFormatParts(weekTextParts),
			textParts: weekTextParts,
			date: dateEnv.toDate(weekDateMarker),
			isNarrow: props.isNarrow,
			hasNavLink,
			options: { dayMinWidth: options.dayMinWidth }
		};
		return u(ContentContainer, {
			tag: "div",
			attrs: {
				role: "gridcell",
				"aria-label": fullDateStr
			},
			className: joinClassNames(classNames.flexRow, classNames.noMargin, classNames.noPadding, props.isLiquid ? classNames.liquid : classNames.contentBox),
			style: { width: props.width },
			renderProps: weekNumberRenderProps,
			generatorName: "weekNumberHeaderContent",
			customGenerator: options.weekNumberHeaderContent,
			defaultGenerator: renderText$1,
			classNameGenerator: options.weekNumberHeaderClass,
			didMount: options.weekNumberHeaderDidMount,
			willUnmount: options.weekNumberHeaderWillUnmount,
			children: (InnerContent) => u("div", {
				ref: this.innerElRef,
				className: joinClassNames(classNames.flexRow, classNames.noShrink, classNames.whiteSpaceNoWrap),
				children: u(InnerContent, {
					tag: "div",
					attrs: hasNavLink ? buildNavLinkAttrs(context, range.start, "week", fullDateStr) : { "aria-label": fullDateStr },
					className: generateClassName(options.weekNumberHeaderInnerClass, weekNumberRenderProps)
				})
			})
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		const { props } = this;
		const innerEl = this.innerElRef.current;
		this.disconnectInnerSize = watchSize(innerEl, (width, height) => {
			if (this._isUnmounting) return;
			setRef(props.innerWidthRef, width);
			setRef(props.innerHeightRef, height);
		});
	}
	componentWillUnmount() {
		const { props } = this;
		this._isUnmounting = true;
		this.disconnectInnerSize();
		setRef(props.innerWidthRef, null);
		setRef(props.innerHeightRef, null);
	}
};
function TimeGridAxisEmpty(props) {
	return u("div", {
		role: "gridcell",
		className: props.isLiquid ? classNames.liquid : classNames.contentBox,
		style: { width: props.width }
	});
}
var TimeGridLayoutPannable = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.state = { headerTierHeights: [] };
		this.headerLabelInnerWidthRefMap = new RefMap(() => {
			afterSize(this.handleAxisWidths);
		});
		this.headerLabelInnerHeightRefMap = new RefMap(() => {
			afterSize(this.handleHeaderHeights);
		});
		this.headerMainInnerHeightRefMap = new RefMap(() => {
			afterSize(this.handleHeaderHeights);
		});
		this.handleAllDayLabelInnerWidth = (width) => {
			this.allDayLabelInnerWidth = width;
			afterSize(this.handleAxisWidths);
		};
		this.slatLabelInnerWidthRefMap = new RefMap(() => {
			afterSize(this.handleAxisWidths);
		});
		this.slatLabelInnerHeightRefMap = new RefMap(() => {
			afterSize(this.handleSlatInnerHeights);
		});
		this.headerScrollerRef = M();
		this.allDayScrollerRef = M();
		this.mainScrollerRef = M();
		this.footScrollerRef = M();
		this.axisScrollerRef = M();
		this.handleTotalWidth = (totalWidth) => {
			if (this._isUnmounting) return;
			this.setState({ totalWidth });
		};
		this.handleBodyHeight = (bodyHeight) => {
			if (this._isUnmounting) return;
			this.setState({ bodyHeight });
		};
		this.handleClientWidth = (clientWidth) => {
			if (this._isUnmounting) return;
			this.setState({ clientWidth });
		};
		this.handleClientHeight = (clientHeight) => {
			if (this._isUnmounting) return;
			this.setState({ clientHeight });
		};
		this.handleStickyBottomScrollbarWidth = (sticykBottomScrollbarWidth) => {
			if (this._isUnmounting) return;
			this.setState({ sticykBottomScrollbarWidth });
		};
		this.handleHeaderHeights = () => {
			if (this._isUnmounting) return;
			const headerLabelInnerHeightMap = this.headerLabelInnerHeightRefMap.current;
			const headerMainInnerHeightMap = this.headerMainInnerHeightRefMap.current;
			const heights = [];
			for (const [tierNum, mainHeight] of headerMainInnerHeightMap.entries()) heights[tierNum] = Math.max(headerLabelInnerHeightMap.get(tierNum) || 0, mainHeight);
			this.setState({ headerTierHeights: heights });
		};
		this.handleSlatInnerHeights = () => {
			if (this._isUnmounting) return;
			const slatLabelInnerHeightMap = this.slatLabelInnerHeightRefMap.current;
			let max = 0;
			for (const slatLabelInnerHeight of slatLabelInnerHeightMap.values()) max = Math.max(max, slatLabelInnerHeight);
			if (this.state.slatInnerHeight !== max) this.setState({ slatInnerHeight: max });
		};
		this.handleAxisWidths = () => {
			if (this._isUnmounting) return;
			const headerLabelInnerWidthMap = this.headerLabelInnerWidthRefMap.current;
			const slatLabelInnerWidthMap = this.slatLabelInnerWidthRefMap.current;
			let max = this.allDayLabelInnerWidth || 0;
			for (const headerLabelInnerWidth of headerLabelInnerWidthMap.values()) max = Math.max(max, headerLabelInnerWidth);
			for (const slatLableInnerWidth of slatLabelInnerWidthMap.values()) max = Math.max(max, slatLableInnerWidth);
			if (this.state.axisWidth !== max) this.setState({ axisWidth: max });
		};
	}
	render() {
		const { props, state, context, headerLabelInnerWidthRefMap, headerLabelInnerHeightRefMap, headerMainInnerHeightRefMap, slatLabelInnerWidthRefMap, slatLabelInnerHeightRefMap } = this;
		const { nowDate, headerTiers, forPrint } = props;
		const nowTimeMs = nowDate.valueOf() - startOfDay(nowDate).valueOf();
		const { axisWidth, totalWidth, clientWidth, clientHeight, bodyHeight, sticykBottomScrollbarWidth } = state;
		const { options } = context;
		const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
		const endScrollbarWidth = totalWidth != null && clientWidth != null && axisWidth != null ? totalWidth - clientWidth - (axisWidth + 1) : void 0;
		const verticalScrolling = !forPrint && !getIsHeightAuto(options);
		const tableHeaderSticky = !forPrint && getTableHeaderSticky(options);
		const footerScrollbarSticky = !forPrint && getFooterScrollbarSticky(options);
		const printStackEnabled = computeTimeGridPrintMode(forPrint, options.eventPrintLayout) === "stack";
		const absPrint = forPrint && !printStackEnabled;
		const simplePrint = forPrint && printStackEnabled;
		const colCount = props.cells.length;
		const [canvasWidth, appliedColWidth] = computeColWidth(colCount, props.dayMinWidth, clientWidth);
		const measuredColWidth = appliedColWidth ?? (clientWidth != null ? clientWidth / colCount : void 0);
		const cellIsMicro = measuredColWidth != null && measuredColWidth <= 60;
		const cellIsNarrow = cellIsMicro || measuredColWidth != null && measuredColWidth <= options.dayNarrowWidth;
		const slatCnt = props.slatMetas.length;
		const [slatHeight, slatLiquidHeight] = computeSlatHeight(verticalScrolling && options.expandRows, slatCnt, options.slotMinHeight, state.slatInnerHeight, clientHeight);
		this.slatHeight = slatHeight;
		const totalSlatHeight = (slatHeight || 0) * slatCnt;
		const forcedBodyHeight = absPrint ? totalSlatHeight : void 0;
		const rowsNotExpanding = verticalScrolling && !options.expandRows && clientHeight != null && clientHeight > totalSlatHeight;
		const firstBodyRowIndex = options.dayHeaders ? headerTiers.length + 1 : 1;
		const bottomScrollbarWidth = footerScrollbarSticky ? sticykBottomScrollbarWidth : bodyHeight != null && clientHeight != null ? bodyHeight - clientHeight : void 0;
		return u(S, { children: [
			options.dayHeaders && u("div", {
				className: joinClassNames(generateClassName(options.tableHeaderClass, {
					isSticky: tableHeaderSticky,
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), classNames.flexCol, tableHeaderSticky && classNames.tableHeaderSticky, classNames.z1),
				children: [u("div", {
					className: classNames.flexRow,
					children: [
						u("div", {
							role: "rowgroup",
							className: classNames.contentBox,
							style: { width: axisWidth },
							children: headerTiers.map((rowConfig, tierNum) => u("div", {
								role: "row",
								"aria-rowindex": tierNum + 1,
								className: joinClassNames(options.dayHeaderRowClass, classNames.flexRow, classNames.contentBox, classNames.borderlessX, classNames.borderlessTop, tierNum === props.headerTiers.length - 1 && classNames.borderlessBottom),
								style: { height: state.headerTierHeights[tierNum] },
								children: options.weekNumbers && rowConfig.isDateRow ? u(TimeGridWeekNumber, {
									dateProfile: props.dateProfile,
									innerWidthRef: headerLabelInnerWidthRefMap.createRef(tierNum),
									innerHeightRef: headerLabelInnerHeightRefMap.createRef(tierNum),
									width: void 0,
									isLiquid: true,
									isNarrow: cellIsNarrow
								}) : u(TimeGridAxisEmpty, {
									width: void 0,
									isLiquid: true
								})
							}, tierNum))
						}),
						u("div", { className: generateClassName(options.slotHeaderDividerClass, {
							inTableHeader: true,
							options: { dayMinWidth: options.dayMinWidth }
						}) }),
						u(Scroller, {
							horizontal: true,
							hideScrollbars: true,
							className: joinClassNames(classNames.flexRow, classNames.liquid),
							ref: this.headerScrollerRef,
							children: [u("div", {
								role: "rowgroup",
								className: canvasWidth == null ? classNames.liquid : "",
								style: { width: canvasWidth },
								children: props.headerTiers.map((rowConfig, tierNum) => k(DayGridHeaderRow, {
									...rowConfig,
									key: tierNum,
									role: "row",
									rowIndex: tierNum,
									borderBottom: tierNum < props.headerTiers.length - 1,
									height: state.headerTierHeights[tierNum],
									colWidth: appliedColWidth,
									viewportWidth: clientWidth,
									innerHeightRef: headerMainInnerHeightRefMap.createRef(tierNum),
									cellIsNarrow,
									cellIsMicro,
									rowLevel: props.headerTiers.length - tierNum - 1
								}))
							}), Boolean(endScrollbarWidth) && u("div", {
								className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: true }), classNames.borderlessY, classNames.borderlessEnd),
								style: { minWidth: endScrollbarWidth }
							})]
						})
					]
				}), u("div", { className: generateClassName(options.dayHeaderDividerClass, {
					isSticky: tableHeaderSticky,
					multiMonthColumns: 0,
					options: { allDaySlot: Boolean(options.allDaySlot) }
				}) })]
			}),
			u("div", {
				role: "rowgroup",
				className: joinClassNames(generateClassName(options.tableBodyClass, {
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), classNames.flexCol, verticalScrolling && classNames.liquid, classNames.isolate, classNames.z0),
				children: [options.allDaySlot && u(S, { children: [u("div", {
					role: "row",
					"aria-rowindex": firstBodyRowIndex,
					className: joinClassNames(classNames.flexRow, classNames.z1),
					children: [
						u(TimeGridAllDayHeader, {
							width: axisWidth,
							innerWidthRef: this.handleAllDayLabelInnerWidth,
							isNarrow: cellIsNarrow
						}),
						u("div", { className: generateClassName(options.slotHeaderDividerClass, {
							inTableHeader: false,
							options: { dayMinWidth: options.dayMinWidth }
						}) }),
						u(Scroller, {
							horizontal: true,
							hideScrollbars: true,
							className: joinClassNames(classNames.flexRow, classNames.liquidX),
							ref: this.allDayScrollerRef,
							children: [u("div", {
								className: classNames.flexRow,
								style: { width: canvasWidth },
								children: u(TimeGridAllDayLane, {
									dateProfile: props.dateProfile,
									todayRange: props.todayRange,
									cells: props.cells,
									showDayNumbers: false,
									forPrint,
									isHitComboAllowed: props.isHitComboAllowed,
									className: joinClassNames(classNames.borderless, classNames.liquidX),
									cellIsNarrow,
									cellIsMicro,
									fgEventSegs: props.fgEventSegs,
									bgEventSegs: props.bgEventSegs,
									businessHourSegs: props.businessHourSegs,
									dateSelectionSegs: props.dateSelectionSegs,
									eventSelection: props.eventSelection,
									eventDrag: props.eventDrag,
									eventResize: props.eventResize,
									dayMaxEvents: props.dayMaxEvents,
									dayMaxEventRows: props.dayMaxEventRows,
									colWidth: appliedColWidth
								})
							}), Boolean(endScrollbarWidth) && u("div", {
								className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: false }), classNames.borderlessY, classNames.borderlessEnd),
								style: { minWidth: endScrollbarWidth }
							})]
						})
					]
				}), u("div", { className: joinClassNames(options.allDayDividerClass, classNames.z2) })] }), u("div", {
					role: "row",
					"aria-rowindex": firstBodyRowIndex + (options.allDaySlot ? 1 : 0),
					className: joinClassNames(classNames.flexRow, classNames.rel, verticalScrolling && classNames.liquid, classNames.z0),
					children: [
						u(Scroller, {
							vertical: verticalScrolling,
							hideScrollbars: true,
							className: joinClassNames(classNames.flexCol, classNames.contentBox),
							style: { width: axisWidth },
							ref: this.axisScrollerRef,
							clientHeightRef: this.handleBodyHeight,
							children: !simplePrint && u(S, { children: u("div", {
								role: "rowheader",
								"aria-label": options.timedText,
								className: joinClassNames(classNames.flexCol, classNames.grow, classNames.rel),
								style: { height: forcedBodyHeight },
								children: [
									u("div", {
										"aria-hidden": true,
										className: joinClassNames(classNames.flexCol, verticalScrolling && options.expandRows && classNames.grow, absPrint && classNames.fillX),
										children: props.slatMetas.map((slatMeta, slatI) => k(TimeGridSlatHeader, {
											...slatMeta,
											key: slatMeta.key,
											innerWidthRef: slatLabelInnerWidthRefMap.createRef(slatMeta.key),
											innerHeightRef: slatLabelInnerHeightRefMap.createRef(slatMeta.key),
											borderTop: Boolean(slatI),
											isNarrow: cellIsNarrow,
											height: slatLiquidHeight ? void 0 : slatHeight,
											liquidHeight: slatLiquidHeight
										}))
									}),
									!forPrint && options.nowIndicator && rangeContainsMarker(props.dateProfile.currentRange, nowDate) && nowTimeMs >= props.dateProfile.slotMinTime.milliseconds && nowTimeMs < props.dateProfile.slotMaxTime.milliseconds && u(TimeGridNowIndicatorArrow, {
										nowDate,
										dateProfile: props.dateProfile,
										totalHeight: slatHeight != null ? slatHeight * slatCnt : void 0
									}),
									Boolean(rowsNotExpanding || bottomScrollbarWidth) && u("div", {
										className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: false }), classNames.borderlessX, classNames.borderlessBottom, rowsNotExpanding && classNames.liquid),
										style: { minHeight: bottomScrollbarWidth }
									})
								]
							}) })
						}),
						u("div", { className: generateClassName(options.slotHeaderDividerClass, {
							inTableHeader: false,
							options: { dayMinWidth: options.dayMinWidth }
						}) }),
						u("div", {
							className: joinClassNames(classNames.flexCol, classNames.liquid),
							children: [u(Scroller, {
								vertical: verticalScrolling,
								horizontal: true,
								hideScrollbars: footerScrollbarSticky || forPrint,
								className: joinClassNames(classNames.flexCol, classNames.rel, verticalScrolling && classNames.liquid),
								ref: this.mainScrollerRef,
								clientWidthRef: this.handleClientWidth,
								clientHeightRef: this.handleClientHeight,
								children: u("div", {
									className: joinClassNames(classNames.flexCol, classNames.grow, classNames.rel),
									style: {
										width: canvasWidth,
										height: forcedBodyHeight
									},
									children: [u(TimeGridCols, {
										dateProfile: props.dateProfile,
										nowDate: props.nowDate,
										nowMs: props.nowMs,
										todayRange: props.todayRange,
										cells: props.cells,
										slatCnt,
										forPrint,
										isHitComboAllowed: props.isHitComboAllowed,
										className: simplePrint ? "" : classNames.fill,
										fgEventSegsByCol: props.fgEventSegsByCol,
										bgEventSegsByCol: props.bgEventSegsByCol,
										businessHourSegsByCol: props.businessHourSegsByCol,
										nowIndicatorSegsByCol: props.nowIndicatorSegsByCol,
										dateSelectionSegsByCol: props.dateSelectionSegsByCol,
										eventDragByCol: props.eventDragByCol,
										eventResizeByCol: props.eventResizeByCol,
										eventSelection: props.eventSelection,
										colWidth: appliedColWidth,
										slatHeight,
										cellIsNarrow,
										cellIsMicro
									}), !simplePrint && u(S, { children: [u("div", {
										"aria-hidden": true,
										className: joinClassNames(classNames.flexCol, verticalScrolling && options.expandRows && classNames.grow, absPrint ? classNames.fillX : classNames.rel),
										children: props.slatMetas.map((slatMeta, slatI) => u("div", {
											className: joinClassNames(classNames.flexRow, slatLiquidHeight && classNames.liquid),
											style: { height: slatLiquidHeight ? "" : slatHeight },
											children: k(TimeGridSlatLane, {
												...slatMeta,
												key: slatMeta.key,
												borderTop: Boolean(slatI)
											})
										}, slatMeta.key))
									}), rowsNotExpanding && u("div", { className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: false }), classNames.borderlessX, classNames.borderlessBottom, classNames.liquid) })] })]
								})
							}), Boolean(footerScrollbarSticky) && u(FooterScrollbar, {
								isSticky: true,
								canvasWidth,
								scrollerRef: this.footScrollerRef,
								scrollbarWidthRef: this.handleStickyBottomScrollbarWidth
							})]
						})
					]
				})]
			}),
			u(Ruler, { widthRef: this.handleTotalWidth })
		] });
	}
	componentDidMount() {
		this._isUnmounting = false;
		this.initScrollers();
		this.updateSlatHeight();
	}
	componentDidUpdate() {
		this.updateScrollers();
		this.updateSlatHeight();
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.destroyScrollers();
		this.prevSlatHeight = void 0;
		setRef(this.props.slatHeightRef, null);
	}
	updateSlatHeight() {
		if (this.prevSlatHeight !== this.slatHeight) setRef(this.props.slatHeightRef, this.prevSlatHeight = this.slatHeight);
	}
	initScrollers() {
		const ScrollerSyncer = getScrollerSyncerClass(this.context.pluginHooks);
		this.dayScroller = new ScrollerSyncer(true);
		this.timeScroller = new ScrollerSyncer();
		setRef(this.props.dayScrollerRef, this.dayScroller);
		setRef(this.props.timeScrollerRef, this.timeScroller);
		this.updateScrollers();
	}
	updateScrollers() {
		this.dayScroller.handleChildren([
			this.headerScrollerRef.current,
			this.allDayScrollerRef.current,
			this.mainScrollerRef.current,
			this.footScrollerRef.current
		]);
		this.timeScroller.handleChildren([this.axisScrollerRef.current, this.mainScrollerRef.current]);
	}
	destroyScrollers() {
		setRef(this.props.dayScrollerRef, null);
		setRef(this.props.timeScrollerRef, null);
	}
};
TimeGridLayoutPannable.addPropsEquality({ headerTierHeights: isArraysEqual });
var TimeGridLayoutNormal = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.state = {};
		this.headerLabelInnerWidthRefMap = new RefMap(() => {
			afterSize(this.handleAxisInnerWidths);
		});
		this.handleAllDayLabelInnerWidth = (width) => {
			this.allDayLabelInnerWidth = width;
			afterSize(this.handleAxisInnerWidths);
		};
		this.handleWeekNumberInnerWidth = (width) => {
			this.weekNumberInnerWidth = width;
			afterSize(this.handleAxisInnerWidths);
		};
		this.slatLabelInnerWidthRefMap = new RefMap(() => {
			afterSize(this.handleAxisInnerWidths);
		});
		this.slatLabelInnerHeightRefMap = new RefMap(() => {
			afterSize(this.handleSlatInnerHeights);
		});
		this.handleTotalWidth = (totalWidth) => {
			if (this._isUnmounting) return;
			requestAnimationFrame(() => {
				if (this._isUnmounting) return;
				this.setState({ totalWidth });
			});
		};
		this.handleClientWidth = (clientWidth) => {
			if (this._isUnmounting) return;
			this.setState({ clientWidth });
		};
		this.handleClientHeight = (clientHeight) => {
			if (this._isUnmounting) return;
			this.setState({ clientHeight });
		};
		this.handleAxisInnerWidths = () => {
			if (this._isUnmounting) return;
			const headerLabelInnerWidthMap = this.headerLabelInnerWidthRefMap.current;
			const slatLabelInnerWidthMap = this.slatLabelInnerWidthRefMap.current;
			let max = Math.max(this.weekNumberInnerWidth || 0, this.allDayLabelInnerWidth || 0);
			for (const headerLabelInnerWidth of headerLabelInnerWidthMap.values()) max = Math.max(max, headerLabelInnerWidth);
			for (const slatLabelInnerWidth of slatLabelInnerWidthMap.values()) max = Math.max(max, slatLabelInnerWidth);
			if (this.state.axisWidth !== max) this.setState({ axisWidth: max });
		};
		this.handleSlatInnerHeights = () => {
			if (this._isUnmounting) return;
			const slatLabelInnerHeightMap = this.slatLabelInnerHeightRefMap.current;
			let max = 0;
			for (const slatLabelInnerHeight of slatLabelInnerHeightMap.values()) max = Math.max(max, slatLabelInnerHeight);
			if (this.state.slatInnerHeight !== max) this.setState({ slatInnerHeight: max });
		};
	}
	render() {
		const { props, state, context, slatLabelInnerWidthRefMap, slatLabelInnerHeightRefMap, headerLabelInnerWidthRefMap } = this;
		const { nowDate, forPrint } = props;
		const nowTimeMs = nowDate.valueOf() - startOfDay(nowDate).valueOf();
		const { axisWidth, clientWidth, totalWidth } = state;
		const { options } = context;
		const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
		const endScrollbarWidth = totalWidth != null && clientWidth != null && !forPrint ? totalWidth - clientWidth : void 0;
		const verticalScrolling = !forPrint && !getIsHeightAuto(options);
		const tableHeaderSticky = !forPrint && getTableHeaderSticky(options);
		const slatCnt = props.slatMetas.length;
		const [slatHeight, slatLiquidHeight] = computeSlatHeight(verticalScrolling && options.expandRows, slatCnt, options.slotMinHeight, state.slatInnerHeight, state.clientHeight);
		this.slatHeight = slatHeight;
		const totalSlatHeight = (slatHeight || 0) * slatCnt;
		const rowsNotExpanding = verticalScrolling && !options.expandRows && state.clientHeight != null && state.clientHeight > totalSlatHeight;
		const printStackEnabled = computeTimeGridPrintMode(forPrint, options.eventPrintLayout) === "stack";
		const absPrint = forPrint && !printStackEnabled;
		const simplePrint = forPrint && printStackEnabled;
		const forcedBodyHeight = absPrint ? totalSlatHeight : void 0;
		const colCount = props.cells.length;
		const measuredColWidth = clientWidth != null ? clientWidth / colCount : void 0;
		const cellIsMicro = measuredColWidth != null && measuredColWidth <= 60;
		const cellIsNarrow = cellIsMicro || measuredColWidth != null && measuredColWidth <= options.dayNarrowWidth;
		return u(S, { children: [
			options.dayHeaders && u("div", {
				role: "rowgroup",
				className: joinClassNames(generateClassName(options.tableHeaderClass, {
					isSticky: tableHeaderSticky,
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), classNames.flexCol, tableHeaderSticky && classNames.tableHeaderSticky, classNames.z1),
				children: [props.headerTiers.map((rowConfig, tierNum) => u("div", {
					role: "row",
					className: classNames.flexRow,
					children: [
						u("div", {
							className: joinClassNames(options.dayHeaderRowClass, classNames.flexRow, classNames.borderlessX, classNames.borderlessTop, tierNum === props.headerTiers.length - 1 && classNames.borderlessBottom),
							children: options.weekNumbers && rowConfig.isDateRow ? u(TimeGridWeekNumber, {
								dateProfile: props.dateProfile,
								innerWidthRef: this.handleWeekNumberInnerWidth,
								innerHeightRef: headerLabelInnerWidthRefMap.createRef(tierNum),
								width: axisWidth,
								isLiquid: false,
								isNarrow: cellIsNarrow
							}) : u(TimeGridAxisEmpty, {
								width: axisWidth,
								isLiquid: false
							})
						}),
						u("div", { className: generateClassName(options.slotHeaderDividerClass, {
							inTableHeader: true,
							options: { dayMinWidth: options.dayMinWidth }
						}) }),
						u(DayGridHeaderRow, {
							...rowConfig,
							className: classNames.liquid,
							borderBottom: tierNum < props.headerTiers.length - 1,
							viewportWidth: clientWidth,
							cellIsNarrow,
							cellIsMicro,
							rowLevel: props.headerTiers.length - tierNum - 1
						}),
						Boolean(endScrollbarWidth) && u("div", {
							className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: true }), classNames.borderlessY, classNames.borderlessEnd),
							style: { minWidth: endScrollbarWidth }
						})
					]
				}, tierNum)), u("div", { className: generateClassName(options.dayHeaderDividerClass, {
					isSticky: tableHeaderSticky,
					multiMonthColumns: 0,
					options: { allDaySlot: Boolean(options.allDaySlot) }
				}) })]
			}),
			u("div", {
				role: "rowgroup",
				className: joinClassNames(generateClassName(options.tableBodyClass, {
					borderlessX,
					borderlessTop,
					borderlessBottom,
					multiMonthColumns: 0
				}), classNames.flexCol, verticalScrolling && classNames.liquid, classNames.isolate, classNames.z0),
				children: [options.allDaySlot && u(S, { children: [u("div", {
					role: "row",
					className: joinClassNames(classNames.flexRow, classNames.z1),
					children: [
						u(TimeGridAllDayHeader, {
							width: axisWidth,
							innerWidthRef: this.handleAllDayLabelInnerWidth,
							isNarrow: cellIsNarrow
						}),
						u("div", { className: generateClassName(options.slotHeaderDividerClass, {
							inTableHeader: false,
							options: { dayMinWidth: options.dayMinWidth }
						}) }),
						u(TimeGridAllDayLane, {
							dateProfile: props.dateProfile,
							todayRange: props.todayRange,
							cells: props.cells,
							showDayNumbers: false,
							forPrint,
							isHitComboAllowed: props.isHitComboAllowed,
							className: joinClassNames(classNames.liquidX, classNames.borderless),
							cellIsNarrow,
							cellIsMicro,
							fgEventSegs: props.fgEventSegs,
							bgEventSegs: props.bgEventSegs,
							businessHourSegs: props.businessHourSegs,
							dateSelectionSegs: props.dateSelectionSegs,
							eventDrag: props.eventDrag,
							eventResize: props.eventResize,
							eventSelection: props.eventSelection,
							dayMaxEvents: props.dayMaxEvents,
							dayMaxEventRows: props.dayMaxEventRows
						}),
						Boolean(endScrollbarWidth) && u("div", {
							className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: false }), classNames.borderlessY, classNames.borderlessEnd),
							style: { minWidth: endScrollbarWidth }
						})
					]
				}), u("div", { className: joinClassNames(options.allDayDividerClass, classNames.z2) })] }), u(Scroller, {
					vertical: verticalScrolling,
					className: joinClassNames(classNames.flexCol, classNames.rel, verticalScrolling && classNames.liquid, classNames.z0),
					ref: props.timeScrollerRef,
					clientWidthRef: this.handleClientWidth,
					clientHeightRef: this.handleClientHeight,
					children: u("div", {
						className: joinClassNames(classNames.flexCol, classNames.grow, classNames.rel),
						style: { height: forcedBodyHeight },
						children: [u("div", {
							role: "row",
							className: joinClassNames(classNames.flexRow, !simplePrint && classNames.fill),
							children: [
								u("div", {
									role: "rowheader",
									"aria-label": options.timedText,
									className: classNames.contentBox,
									style: { width: axisWidth }
								}),
								u("div", { className: generateClassName(options.slotHeaderDividerClass, {
									inTableHeader: false,
									options: { dayMinWidth: options.dayMinWidth }
								}) }),
								u(TimeGridCols, {
									dateProfile: props.dateProfile,
									nowDate: props.nowDate,
									nowMs: props.nowMs,
									todayRange: props.todayRange,
									cells: props.cells,
									slatCnt,
									forPrint,
									isHitComboAllowed: props.isHitComboAllowed,
									className: classNames.liquid,
									fgEventSegsByCol: props.fgEventSegsByCol,
									bgEventSegsByCol: props.bgEventSegsByCol,
									businessHourSegsByCol: props.businessHourSegsByCol,
									nowIndicatorSegsByCol: props.nowIndicatorSegsByCol,
									dateSelectionSegsByCol: props.dateSelectionSegsByCol,
									eventDragByCol: props.eventDragByCol,
									eventResizeByCol: props.eventResizeByCol,
									eventSelection: props.eventSelection,
									slatHeight,
									cellIsNarrow,
									cellIsMicro
								})
							]
						}), !simplePrint && u(S, { children: [
							u("div", {
								"aria-hidden": true,
								className: joinClassNames(classNames.flexCol, verticalScrolling && options.expandRows && classNames.grow, absPrint ? classNames.fillX : classNames.rel),
								children: props.slatMetas.map((slatMeta, slatI) => u("div", {
									className: joinClassNames(slatLiquidHeight && classNames.liquid, classNames.flexRow),
									style: { height: slatLiquidHeight ? void 0 : slatHeight },
									children: [
										u("div", {
											className: classNames.flexCol,
											style: { width: axisWidth },
											children: k(TimeGridSlatHeader, {
												...slatMeta,
												key: slatMeta.key,
												innerWidthRef: slatLabelInnerWidthRefMap.createRef(slatMeta.key),
												innerHeightRef: slatLabelInnerHeightRefMap.createRef(slatMeta.key),
												borderTop: Boolean(slatI),
												isNarrow: cellIsNarrow
											})
										}),
										u("div", {
											className: generateClassName(options.slotHeaderDividerClass, {
												inTableHeader: false,
												options: { dayMinWidth: options.dayMinWidth }
											}),
											style: { visibility: "hidden" }
										}),
										k(TimeGridSlatLane, {
											...slatMeta,
											key: slatMeta.key,
											borderTop: Boolean(slatI)
										})
									]
								}, slatMeta.key))
							}),
							rowsNotExpanding && u("div", { className: joinClassNames(generateClassName(options.fillerClass, { inTableHeader: false }), classNames.borderlessX, classNames.borderlessBottom, classNames.liquid) }),
							!forPrint && options.nowIndicator && rangeContainsMarker(props.dateProfile.currentRange, nowDate) && nowTimeMs >= props.dateProfile.slotMinTime.milliseconds && nowTimeMs < props.dateProfile.slotMaxTime.milliseconds && u(TimeGridNowIndicatorArrow, {
								nowDate,
								dateProfile: props.dateProfile,
								totalHeight: slatHeight != null ? slatHeight * slatCnt : void 0
							})
						] })]
					})
				})]
			}),
			u(Ruler, { widthRef: this.handleTotalWidth })
		] });
	}
	componentDidMount() {
		this._isUnmounting = false;
		this.updateSlatHeight();
	}
	componentDidUpdate() {
		this.updateSlatHeight();
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.prevSlatHeight = void 0;
		setRef(this.props.slatHeightRef, null);
	}
	updateSlatHeight() {
		if (this.prevSlatHeight !== this.slatHeight) setRef(this.props.slatHeightRef, this.prevSlatHeight = this.slatHeight);
	}
};
function buildEmptySegCols(segsByCol) {
	return segsByCol.map(() => []);
}
function buildEmptyInteractionCols(interactionsByCol) {
	return interactionsByCol.map(() => null);
}
var TimeGridLayout = class extends BaseComponent {
	constructor() {
		super(...arguments);
		this.buildSlatMetas = memoize(buildSlatMetas);
		this.dayScrollerRef = M();
		this.timeScrollerRef = M();
		this.scrollState = {};
		this.handleSlatHeight = (slatHeight) => {
			if (this._isUnmounting) return;
			this.slatHeight = slatHeight;
			if (slatHeight != null) afterSize(this.applyTimeScroll);
		};
		this.handleTimeScrollRequest = (scrollTime) => {
			this.scrollState.time = scrollTime;
			this.scrollState.y = void 0;
			this.applyTimeScroll();
		};
		this.handleTimeScrollEnd = (isDevice) => {
			if (isDevice) {
				const y = this.timeScrollerRef.current.y;
				if (!this.props.forPrint) {
					this.scrollState.y = y;
					this.scrollState.time = void 0;
				}
			}
		};
		this.applyTimeScroll = () => {
			const timeScroller = this.timeScrollerRef.current;
			const { slatHeight, scrollState } = this;
			let { y, time } = scrollState;
			if (y == null && time && slatHeight != null && timeScroller) {
				y = computeTimeTopFrac(time, this.props.dateProfile) * (slatHeight * this.currentSlatCnt);
				if (y) y++;
				scrollState.y = y;
			}
			if (y != null) timeScroller.scrollTo({ y });
		};
	}
	render() {
		const { props, context } = this;
		const { dateProfile } = props;
		const { options, dateEnv } = context;
		const { dayMinWidth } = options;
		const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
		const slatMetas = this.buildSlatMetas(dateProfile.slotMinTime, dateProfile.slotMaxTime, options.slotHeaderInterval, options.slotDuration, dateEnv);
		this.currentSlatCnt = slatMetas.length;
		const dateSelectionSegs = props.forPrint ? [] : props.dateSelectionSegs;
		const eventDrag = props.forPrint ? null : props.eventDrag;
		const eventResize = props.forPrint ? null : props.eventResize;
		const dateSelectionSegsByCol = props.forPrint ? buildEmptySegCols(props.dateSelectionSegsByCol) : props.dateSelectionSegsByCol;
		const eventDragByCol = props.forPrint ? buildEmptyInteractionCols(props.eventDragByCol) : props.eventDragByCol;
		const eventResizeByCol = props.forPrint ? buildEmptyInteractionCols(props.eventResizeByCol) : props.eventResizeByCol;
		const commonLayoutProps = {
			dateProfile,
			nowDate: props.nowDate,
			nowMs: props.nowMs,
			todayRange: props.todayRange,
			cells: props.cells,
			slatMetas,
			forPrint: props.forPrint,
			isHitComboAllowed: props.isHitComboAllowed,
			headerTiers: props.headerTiers,
			fgEventSegs: props.fgEventSegs,
			bgEventSegs: props.bgEventSegs,
			businessHourSegs: props.businessHourSegs,
			dateSelectionSegs,
			eventDrag,
			eventResize,
			...getAllDayMaxEventProps(options),
			fgEventSegsByCol: props.fgEventSegsByCol,
			bgEventSegsByCol: props.bgEventSegsByCol,
			businessHourSegsByCol: props.businessHourSegsByCol,
			nowIndicatorSegsByCol: props.nowIndicatorSegsByCol,
			dateSelectionSegsByCol,
			eventDragByCol,
			eventResizeByCol,
			eventSelection: props.eventSelection,
			timeScrollerRef: this.timeScrollerRef,
			timeScrollState: this.scrollState,
			slatHeightRef: this.handleSlatHeight,
			borderlessX,
			borderlessBottom
		};
		return u(ViewContainer, {
			attrs: {
				role: "grid",
				"aria-colcount": props.cells.length,
				"aria-labelledby": props.labelId,
				"aria-label": props.labelStr
			},
			className: joinClassNames(props.className, generateClassName(options.tableClass, {
				borderlessX,
				borderlessTop,
				borderlessBottom,
				multiMonthColumns: 0
			}), !props.forPrint && classNames.flexCol, classNames.isolate),
			viewSpec: context.viewSpec,
			children: dayMinWidth ? u(TimeGridLayoutPannable, {
				...commonLayoutProps,
				dayMinWidth,
				dayScrollerRef: this.dayScrollerRef
			}) : u(TimeGridLayoutNormal, { ...commonLayoutProps })
		});
	}
	componentDidMount() {
		this._isUnmounting = false;
		this.resetScroll();
		this.context.emitter.on("_timeScrollRequest", this.handleTimeScrollRequest);
		const timeScroller = this.timeScrollerRef.current;
		if (timeScroller) timeScroller.addScrollEndListener(this.handleTimeScrollEnd);
	}
	componentDidUpdate(prevProps) {
		if (prevProps.dateProfile !== this.props.dateProfile && this.context.options.scrollTimeReset) this.resetScroll();
		else if (prevProps.forPrint && !this.props.forPrint) this.applyTimeScroll();
	}
	componentWillUnmount() {
		this._isUnmounting = true;
		this.context.emitter.off("_timeScrollRequest", this.handleTimeScrollRequest);
		const timeScroller = this.timeScrollerRef.current;
		if (timeScroller) timeScroller.removeScrollEndListener(this.handleTimeScrollEnd);
	}
	resetScroll() {
		this.handleTimeScrollRequest(this.context.options.scrollTime);
		const dayScroller = this.dayScrollerRef.current;
		if (dayScroller) dayScroller.scrollTo({ x: 0 });
	}
};
var AUTO_ALL_DAY_MAX_EVENT_ROWS = 5;
function getAllDayMaxEventProps(options) {
	let { dayMaxEvents, dayMaxEventRows } = options;
	if (dayMaxEvents === true || dayMaxEventRows === true) {
		dayMaxEvents = void 0;
		dayMaxEventRows = AUTO_ALL_DAY_MAX_EVENT_ROWS;
	}
	return {
		dayMaxEvents,
		dayMaxEventRows
	};
}
//#endregion
//#region node_modules/fullcalendar/chunks/4ec5fb96.js
var TimeGridView = class extends DateComponent {
	constructor() {
		super(...arguments);
		this.createDayHeaderFormatter = memoize(createDayHeaderFormatter);
		this.buildDaySeries = memoize((dateProfile, dateProfileGenerator) => new DaySeriesModel(dateProfile.renderRange, dateProfileGenerator));
		this.buildDayCols = memoize(buildDayColsFromSeries);
		this.extractColDates = memoize((cols) => cols.map((col) => col.date));
		this.extractColRanges = memoize((cols) => cols.map((col) => col.range));
		this.buildDateRowConfigs = memoize(buildDateRowConfigs);
		this.splitFgEventSegs = memoize(organizeSegsByCol);
		this.splitBgEventSegs = memoize(organizeSegsByCol);
		this.splitBusinessHourSegs = memoize(organizeSegsByCol);
		this.splitNowIndicatorSegs = memoize(organizeSegsByCol);
		this.splitDateSelectionSegs = memoize(organizeSegsByCol);
		this.splitEventDrag = memoize(splitInteractionByCol);
		this.splitEventResize = memoize(splitInteractionByCol);
		this.allDaySplitter = new AllDaySplitter();
		this.daySeriesSlicer = new DaySeriesSlicer();
		this.dayTimeColsSlicer = new DayTimeColsSlicer();
	}
	render() {
		const { props, context } = this;
		const { dateProfile } = props;
		const { options, dateProfileGenerator } = context;
		const daySeries = this.buildDaySeries(dateProfile, dateProfileGenerator);
		const cols = this.buildDayCols(daySeries, context.dateEnv, {
			slotRange: dateProfile,
			activeRange: dateProfile.activeRange
		});
		const colDates = this.extractColDates(cols);
		const dayRanges = this.extractColRanges(cols);
		const splitProps = this.allDaySplitter.splitProps(props);
		const allDayProps = this.daySeriesSlicer.sliceProps(splitProps.allDay, dateProfile, options.nextDayThreshold, context, daySeries);
		const timedProps = this.dayTimeColsSlicer.sliceProps(splitProps.timed, dateProfile, null, context, dayRanges);
		const dayHeaderFormat = this.createDayHeaderFormatter(context.options.dayHeaderFormat, true, cols.length);
		return u(NowTimer, {
			unit: options.nowIndicator ? "minute" : "day",
			children: (nowDate, todayRange, nowMs) => {
				const colCount = cols.length;
				const nowIndicatorSeg = !props.forPrint && options.nowIndicator && this.dayTimeColsSlicer.sliceNowDate(nowDate, dateProfile, options.nextDayThreshold, context, dayRanges);
				const fgEventSegsByCol = this.splitFgEventSegs(timedProps.fgEventSegs, colCount);
				const bgEventSegsByCol = this.splitBgEventSegs(timedProps.bgEventSegs, colCount);
				const businessHourSegsByCol = this.splitBusinessHourSegs(timedProps.businessHourSegs, colCount);
				const nowIndicatorSegsByCol = this.splitNowIndicatorSegs(nowIndicatorSeg, colCount);
				const dateSelectionSegsByCol = this.splitDateSelectionSegs(timedProps.dateSelectionSegs, colCount);
				const eventDragByCol = this.splitEventDrag(timedProps.eventDrag, colCount);
				const eventResizeByCol = this.splitEventResize(timedProps.eventResize, colCount);
				const headerTiers = this.buildDateRowConfigs(colDates, true, props.dateProfile, todayRange, dayHeaderFormat, context);
				return u(TimeGridLayout, {
					labelId: props.labelId,
					labelStr: props.labelStr,
					dateProfile,
					nowDate,
					nowMs,
					todayRange,
					cells: cols,
					forPrint: props.forPrint,
					className: props.className,
					headerTiers,
					fgEventSegs: allDayProps.fgEventSegs,
					bgEventSegs: allDayProps.bgEventSegs,
					businessHourSegs: allDayProps.businessHourSegs,
					dateSelectionSegs: allDayProps.dateSelectionSegs,
					eventDrag: allDayProps.eventDrag,
					eventResize: allDayProps.eventResize,
					fgEventSegsByCol,
					bgEventSegsByCol,
					businessHourSegsByCol,
					nowIndicatorSegsByCol,
					dateSelectionSegsByCol,
					eventDragByCol,
					eventResizeByCol,
					eventSelection: props.eventSelection
				});
			}
		});
	}
};
var timeGridPlugin = {
	name: "timegrid",
	initialView: "timeGridWeek",
	deps: [dayGridPlugin],
	views: {
		timeGrid: {
			component: TimeGridView,
			usesMinMaxTime: true,
			allDaySlot: true,
			slotDuration: "00:30:00",
			slotEventOverlap: true
		},
		timeGridDay: {
			type: "timeGrid",
			duration: { days: 1 }
		},
		timeGridWeek: {
			type: "timeGrid",
			duration: { weeks: 1 }
		}
	}
};
//#endregion
export { timeGridPlugin as default };
